--!strict
-- UIFactory: helpers to build common Instance-based UI objects. Stub for Step 1.

local UIFactory = {}

local deps: any = nil

function UIFactory.init(injected: any)
	deps = injected
end

function UIFactory.start() end

return UIFactory
