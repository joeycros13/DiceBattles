--!strict
-- MenuShell: ScreenGui with three bottom tabs (Equipment, Battle, Talents)
-- and tab switching. Stub for Step 1.

local MenuShell = {}

local deps: any = nil

function MenuShell.init(injected: any)
	deps = injected
end

function MenuShell.start() end

return MenuShell
