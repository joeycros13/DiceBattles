--!strict
-- TalentsTab: talent list (level + rarity) and Roll button. Stub for Step 1.

local TalentsTab = {}

local deps: any = nil

function TalentsTab.init(injected: any)
	deps = injected
end

function TalentsTab.start() end

return TalentsTab
