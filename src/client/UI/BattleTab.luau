--!strict
-- BattleTab: current chapter view, scrollable chapter list, Start Battle.
-- Stub for Step 1.

local BattleTab = {}

local deps: any = nil

function BattleTab.init(injected: any)
	deps = injected
end

function BattleTab.start() end

return BattleTab
