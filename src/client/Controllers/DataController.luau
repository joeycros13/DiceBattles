--!strict
-- DataController: mirrors replicated player data client-side. Stub for Step 1.

local DataController = {}

local deps: any = nil

function DataController.init(injected: any)
	deps = injected
end

function DataController.start() end

return DataController
