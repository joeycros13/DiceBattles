--!strict
-- TalentService: talent rolls (coin cost, rarity-weighted +1). Stub for Step 1.

local TalentService = {}

local deps: any = nil

function TalentService.init(injected: any)
	deps = injected
end

function TalentService.start() end

return TalentService
