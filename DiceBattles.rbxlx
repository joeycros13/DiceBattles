<roblox xmlns:xmime="http://www.w3.org/2005/05/xmlmime" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://www.roblox.com/roblox.xsd" version="4">
	<External>null</External>
	<External>nil</External>
	<Item class="Workspace" referent="10">
		<Properties>
			<float name="AirDensity">0.00120000006</float>
			<float name="AirTurbulenceIntensity">0</float>
			<bool name="AllowThirdPartySales">false</bool>
			<token name="AuthorityMode">1</token>
			<token name="AvatarUnificationMode">0</token>
			<token name="ClientAnimatorThrottling">0</token>
			<BinaryString name="CollisionGroupData">AQEABP////8HRGVmYXVsdA==</BinaryString>
			<Ref name="CurrentCamera">RBXB928A3212E0D46BD8A563626BA60DAA9</Ref>
			<double name="DistributedGameTime">0</double>
			<token name="EnableSLIMAvatars">0</token>
			<bool name="ExplicitAutoJoints">true</bool>
			<bool name="FallHeightEnabled">true</bool>
			<float name="FallenPartsDestroyHeight">-500</float>
			<token name="FluidForces">0</token>
			<Vector3 name="GlobalWind">
				<X>0</X>
				<Y>0</Y>
				<Z>0</Z>
			</Vector3>
			<float name="Gravity">196.199997</float>
			<token name="IKControlConstraintSupport">0</token>
			<token name="ImprovedAnimationConstraint">0</token>
			<token name="ImprovedPhysicsReplication">0</token>
			<token name="LayeredClothingCacheOptimizations">0</token>
			<token name="LuauTypeCheckMode">0</token>
			<token name="MeshPartHeadsAndAccessories">0</token>
			<token name="MeshStreamingAndImprovedLods">0</token>
			<token name="ModelStreamingBehavior">0</token>
			<token name="NextGenerationReplication">0</token>
			<token name="PathfindingUseImprovedSearch">0</token>
			<token name="PhysicsSteppingMethod">0</token>
			<token name="PlayerCharacterDestroyBehavior">0</token>
			<token name="PlayerScriptsUseInputActionSystem">0</token>
			<token name="PrimalPhysicsSolver">0</token>
			<token name="RejectCharacterDeletions">0</token>
			<token name="RenderingCacheOptimizations">0</token>
			<token name="ReplicateInstanceDestroySetting">0</token>
			<token name="Retargeting">0</token>
			<token name="SandboxedInstanceMode">0</token>
			<token name="SignalBehavior2">0</token>
			<token name="StreamOutBehavior">0</token>
			<bool name="StreamingEnabled">false</bool>
			<token name="StreamingIntegrityMode">0</token>
			<int name="StreamingMinRadius">64</int>
			<int name="StreamingTargetRadius">1024</int>
			<bool name="TerrainWeldsFixed">true</bool>
			<token name="TouchEventsUseCollisionGroups">0</token>
			<bool name="TouchesUseCollisionGroups">false</bool>
			<token name="UseFixedSimulation">0</token>
			<token name="UseNewLuauTypeSolver">2</token>
			<token name="ValidateEnabledProximityPrompt">0</token>
			<token name="LevelOfDetail">0</token>
			<CoordinateFrame name="ModelMeshCFrame">
				<X>0</X>
				<Y>0</Y>
				<Z>0</Z>
				<R00>1</R00>
				<R01>0</R01>
				<R02>0</R02>
				<R10>0</R10>
				<R11>1</R11>
				<R12>0</R12>
				<R20>0</R20>
				<R21>0</R21>
				<R22>1</R22>
			</CoordinateFrame>
			<SharedString name="ModelMeshData">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<Vector3 name="ModelMeshSize">
				<X>0</X>
				<Y>0</Y>
				<Z>0</Z>
			</Vector3>
			<token name="ModelStreamingMode">0</token>
			<bool name="NeedsPivotMigration">false</bool>
			<Ref name="PrimaryPart">null</Ref>
			<float name="ScaleFactor">1</float>
			<SharedString name="SlimHash">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<OptionalCoordinateFrame name="WorldPivotData"></OptionalCoordinateFrame>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">Workspace</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000002</UniqueId>
		</Properties>
		<Item class="Camera" referent="RBXB928A3212E0D46BD8A563626BA60DAA9">
			<Properties>
				<CoordinateFrame name="CFrame">
					<X>45.8838348</X>
					<Y>43.4341087</Y>
					<Z>61.5336151</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>-0</R02>
					<R10>-0</R10>
					<R11>0.780868888</R11>
					<R12>0.624694943</R12>
					<R20>0</R20>
					<R21>-0.624694943</R21>
					<R22>0.780868888</R22>
				</CoordinateFrame>
				<Ref name="CameraSubject">null</Ref>
				<token name="CameraType">0</token>
				<float name="FieldOfView">70</float>
				<token name="FieldOfViewMode">0</token>
				<CoordinateFrame name="Focus">
					<X>45.8838348</X>
					<Y>23.4341164</Y>
					<Z>36.5336189</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>1</R11>
					<R12>0</R12>
					<R20>0</R20>
					<R21>0</R21>
					<R22>1</R22>
				</CoordinateFrame>
				<bool name="HeadLocked">true</bool>
				<float name="HeadScale">1</float>
				<bool name="VRTiltAndRollEnabled">false</bool>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">Camera</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003a1</UniqueId>
			</Properties>
		</Item>
		<Item class="Part" referent="11">
			<Properties>
				<token name="shape">1</token>
				<token name="formFactorRaw">1</token>
				<bool name="Anchored">true</bool>
				<bool name="AudioCanCollide">true</bool>
				<float name="BackParamA">-0.5</float>
				<float name="BackParamB">0.5</float>
				<token name="BackSurface">0</token>
				<token name="BackSurfaceInput">0</token>
				<float name="BottomParamA">-0.5</float>
				<float name="BottomParamB">0.5</float>
				<token name="BottomSurface">4</token>
				<token name="BottomSurfaceInput">0</token>
				<CoordinateFrame name="CFrame">
					<X>0</X>
					<Y>-10</Y>
					<Z>0</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>1</R11>
					<R12>0</R12>
					<R20>0</R20>
					<R21>0</R21>
					<R22>1</R22>
				</CoordinateFrame>
				<bool name="CanCollide">true</bool>
				<bool name="CanQuery">true</bool>
				<bool name="CanTouch">true</bool>
				<bool name="CastShadow">true</bool>
				<string name="CollisionGroup">Default</string>
				<int name="CollisionGroupId">0</int>
				<Color3uint8 name="Color3uint8">4284636770</Color3uint8>
				<PhysicalProperties name="CustomPhysicalProperties">
					<CustomPhysics>false</CustomPhysics>
				</PhysicalProperties>
				<bool name="EnableFluidForces">true</bool>
				<float name="FrontParamA">-0.5</float>
				<float name="FrontParamB">0.5</float>
				<token name="FrontSurface">0</token>
				<token name="FrontSurfaceInput">0</token>
				<float name="LeftParamA">-0.5</float>
				<float name="LeftParamB">0.5</float>
				<token name="LeftSurface">0</token>
				<token name="LeftSurfaceInput">0</token>
				<bool name="Locked">true</bool>
				<bool name="Massless">false</bool>
				<token name="Material">256</token>
				<string name="MaterialVariantSerialized"></string>
				<CoordinateFrame name="PivotOffset">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>1</R11>
					<R12>0</R12>
					<R20>0</R20>
					<R21>0</R21>
					<R22>1</R22>
				</CoordinateFrame>
				<float name="Reflectance">0</float>
				<float name="RightParamA">-0.5</float>
				<float name="RightParamB">0.5</float>
				<token name="RightSurface">0</token>
				<token name="RightSurfaceInput">0</token>
				<int name="RootPriority">0</int>
				<Vector3 name="RotVelocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<float name="TopParamA">-0.5</float>
				<float name="TopParamB">0.5</float>
				<token name="TopSurface">3</token>
				<token name="TopSurfaceInput">0</token>
				<float name="Transparency">0</float>
				<Vector3 name="Velocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<Vector3 name="size">
					<X>512</X>
					<Y>20</Y>
					<Z>512</Z>
				</Vector3>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">Baseplate</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003ba</UniqueId>
			</Properties>
		</Item>
		<Item class="Terrain" referent="RBX6643276990E14FA5B3E688D26C7E9536">
			<Properties>
				<token name="AcquisitionMethod">0</token>
				<bool name="Decoration">false</bool>
				<float name="GrassLength">0.699999988</float>
				<BinaryString name="MaterialColors"><![CDATA[AAAAAAAAan8/P39rf2Y/ilY+j35fi21PZmxvZbDqw8faiVpHOi4kHh4lZlw76JxKc3trhHta
gcLgc4RKxr21zq2UlJSM]]></BinaryString>
				<SharedString name="Materials">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<BinaryString name="PhysicsGrid">AgMAAAAAAAAAAAAAAAA=</BinaryString>
				<BinaryString name="SmoothGrid">AQU=</BinaryString>
				<bool name="SmoothVoxelsUpgraded">false</bool>
				<Color3 name="WaterColor">
					<R>0.0500000007</R>
					<G>0.330000013</G>
					<B>0.360000014</B>
				</Color3>
				<float name="WaterReflectance">1</float>
				<float name="WaterTransparency">0.300000012</float>
				<float name="WaterWaveSize">0.150000006</float>
				<float name="WaterWaveSpeed">10</float>
				<bool name="Anchored">true</bool>
				<bool name="AudioCanCollide">true</bool>
				<float name="BackParamA">-0.5</float>
				<float name="BackParamB">0.5</float>
				<token name="BackSurface">0</token>
				<token name="BackSurfaceInput">0</token>
				<float name="BottomParamA">-0.5</float>
				<float name="BottomParamB">0.5</float>
				<token name="BottomSurface">4</token>
				<token name="BottomSurfaceInput">0</token>
				<CoordinateFrame name="CFrame">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>1</R11>
					<R12>0</R12>
					<R20>0</R20>
					<R21>0</R21>
					<R22>1</R22>
				</CoordinateFrame>
				<bool name="CanCollide">true</bool>
				<bool name="CanQuery">true</bool>
				<bool name="CanTouch">true</bool>
				<bool name="CastShadow">true</bool>
				<string name="CollisionGroup">Default</string>
				<int name="CollisionGroupId">0</int>
				<Color3uint8 name="Color3uint8">4288914085</Color3uint8>
				<PhysicalProperties name="CustomPhysicalProperties">
					<CustomPhysics>false</CustomPhysics>
				</PhysicalProperties>
				<bool name="EnableFluidForces">true</bool>
				<float name="FrontParamA">-0.5</float>
				<float name="FrontParamB">0.5</float>
				<token name="FrontSurface">0</token>
				<token name="FrontSurfaceInput">0</token>
				<float name="LeftParamA">-0.5</float>
				<float name="LeftParamB">0.5</float>
				<token name="LeftSurface">0</token>
				<token name="LeftSurfaceInput">0</token>
				<bool name="Locked">true</bool>
				<bool name="Massless">false</bool>
				<token name="Material">256</token>
				<string name="MaterialVariantSerialized"></string>
				<CoordinateFrame name="PivotOffset">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
					<R00>1</R00>
					<R01>0</R01>
					<R02>0</R02>
					<R10>0</R10>
					<R11>1</R11>
					<R12>0</R12>
					<R20>0</R20>
					<R21>0</R21>
					<R22>1</R22>
				</CoordinateFrame>
				<float name="Reflectance">0</float>
				<float name="RightParamA">-0.5</float>
				<float name="RightParamB">0.5</float>
				<token name="RightSurface">0</token>
				<token name="RightSurfaceInput">0</token>
				<int name="RootPriority">0</int>
				<Vector3 name="RotVelocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<float name="TopParamA">-0.5</float>
				<float name="TopParamB">0.5</float>
				<token name="TopSurface">3</token>
				<token name="TopSurfaceInput">0</token>
				<float name="Transparency">0</float>
				<Vector3 name="Velocity">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<Vector3 name="size">
					<X>2044</X>
					<Y>252</Y>
					<Z>2044</Z>
				</Vector3>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">Terrain</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003bb</UniqueId>
			</Properties>
		</Item>
	</Item>
	<Item class="TimerService" referent="RBX1D92DD5C4AA54A0AAD1A5576AA12105F">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">TimerService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000338</UniqueId>
		</Properties>
	</Item>
	<Item class="CollectionService" referent="RBX348B389CB71D48CA92F9AB91BC392CF2">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">CollectionService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000339</UniqueId>
		</Properties>
	</Item>
	<Item class="SoundService" referent="6">
		<Properties>
			<bool name="AcousticSimulationEnabled">false</bool>
			<token name="AmbientReverb">0</token>
			<token name="AudioApiByDefault">0</token>
			<token name="CharacterSoundsUseNewApi">0</token>
			<token name="DefaultListenerLocation">0</token>
			<bool name="DiffractionEnabled">true</bool>
			<float name="DistanceFactor">3.32999992</float>
			<float name="DopplerScale">1</float>
			<bool name="IsNewExpForAudioApiByDefault">false</bool>
			<CoordinateFrame name="ListenerCFrame">
				<X>0</X>
				<Y>0</Y>
				<Z>0</Z>
				<R00>1</R00>
				<R01>0</R01>
				<R02>0</R02>
				<R10>0</R10>
				<R11>1</R11>
				<R12>0</R12>
				<R20>0</R20>
				<R21>0</R21>
				<R22>1</R22>
			</CoordinateFrame>
			<Ref name="ListenerObject">null</Ref>
			<token name="ListenerType">0</token>
			<bool name="OcclusionEnabled">true</bool>
			<bool name="RespectFilteringEnabled">true</bool>
			<bool name="ReverbEnabled">true</bool>
			<float name="RolloffScale">1</float>
			<token name="VolumetricAudio">1</token>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">SoundService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000033a</UniqueId>
		</Properties>
	</Item>
	<Item class="VideoCaptureService" referent="RBX0B1FA4C6E6704A038F56C0016FA798FC">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">VideoCaptureService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000346</UniqueId>
		</Properties>
	</Item>
	<Item class="NonReplicatedCSGDictionaryService" referent="RBX7DA0003B55504553ADD4156098BBF443">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">NonReplicatedCSGDictionaryService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000347</UniqueId>
		</Properties>
	</Item>
	<Item class="CSGDictionaryService" referent="RBX24D3E4D95F9149479AC276EBDB94CC0E">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">CSGDictionaryService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000348</UniqueId>
		</Properties>
	</Item>
	<Item class="Chat" referent="RBXA00297AC3B274B458626C5832363C3FF">
		<Properties>
			<bool name="BubbleChatEnabled">false</bool>
			<bool name="IsAutoMigrated">false</bool>
			<bool name="LoadDefaultChat">true</bool>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">Chat</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000034e</UniqueId>
		</Properties>
	</Item>
	<Item class="Players" referent="RBXD67517845A5443FE976D6086B14D714A">
		<Properties>
			<bool name="BanningEnabled">true</bool>
			<bool name="CharacterAutoLoads">true</bool>
			<int name="MaxPlayersInternal">12</int>
			<int name="PreferredPlayersInternal">0</int>
			<float name="RespawnTime">5</float>
			<bool name="UseStrafingAnimations">false</bool>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">Players</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000350</UniqueId>
		</Properties>
	</Item>
	<Item class="ReplicatedFirst" referent="RBX06603A7E01FD4A41B83241EF5AD0F72B">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">ReplicatedFirst</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000353</UniqueId>
		</Properties>
	</Item>
	<Item class="TweenService" referent="RBXB84FF8736F6344A1BA4C1F8A8C7F81CA">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">TweenService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000355</UniqueId>
		</Properties>
	</Item>
	<Item class="MaterialService" referent="RBXD7686BCB5A764F159CCC063124748166">
		<Properties>
			<string name="AsphaltName">Asphalt</string>
			<string name="BasaltName">Basalt</string>
			<string name="BrickName">Brick</string>
			<string name="CardboardName">Cardboard</string>
			<string name="CarpetName">Carpet</string>
			<string name="CeramicTilesName">CeramicTiles</string>
			<string name="ClayRoofTilesName">ClayRoofTiles</string>
			<string name="CobblestoneName">Cobblestone</string>
			<string name="ConcreteName">Concrete</string>
			<string name="CorrodedMetalName">CorrodedMetal</string>
			<string name="CrackedLavaName">CrackedLava</string>
			<string name="DiamondPlateName">DiamondPlate</string>
			<string name="FabricName">Fabric</string>
			<string name="FoilName">Foil</string>
			<string name="GlacierName">Glacier</string>
			<string name="GraniteName">Granite</string>
			<string name="GrassName">Grass</string>
			<string name="GroundName">Ground</string>
			<string name="IceName">Ice</string>
			<string name="LeafyGrassName">LeafyGrass</string>
			<string name="LeatherName">Leather</string>
			<string name="LimestoneName">Limestone</string>
			<string name="MarbleName">Marble</string>
			<string name="MetalName">Metal</string>
			<string name="MudName">Mud</string>
			<string name="PavementName">Pavement</string>
			<string name="PebbleName">Pebble</string>
			<string name="PlasterName">Plaster</string>
			<string name="PlasticName">Plastic</string>
			<string name="RockName">Rock</string>
			<string name="RoofShinglesName">RoofShingles</string>
			<string name="RubberName">Rubber</string>
			<string name="SaltName">Salt</string>
			<string name="SandName">Sand</string>
			<string name="SandstoneName">Sandstone</string>
			<string name="SlateName">Slate</string>
			<string name="SmoothPlasticName">SmoothPlastic</string>
			<string name="SnowName">Snow</string>
			<bool name="Use2022MaterialsXml">false</bool>
			<string name="WoodName">Wood</string>
			<string name="WoodPlanksName">WoodPlanks</string>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">MaterialService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000356</UniqueId>
		</Properties>
	</Item>
	<Item class="TextChatService" referent="RBXA029D47C18F746788AD63F5DC1F5B677">
		<Properties>
			<bool name="ChatTranslationFTUXShown">true</bool>
			<bool name="ChatTranslationToggleEnabled">false</bool>
			<token name="ChatVersion">0</token>
			<bool name="CreateDefaultCommands">true</bool>
			<bool name="CreateDefaultTextChannels">true</bool>
			<token name="EnableProtectedChat">0</token>
			<bool name="HasSeenDeprecationDialog">false</bool>
			<bool name="IsLegacyChatDisabled">false</bool>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">TextChatService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000357</UniqueId>
		</Properties>
		<Item class="ChatWindowConfiguration" referent="RBXCFACBCFCA69E4C158CD670DE3A62E04B">
			<Properties>
				<Color3 name="BackgroundColor3">
					<R>0.0980392173</R>
					<G>0.105882354</G>
					<B>0.113725491</B>
				</Color3>
				<double name="BackgroundTransparency">0.2999999999999999889</double>
				<bool name="Enabled">true</bool>
				<Font name="FontFace">
					<Family><url>rbxasset://fonts/families/GothamSSm.json</url></Family>
					<Weight>500</Weight>
					<Style>Normal</Style>
					<CachedFaceId><url>rbxasset://fonts/Montserrat-Medium.ttf</url></CachedFaceId>
				</Font>
				<float name="HeightScale">1</float>
				<token name="HorizontalAlignment">1</token>
				<Color3 name="TextColor3">
					<R>1</R>
					<G>1</G>
					<B>1</B>
				</Color3>
				<int64 name="TextSize">14</int64>
				<Color3 name="TextStrokeColor3">
					<R>0</R>
					<G>0</G>
					<B>0</B>
				</Color3>
				<double name="TextStrokeTransparency">0.5</double>
				<token name="VerticalAlignment">1</token>
				<float name="WidthScale">1</float>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">ChatWindowConfiguration</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003bd</UniqueId>
			</Properties>
		</Item>
		<Item class="ChatInputBarConfiguration" referent="RBXE8766E3FDF984610982C8DBBD7E49458">
			<Properties>
				<bool name="AutocompleteEnabled">true</bool>
				<Color3 name="BackgroundColor3">
					<R>0.0980392173</R>
					<G>0.105882354</G>
					<B>0.113725491</B>
				</Color3>
				<double name="BackgroundTransparency">0.2000000000000000111</double>
				<bool name="Enabled">true</bool>
				<Font name="FontFace">
					<Family><url>rbxasset://fonts/families/GothamSSm.json</url></Family>
					<Weight>500</Weight>
					<Style>Normal</Style>
					<CachedFaceId><url>rbxasset://fonts/Montserrat-Medium.ttf</url></CachedFaceId>
				</Font>
				<token name="KeyboardKeyCode">47</token>
				<Color3 name="PlaceholderColor3">
					<R>0.698039234</R>
					<G>0.698039234</G>
					<B>0.698039234</B>
				</Color3>
				<Ref name="TargetTextChannel">null</Ref>
				<Color3 name="TextColor3">
					<R>1</R>
					<G>1</G>
					<B>1</B>
				</Color3>
				<int64 name="TextSize">14</int64>
				<Color3 name="TextStrokeColor3">
					<R>0</R>
					<G>0</G>
					<B>0</B>
				</Color3>
				<double name="TextStrokeTransparency">0.5</double>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">ChatInputBarConfiguration</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003be</UniqueId>
			</Properties>
		</Item>
		<Item class="BubbleChatConfiguration" referent="RBX51D1645894E449EABE8699E8BEE9D70E">
			<Properties>
				<string name="AdorneeName">HumanoidRootPart</string>
				<Color3 name="BackgroundColor3">
					<R>0.980392158</R>
					<G>0.980392158</G>
					<B>0.980392158</B>
				</Color3>
				<double name="BackgroundTransparency">0.10000000000000000555</double>
				<float name="BubbleDuration">15</float>
				<float name="BubblesSpacing">6</float>
				<bool name="Enabled">true</bool>
				<token name="Font">18</token>
				<Font name="FontFace">
					<Family><url>rbxasset://fonts/families/GothamSSm.json</url></Family>
					<Weight>500</Weight>
					<Style>Normal</Style>
					<CachedFaceId><url>rbxasset://fonts/Montserrat-Medium.ttf</url></CachedFaceId>
				</Font>
				<Vector3 name="LocalPlayerStudsOffset">
					<X>0</X>
					<Y>0</Y>
					<Z>0</Z>
				</Vector3>
				<float name="MaxBubbles">3</float>
				<float name="MaxDistance">100</float>
				<float name="MinimizeDistance">40</float>
				<bool name="TailVisible">true</bool>
				<Color3 name="TextColor3">
					<R>0.223529413</R>
					<G>0.23137255</G>
					<B>0.239215687</B>
				</Color3>
				<int64 name="TextSize">16</int64>
				<float name="VerticalStudsOffset">0</float>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">BubbleChatConfiguration</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003bf</UniqueId>
			</Properties>
		</Item>
		<Item class="ChannelTabsConfiguration" referent="RBX679208229B0F44D5AE370D70370D5BB4">
			<Properties>
				<Color3 name="BackgroundColor3">
					<R>0.0980392173</R>
					<G>0.105882354</G>
					<B>0.113725491</B>
				</Color3>
				<double name="BackgroundTransparency">0</double>
				<bool name="Enabled">false</bool>
				<Font name="FontFace">
					<Family><url>rbxasset://fonts/families/BuilderSans.json</url></Family>
					<Weight>700</Weight>
					<Style>Normal</Style>
					<CachedFaceId><url>rbxasset://fonts/BuilderSans-Bold.otf</url></CachedFaceId>
				</Font>
				<Color3 name="HoverBackgroundColor3">
					<R>0.490196079</R>
					<G>0.490196079</G>
					<B>0.490196079</B>
				</Color3>
				<Color3 name="SelectedTabTextColor3">
					<R>1</R>
					<G>1</G>
					<B>1</B>
				</Color3>
				<Color3 name="TextColor3">
					<R>0.686274529</R>
					<G>0.686274529</G>
					<B>0.686274529</B>
				</Color3>
				<int64 name="TextSize">18</int64>
				<Color3 name="TextStrokeColor3">
					<R>0</R>
					<G>0</G>
					<B>0</B>
				</Color3>
				<double name="TextStrokeTransparency">1</double>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">ChannelTabsConfiguration</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003c0</UniqueId>
			</Properties>
		</Item>
	</Item>
	<Item class="PermissionsService" referent="RBXC84D2A0AD1D84A20AA39C7754C0D8A42">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">PermissionsService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000359</UniqueId>
		</Properties>
	</Item>
	<Item class="PlayerEmulatorService" referent="RBX9F0B61C8280A4700A3A51C603C7B9FF8">
		<Properties>
			<bool name="CustomPoliciesEnabled">false</bool>
			<string name="EmulatedCountryCode"></string>
			<string name="EmulatedGameLocale"></string>
			<bool name="PlayerEmulationEnabled">false</bool>
			<bool name="PseudolocalizationEnabled">false</bool>
			<BinaryString name="SerializedEmulatedPolicyInfo"></BinaryString>
			<int name="TextElongationFactor">0</int>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">PlayerEmulatorService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000035a</UniqueId>
		</Properties>
	</Item>
	<Item class="StudioData" referent="RBXB12CFC52A9DF4689848D8072F7F21454">
		<Properties>
			<bool name="EnableScriptCollabByDefaultOnLoad">false</bool>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">StudioData</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000035d</UniqueId>
		</Properties>
	</Item>
	<Item class="StarterPlayer" referent="7">
		<Properties>
			<bool name="AllowCustomAnimations">true</bool>
			<bool name="AutoJumpEnabled">true</bool>
			<token name="AvatarJointUpgrade_SerializedRollout">0</token>
			<float name="CameraMaxZoomDistance">400</float>
			<float name="CameraMinZoomDistance">0.5</float>
			<token name="CameraMode">0</token>
			<bool name="CharacterBreakJointsOnDeath">true</bool>
			<float name="CharacterJumpHeight">7.19999981</float>
			<float name="CharacterJumpPower">50</float>
			<float name="CharacterMaxSlopeAngle">89</float>
			<bool name="CharacterUseJumpPower">true</bool>
			<float name="CharacterWalkSpeed">16</float>
			<bool name="ClassicDeath">true</bool>
			<bool name="CreateDefaultPlayerModule">true</bool>
			<token name="DevCameraOcclusionMode">0</token>
			<token name="DevComputerCameraMovementMode">0</token>
			<token name="DevComputerMovementMode">0</token>
			<token name="DevTouchCameraMovementMode">0</token>
			<token name="DevTouchMovementMode">0</token>
			<token name="EnableDynamicHeads">0</token>
			<bool name="EnableMouseLockOption">true</bool>
			<int64 name="GameSettingsAssetIDFace">0</int64>
			<int64 name="GameSettingsAssetIDHead">0</int64>
			<int64 name="GameSettingsAssetIDLeftArm">0</int64>
			<int64 name="GameSettingsAssetIDLeftLeg">0</int64>
			<int64 name="GameSettingsAssetIDPants">0</int64>
			<int64 name="GameSettingsAssetIDRightArm">0</int64>
			<int64 name="GameSettingsAssetIDRightLeg">0</int64>
			<int64 name="GameSettingsAssetIDShirt">0</int64>
			<int64 name="GameSettingsAssetIDTeeShirt">0</int64>
			<int64 name="GameSettingsAssetIDTorso">0</int64>
			<token name="GameSettingsAvatar">1</token>
			<token name="GameSettingsR15Collision">0</token>
			<NumberRange name="GameSettingsScaleRangeBodyType">0 1 </NumberRange>
			<NumberRange name="GameSettingsScaleRangeHead">0.95 1 </NumberRange>
			<NumberRange name="GameSettingsScaleRangeHeight">0.9 1.05 </NumberRange>
			<NumberRange name="GameSettingsScaleRangeProportion">0 1 </NumberRange>
			<NumberRange name="GameSettingsScaleRangeWidth">0.7 1 </NumberRange>
			<float name="HealthDisplayDistance">100</float>
			<bool name="LoadCharacterAppearance">true</bool>
			<token name="LoadCharacterLayeredClothing">0</token>
			<token name="LuaCharacterController">0</token>
			<float name="NameDisplayDistance">100</float>
			<int name="PlayerModuleStatus">0</int>
			<bool name="UserEmotesEnabled">true</bool>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">StarterPlayer</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000035f</UniqueId>
		</Properties>
		<Item class="StarterPlayerScripts" referent="8">
			<Properties>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">StarterPlayerScripts</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003b7</UniqueId>
			</Properties>
			<Item class="LocalScript" referent="9">
				<Properties>
					<ProtectedString name="Source"><![CDATA[--!strict
-- Client bootstrap: loads util, controllers, and UI, then inits/starts them in
-- dependency order via the shared Loader (util -> controllers -> UI).

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Loader = require(Shared:WaitForChild("Loader"))
local Remotes = require(Shared:WaitForChild("Remotes"))

local UTIL_ORDER = {
	"Responsive",
	"AssetResolver",
	"UIFactory",
}

local CONTROLLER_ORDER = {
	"DataController",
	"CombatController",
}

local UI_ORDER = {
	"MenuShell",
	"EquipmentTab",
	"BattleTab",
	"TalentsTab",
	"CombatUI",
}

print("[Client] bootstrap starting")

local util = Loader.requireChildren(script:WaitForChild("Util"))
local controllers = Loader.requireChildren(script:WaitForChild("Controllers"))
local ui = Loader.requireChildren(script:WaitForChild("UI"))

local deps = {
	Remotes = Remotes,
	Util = util,
	Controllers = controllers,
	UI = ui,
}

-- Init in dependency order: util first, then controllers, then UI.
Loader.initModules(util, deps, UTIL_ORDER)
Loader.initModules(controllers, deps, CONTROLLER_ORDER)
Loader.initModules(ui, deps, UI_ORDER)

Loader.startModules(util, UTIL_ORDER)
Loader.startModules(controllers, CONTROLLER_ORDER)
Loader.startModules(ui, UI_ORDER)

print("[Client] bootstrap complete")
]]></ProtectedString>
					<bool name="Disabled">false</bool>
					<Content name="LinkedSource"><null></null></Content>
					<token name="RunContext">0</token>
					<string name="ScriptGuid">{761BA18A-A94D-43C5-B4DF-A64B0F1D2083}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
					<string name="Name">Client</string>
					<int64 name="SourceAssetId">-1</int64>
					<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
					<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003b8</UniqueId>
				</Properties>
				<Item class="Folder" referent="RBX1EC189AD275D43C4882B3155CFB07F1C">
					<Properties>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Util</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ab8</UniqueId>
					</Properties>
					<Item class="ModuleScript" referent="RBXEA9DD5E844C241F6AFE3BE61C5846B96">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- Responsive: viewport scaling + device-class helper for phone/desktop (R-P1, R-M2).
-- Derives a uniform UI scale factor from the current viewport relative to a base
-- design resolution, classifies the device (Phone/Tablet/Desktop), and notifies
-- listeners whenever any of those change. Pure client utility: no networking.

local UserInputService = game:GetService("UserInputService")

local Responsive = {}

export type DeviceClass = "Phone" | "Tablet" | "Desktop"

-- Design resolution the layouts are authored against.
local BASE = Vector2.new(1280, 720)
local MIN_SCALE = 0.65
local MAX_SCALE = 1.5
-- Viewport min-axis (px) thresholds used to classify touch devices.
local PHONE_MAX_MIN_AXIS = 550
local TABLET_MAX_MIN_AXIS = 1100

local deps: any = nil
local changedEvent: BindableEvent? = nil
local initialized = false

local currentViewport = BASE
local currentScale = 1
local currentClass: DeviceClass = "Desktop"

local function computeScale(viewport: Vector2): number
	local byWidth = viewport.X / BASE.X
	local byHeight = viewport.Y / BASE.Y
	return math.clamp(math.min(byWidth, byHeight), MIN_SCALE, MAX_SCALE)
end

local function computeClass(viewport: Vector2): DeviceClass
	-- A device with touch but no keyboard is treated as a mobile form factor.
	local touchOnly = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
	if not touchOnly then
		return "Desktop"
	end
	local minAxis = math.min(viewport.X, viewport.Y)
	if minAxis <= PHONE_MAX_MIN_AXIS then
		return "Phone"
	elseif minAxis <= TABLET_MAX_MIN_AXIS then
		return "Tablet"
	end
	return "Desktop"
end

local function getViewportSize(): Vector2
	local camera = workspace.CurrentCamera
	if camera and camera.ViewportSize.Y > 0 then
		return camera.ViewportSize
	end
	return BASE
end

local function recompute()
	local viewport = getViewportSize()
	local scale = computeScale(viewport)
	local class = computeClass(viewport)
	local didChange = viewport ~= currentViewport or scale ~= currentScale or class ~= currentClass
	currentViewport = viewport
	currentScale = scale
	currentClass = class
	if didChange and changedEvent then
		changedEvent:Fire()
	end
end

local function bindCamera(camera: Camera?)
	if camera then
		camera:GetPropertyChangedSignal("ViewportSize"):Connect(recompute)
	end
end

local function ensureInit()
	if initialized then
		return
	end
	initialized = true
	changedEvent = Instance.new("BindableEvent")
	recompute()

	bindCamera(workspace.CurrentCamera)
	workspace:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
		bindCamera(workspace.CurrentCamera)
		recompute()
	end)
	-- Switching between touch and mouse/keyboard can change the device class.
	UserInputService.LastInputTypeChanged:Connect(recompute)
end

function Responsive.init(injected: any)
	deps = injected
	ensureInit()
end

function Responsive.start()
	ensureInit()
end

-- Current viewport size in pixels.
function Responsive.getViewport(): Vector2
	ensureInit()
	return currentViewport
end

-- Uniform UI scale factor for the current viewport (clamped).
function Responsive.scale(): number
	ensureInit()
	return currentScale
end

-- Convert design pixels to device pixels using the current scale (rounded).
function Responsive.scaled(px: number): number
	ensureInit()
	return math.round(px * currentScale)
end

-- Device classification derived from touch capability + viewport size.
function Responsive.getDeviceClass(): DeviceClass
	ensureInit()
	return currentClass
end

function Responsive.isPhone(): boolean
	return Responsive.getDeviceClass() == "Phone"
end

function Responsive.isTouch(): boolean
	return UserInputService.TouchEnabled
end

-- True when layouts should use the compact (single-column, taller controls) form.
function Responsive.isCompact(): boolean
	return Responsive.getDeviceClass() == "Phone"
end

-- Subscribe to viewport/scale/device-class changes. Returns the connection.
function Responsive.onChanged(callback: () -> ()): RBXScriptConnection
	ensureInit()
	return (changedEvent :: BindableEvent).Event:Connect(callback)
end

-- Attach a UIScale to a GuiObject that tracks the current scale automatically.
function Responsive.applyUIScale(guiObject: GuiObject): UIScale
	ensureInit()
	local uiScale = Instance.new("UIScale")
	uiScale.Scale = currentScale
	uiScale.Parent = guiObject
	Responsive.onChanged(function()
		uiScale.Scale = currentScale
	end)
	return uiScale
end

return Responsive
]]></ProtectedString>
							<string name="ScriptGuid">{BD29FE1F-05FB-4105-B655-23572CCA3C66}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">Responsive</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ab9</UniqueId>
						</Properties>
					</Item>
					<Item class="ModuleScript" referent="RBX5CB40A86FC7845A8BB9CD886A24CF027">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- AssetResolver: maps a logical id (e.g. "slot/Weapon", "rarity/Epic", "rig/idle")
-- to a placeholder visual (ARCHITECTURE.md 10, R-X4). v1 returns colors + a short
-- glyph label; swapping in real assets later means editing only the maps below.
-- UI code renders whatever `resolve` returns and never hard-codes asset ids.

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Config = Shared:WaitForChild("Config")
local Rarities = require(Config:WaitForChild("Rarities"))

local AssetResolver = {}

-- A resolved placeholder visual. `image` is "" until a real asset id is wired in;
-- consumers should show the color + label when `image` is empty.
export type Visual = {
	image: string, -- rbxassetid string, or "" for a pure color/label placeholder
	color: Color3,
	label: string, -- short glyph/text drawn over the placeholder
}

local deps: any = nil

local FALLBACK: Visual = {
	image = "",
	color = Color3.fromRGB(70, 74, 92),
	label = "?",
}

-- Explicit visuals for well-known logical ids. Category prefixes not listed here
-- fall through to the rule-based resolver below.
local MAP: { [string]: Visual } = {
	["tab/Equipment"] = { image = "", color = Color3.fromRGB(96, 140, 255), label = "EQ" },
	["tab/Battle"] = { image = "", color = Color3.fromRGB(228, 108, 92), label = "BT" },
	["tab/Talents"] = { image = "", color = Color3.fromRGB(140, 108, 235), label = "TL" },

	["rig/idle"] = { image = "", color = Color3.fromRGB(120, 124, 140), label = "RIG" },

	["currency/coins"] = { image = "", color = Color3.fromRGB(245, 200, 70), label = "C" },
	["currency/gems"] = { image = "", color = Color3.fromRGB(120, 210, 235), label = "G" },
	["currency/materials"] = { image = "", color = Color3.fromRGB(180, 150, 110), label = "M" },
	["currency/keys"] = { image = "", color = Color3.fromRGB(230, 210, 150), label = "K" },
}

-- Per-talent-effect placeholder tints + short glyphs (keyed by TalentEffect from
-- Config/Talents). Talent icons resolve as "talent/<effect>" e.g. "talent/hp".
local TALENT_VISUALS: { [string]: Visual } = {
	flatAttack = { image = "", color = Color3.fromRGB(224, 108, 92), label = "ATK" },
	pctAttack = { image = "", color = Color3.fromRGB(232, 140, 96), label = "%AK" },
	critChance = { image = "", color = Color3.fromRGB(245, 200, 70), label = "CRT" },
	extraReroll = { image = "", color = Color3.fromRGB(120, 210, 235), label = "RRL" },
	extraDice = { image = "", color = Color3.fromRGB(140, 108, 235), label = "DIE" },
	hp = { image = "", color = Color3.fromRGB(80, 200, 120), label = "HP" },
	lifesteal = { image = "", color = Color3.fromRGB(200, 90, 150), label = "LFS" },
}

-- Per-slot placeholder tints (keyed by slot id from Config/Slots).
local SLOT_COLORS: { [string]: Color3 } = {
	Helmet = Color3.fromRGB(150, 170, 200),
	Weapon = Color3.fromRGB(210, 120, 110),
	Belt = Color3.fromRGB(170, 140, 110),
	Charm = Color3.fromRGB(200, 160, 210),
	Chestplate = Color3.fromRGB(130, 160, 150),
	Boots = Color3.fromRGB(140, 150, 175),
}

function AssetResolver.init(injected: any)
	deps = injected
end

function AssetResolver.start() end

-- Split "category/name" into its two parts (name may be empty).
local function split(id: string): (string, string)
	local slash = string.find(id, "/", 1, true)
	if not slash then
		return id, ""
	end
	return string.sub(id, 1, slash - 1), string.sub(id, slash + 1)
end

-- Color for a rarity id, from Config/Rarities (falls back to grey).
function AssetResolver.rarityColor(rarityId: string): Color3
	local def = Rarities.get(rarityId)
	return def and def.color or FALLBACK.color
end

-- Resolve a logical id to a placeholder Visual. Always returns a value.
function AssetResolver.resolve(id: string): Visual
	local explicit = MAP[id]
	if explicit then
		return explicit
	end

	local category, name = split(id)
	if category == "rarity" then
		return {
			image = "",
			color = AssetResolver.rarityColor(name),
			label = string.sub(name, 1, 1),
		}
	elseif category == "slot" then
		return {
			image = "",
			color = SLOT_COLORS[name] or FALLBACK.color,
			label = string.sub(name, 1, 2),
		}
	elseif category == "gear" then
		-- Gear placeholders inherit their slot's tint (name is the slot id).
		return {
			image = "",
			color = SLOT_COLORS[name] or FALLBACK.color,
			label = string.sub(name, 1, 2),
		}
	elseif category == "talent" then
		-- Talent placeholders keyed by effect type (name is the TalentEffect).
		return TALENT_VISUALS[name] or FALLBACK
	end

	return FALLBACK
end

-- Convenience: apply a resolved visual onto an ImageLabel/ImageButton.
-- Uses the real image if present, otherwise renders the color placeholder.
function AssetResolver.applyImage(target: ImageLabel | ImageButton, id: string)
	local visual = AssetResolver.resolve(id)
	target.Image = visual.image
	if visual.image == "" then
		target.BackgroundTransparency = 0
		target.BackgroundColor3 = visual.color
	else
		target.ImageColor3 = Color3.new(1, 1, 1)
	end
end

return AssetResolver
]]></ProtectedString>
							<string name="ScriptGuid">{92699AF5-CD05-4FF5-9915-E3B2285E2BE8}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">AssetResolver</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000abb</UniqueId>
						</Properties>
					</Item>
					<Item class="ModuleScript" referent="RBX11167E7652D140898A995C73A54D4710">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- UIFactory: helpers to build common vanilla Instance-based UI objects (R-P2).
-- Provides a tiny declarative `create(className, props, children)` plus shortcuts
-- for the widgets used across the menu, and a shared color/theme palette so the
-- UI stays visually consistent. Pure construction helpers; no game-state.

local UIFactory = {}

local deps: any = nil

-- Shared visual palette. Tabs/panels reference these for a consistent look.
UIFactory.Theme = {
	Background = Color3.fromRGB(18, 20, 28),
	Panel = Color3.fromRGB(30, 33, 44),
	PanelAlt = Color3.fromRGB(38, 42, 56),
	Accent = Color3.fromRGB(88, 132, 255),
	AccentText = Color3.fromRGB(255, 255, 255),
	Text = Color3.fromRGB(236, 238, 244),
	SubText = Color3.fromRGB(150, 156, 172),
	Stroke = Color3.fromRGB(58, 64, 82),
	Positive = Color3.fromRGB(80, 200, 120),
	Negative = Color3.fromRGB(228, 92, 92),
	Font = Enum.Font.GothamMedium,
	FontBold = Enum.Font.GothamBold,
}

function UIFactory.init(injected: any)
	deps = injected
end

function UIFactory.start() end

-- Shallow-merge `props` over `defaults`, returning a new table.
local function withDefaults(defaults: { [string]: any }, props: { [string]: any }?): { [string]: any }
	local merged: { [string]: any } = {}
	for key, value in defaults do
		merged[key] = value
	end
	if props then
		for key, value in props do
			merged[key] = value
		end
	end
	return merged
end

-- Generic instance builder: applies properties then parents children.
-- Children may be passed via `props.Children` or the third argument.
function UIFactory.create(className: string, props: { [string]: any }?, children: { Instance }?): any
	local instance = Instance.new(className)
	if props then
		local nested = props.Children
		for key, value in props do
			if key ~= "Children" then
				(instance :: any)[key] = value
			end
		end
		if nested then
			for _, child in nested :: { Instance } do
				child.Parent = instance
			end
		end
	end
	if children then
		for _, child in children do
			child.Parent = instance
		end
	end
	return instance
end

local create = UIFactory.create
local Theme = UIFactory.Theme

function UIFactory.frame(props: { [string]: any }?): Frame
	return create("Frame", withDefaults({
		BackgroundColor3 = Theme.Panel,
		BorderSizePixel = 0,
	}, props))
end

function UIFactory.textLabel(props: { [string]: any }?): TextLabel
	return create("TextLabel", withDefaults({
		BackgroundTransparency = 1,
		Font = Theme.Font,
		TextColor3 = Theme.Text,
		TextScaled = false,
		TextSize = 18,
		Text = "",
	}, props))
end

function UIFactory.textButton(props: { [string]: any }?): TextButton
	return create("TextButton", withDefaults({
		BackgroundColor3 = Theme.Accent,
		BorderSizePixel = 0,
		AutoButtonColor = true,
		Font = Theme.FontBold,
		TextColor3 = Theme.AccentText,
		TextSize = 18,
		Text = "",
	}, props))
end

function UIFactory.imageLabel(props: { [string]: any }?): ImageLabel
	return create("ImageLabel", withDefaults({
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
	}, props))
end

function UIFactory.imageButton(props: { [string]: any }?): ImageButton
	return create("ImageButton", withDefaults({
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
		AutoButtonColor = false,
	}, props))
end

function UIFactory.scrolling(props: { [string]: any }?): ScrollingFrame
	return create("ScrollingFrame", withDefaults({
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
		ScrollBarThickness = 6,
		ScrollBarImageColor3 = Theme.Stroke,
		CanvasSize = UDim2.new(0, 0, 0, 0),
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		ScrollingDirection = Enum.ScrollingDirection.Y,
	}, props))
end

-- Rounded corners.
function UIFactory.corner(radius: number?): UICorner
	return create("UICorner", { CornerRadius = UDim.new(0, radius or 8) })
end

-- Uniform inner padding.
function UIFactory.padding(pad: number): UIPadding
	local udim = UDim.new(0, pad)
	return create("UIPadding", {
		PaddingTop = udim,
		PaddingBottom = udim,
		PaddingLeft = udim,
		PaddingRight = udim,
	})
end

function UIFactory.stroke(props: { [string]: any }?): UIStroke
	return create("UIStroke", withDefaults({
		Color = Theme.Stroke,
		Thickness = 1,
		ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
	}, props))
end

function UIFactory.listLayout(props: { [string]: any }?): UIListLayout
	return create("UIListLayout", withDefaults({
		SortOrder = Enum.SortOrder.LayoutOrder,
		FillDirection = Enum.FillDirection.Vertical,
		Padding = UDim.new(0, 8),
	}, props))
end

function UIFactory.gridLayout(props: { [string]: any }?): UIGridLayout
	return create("UIGridLayout", withDefaults({
		SortOrder = Enum.SortOrder.LayoutOrder,
		CellSize = UDim2.new(0, 96, 0, 96),
		CellPadding = UDim2.new(0, 8, 0, 8),
	}, props))
end

-- Locks a GuiObject to a fixed aspect ratio (e.g. square gear cells).
function UIFactory.aspectRatio(ratio: number): UIAspectRatioConstraint
	return create("UIAspectRatioConstraint", { AspectRatio = ratio })
end

return UIFactory
]]></ProtectedString>
							<string name="ScriptGuid">{584AC98C-CD1F-4D15-BFD4-68F763F7FDB4}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">UIFactory</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000abd</UniqueId>
						</Properties>
					</Item>
				</Item>
				<Item class="Folder" referent="RBX31E809EC5048438489EAA125218851EA">
					<Properties>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Controllers</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000abf</UniqueId>
					</Properties>
					<Item class="ModuleScript" referent="RBXDC7E1EAB5F234B3E974C908315B08D74">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- DataController: mirrors the authoritative PlayerData replicated from the server
-- (ARCHITECTURE.md 2). Listens to the `ProfileUpdated` remote, caches the latest
-- profile, and notifies UI subscribers so views re-render on any server change.
-- Read-only on the client: it never mutates state, only requests actions.

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Types = require(Shared:WaitForChild("Types"))

type PlayerData = Types.PlayerData

local DataController = {}

local deps: any = nil
local Remotes: any = nil

local profile: PlayerData? = nil
local changedEvent: BindableEvent = Instance.new("BindableEvent")

function DataController.init(injected: any)
	deps = injected
	Remotes = deps.Remotes
end

function DataController.start()
	local profileUpdated = Remotes.getEvent("ProfileUpdated")
	profileUpdated.OnClientEvent:Connect(function(data: PlayerData)
		profile = data
		changedEvent:Fire(data)
	end)
end

-- Latest replicated profile, or nil if it has not arrived yet.
function DataController.get(): PlayerData?
	return profile
end

-- Block until the first profile arrives (or `timeout` seconds elapse).
function DataController.waitForData(timeout: number?): PlayerData?
	local deadline = os.clock() + (timeout or 10)
	while profile == nil and os.clock() < deadline do
		task.wait()
	end
	return profile
end

-- Subscribe to profile updates; callback receives the new PlayerData.
function DataController.onChanged(callback: (PlayerData) -> ()): RBXScriptConnection
	return changedEvent.Event:Connect(callback)
end

return DataController
]]></ProtectedString>
							<string name="ScriptGuid">{151DE1DA-E1C3-4C99-8B35-12D843264423}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">DataController</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ac0</UniqueId>
						</Properties>
					</Item>
					<Item class="ModuleScript" referent="RBX25D707D1099340E1A377282EEB25EEEF">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- CombatController: mirrors combat state and sends combat intents. Stub for Step 1.

local CombatController = {}

local deps: any = nil

function CombatController.init(injected: any)
	deps = injected
end

function CombatController.start() end

return CombatController
]]></ProtectedString>
							<string name="ScriptGuid">{96F634AC-5396-43CD-83C4-F32D62602254}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">CombatController</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ac2</UniqueId>
						</Properties>
					</Item>
				</Item>
				<Item class="Folder" referent="RBX1373C6A36B8E4A3E9EF4DBA0CFA049D1">
					<Properties>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">UI</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ac4</UniqueId>
					</Properties>
					<Item class="ModuleScript" referent="RBX5A9BE289ABF54AC1AD69D523435F9132">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- MenuShell: the root menu ScreenGui with three bottom tabs (Equipment, Battle,
-- Talents) and tab switching (R-M1). Owns the shared layout chrome and exposes a
-- content container per tab so the individual tab modules (later steps) parent
-- their views into it. Responsive for phone and desktop (R-M2) via Util/Responsive.

local Players = game:GetService("Players")

local MenuShell = {}

local deps: any = nil
local UIFactory: any = nil
local Responsive: any = nil
local AssetResolver: any = nil

-- Bottom tabs, left-to-right (R-M1).
local TAB_ORDER = { "Equipment", "Battle", "Talents" }

-- Design-pixel heights for the bottom tab bar per form factor (scaled at runtime).
local TABBAR_HEIGHT_DESKTOP = 66
local TABBAR_HEIGHT_PHONE = 104

local screenGui: ScreenGui? = nil
local contentArea: Frame? = nil
local tabBar: Frame? = nil

local contentFrames: { [string]: Frame } = {}
local tabButtons: { [string]: TextButton } = {}
local tabButtonLabels: { [string]: TextLabel } = {}
local activeTab: string? = nil
local tabChangedEvent: BindableEvent? = nil

function MenuShell.init(injected: any)
	deps = injected
	UIFactory = deps.Util.UIFactory
	Responsive = deps.Util.Responsive
	AssetResolver = deps.Util.AssetResolver
	tabChangedEvent = Instance.new("BindableEvent")
end

-- Build a single content panel (one per tab). Later tab modules parent into it.
local function buildContentFrame(tabId: string): Frame
	local Theme = UIFactory.Theme
	local frame = UIFactory.frame({
		Name = tabId .. "Content",
		BackgroundColor3 = Theme.Background,
		Size = UDim2.fromScale(1, 1),
		Visible = false,
	})
	UIFactory.padding(Responsive.scaled(12)).Parent = frame

	-- Placeholder shown until the dedicated tab module renders its own view.
	local placeholder = UIFactory.textLabel({
		Name = "Placeholder",
		Size = UDim2.fromScale(1, 1),
		TextColor3 = Theme.SubText,
		TextSize = Responsive.scaled(22),
		Font = Theme.FontBold,
		Text = tabId,
	})
	placeholder.Parent = frame

	return frame
end

-- Build one bottom-bar tab button (icon placeholder + label), wired to switch tabs.
local function buildTabButton(tabId: string, order: number): TextButton
	local Theme = UIFactory.Theme
	local button = UIFactory.textButton({
		Name = tabId .. "Tab",
		LayoutOrder = order,
		Size = UDim2.fromScale(1 / #TAB_ORDER, 1),
		BackgroundColor3 = Theme.Panel,
		AutoButtonColor = false,
		Text = "",
	})

	local stack = UIFactory.frame({
		Name = "Stack",
		BackgroundTransparency = 1,
		Size = UDim2.fromScale(1, 1),
	})
	stack.Parent = button
	local layout = UIFactory.listLayout({
		FillDirection = Enum.FillDirection.Vertical,
		HorizontalAlignment = Enum.HorizontalAlignment.Center,
		VerticalAlignment = Enum.VerticalAlignment.Center,
		Padding = UDim.new(0, Responsive.scaled(4)),
	})
	layout.Parent = stack

	local visual = AssetResolver.resolve("tab/" .. tabId)
	local iconSize = Responsive.scaled(Responsive.isCompact() and 30 or 26)
	local icon = UIFactory.frame({
		Name = "Icon",
		LayoutOrder = 1,
		BackgroundColor3 = visual.color,
		Size = UDim2.fromOffset(iconSize, iconSize),
	})
	UIFactory.corner(math.floor(iconSize / 4)).Parent = icon
	local glyph = UIFactory.textLabel({
		Size = UDim2.fromScale(1, 1),
		Text = visual.label,
		TextColor3 = Theme.AccentText,
		Font = Theme.FontBold,
		TextSize = Responsive.scaled(12),
	})
	glyph.Parent = icon
	icon.Parent = stack

	local label = UIFactory.textLabel({
		Name = "Label",
		LayoutOrder = 2,
		AutomaticSize = Enum.AutomaticSize.X,
		Size = UDim2.fromOffset(0, Responsive.scaled(18)),
		Text = tabId,
		TextColor3 = Theme.SubText,
		Font = Theme.FontBold,
		TextSize = Responsive.scaled(Responsive.isCompact() and 15 or 14),
	})
	label.Parent = stack
	tabButtonLabels[tabId] = label

	button.Activated:Connect(function()
		MenuShell.showTab(tabId)
	end)

	return button
end

-- Recompute size-dependent properties for the current viewport/device class.
local function layout()
	if not (screenGui and contentArea and tabBar) then
		return
	end
	local barHeight = Responsive.scaled(
		Responsive.isCompact() and TABBAR_HEIGHT_PHONE or TABBAR_HEIGHT_DESKTOP
	)
	tabBar.Size = UDim2.new(1, 0, 0, barHeight)
	contentArea.Size = UDim2.new(1, 0, 1, -barHeight)

	for tabId, button in tabButtons do
		local icon = button:FindFirstChild("Stack") and (button.Stack :: Frame):FindFirstChild("Icon")
		local iconSize = Responsive.scaled(Responsive.isCompact() and 30 or 26)
		if icon and icon:IsA("Frame") then
			icon.Size = UDim2.fromOffset(iconSize, iconSize)
		end
		local label = tabButtonLabels[tabId]
		if label then
			label.TextSize = Responsive.scaled(Responsive.isCompact() and 15 or 14)
		end
	end
end

-- Apply the visual selected/deselected state to every tab button.
local function refreshSelection()
	local Theme = UIFactory.Theme
	for tabId, button in tabButtons do
		local selected = tabId == activeTab
		button.BackgroundColor3 = selected and Theme.PanelAlt or Theme.Panel
		local label = tabButtonLabels[tabId]
		if label then
			label.TextColor3 = selected and Theme.Accent or Theme.SubText
		end
	end
end

function MenuShell.start()
	local player = Players.LocalPlayer
	local playerGui = player:WaitForChild("PlayerGui")

	local gui = UIFactory.create("ScreenGui", {
		Name = "MenuShell",
		ResetOnSpawn = false,
		IgnoreGuiInset = true,
		ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
		DisplayOrder = 1,
	})
	screenGui = gui

	local root = UIFactory.frame({
		Name = "Root",
		BackgroundColor3 = UIFactory.Theme.Background,
		Size = UDim2.fromScale(1, 1),
	})
	root.Parent = gui

	local content = UIFactory.frame({
		Name = "ContentArea",
		BackgroundColor3 = UIFactory.Theme.Background,
		Position = UDim2.fromScale(0, 0),
		Size = UDim2.new(1, 0, 1, 0),
	})
	content.Parent = root
	contentArea = content

	local bar = UIFactory.frame({
		Name = "TabBar",
		BackgroundColor3 = UIFactory.Theme.Panel,
		AnchorPoint = Vector2.new(0.5, 1),
		Position = UDim2.fromScale(0.5, 1),
		Size = UDim2.new(1, 0, 0, TABBAR_HEIGHT_DESKTOP),
	})
	bar.Parent = root
	tabBar = bar

	UIFactory.stroke({ Color = UIFactory.Theme.Stroke }).Parent = bar
	local barLayout = UIFactory.listLayout({
		FillDirection = Enum.FillDirection.Horizontal,
		HorizontalAlignment = Enum.HorizontalAlignment.Center,
		VerticalAlignment = Enum.VerticalAlignment.Center,
		Padding = UDim.new(0, 0),
	})
	barLayout.Parent = bar

	for order, tabId in TAB_ORDER do
		local frame = buildContentFrame(tabId)
		frame.Parent = content
		contentFrames[tabId] = frame

		local button = buildTabButton(tabId, order)
		button.Parent = bar
		tabButtons[tabId] = button
	end

	gui.Parent = playerGui

	layout()
	Responsive.onChanged(layout)

	-- Default to the first tab.
	MenuShell.showTab(TAB_ORDER[1])
end

-- Switch the visible tab. No-op if the id is unknown or already active.
function MenuShell.showTab(tabId: string)
	if not contentFrames[tabId] then
		return
	end
	if activeTab == tabId then
		return
	end
	activeTab = tabId
	for id, frame in contentFrames do
		frame.Visible = id == tabId
	end
	refreshSelection()
	if tabChangedEvent then
		tabChangedEvent:Fire(tabId)
	end
end

-- The content container for a tab; tab modules parent their views here.
function MenuShell.getContentFrame(tabId: string): Frame?
	return contentFrames[tabId]
end

function MenuShell.getActiveTab(): string?
	return activeTab
end

function MenuShell.getScreenGui(): ScreenGui?
	return screenGui
end

-- Subscribe to tab changes; callback receives the new tab id.
function MenuShell.onTabChanged(callback: (string) -> ()): RBXScriptConnection
	assert(tabChangedEvent, "MenuShell not initialized")
	return tabChangedEvent.Event:Connect(callback)
end

return MenuShell
]]></ProtectedString>
							<string name="ScriptGuid">{6668EFBD-E516-48AE-90DB-24E3D4630414}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">MenuShell</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ac5</UniqueId>
						</Properties>
					</Item>
					<Item class="ModuleScript" referent="RBXAD86E5FDDA8D4EB0A3D31248FC0BD8DB">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- EquipmentTab: the Equipment view (R-E1..R-E8). Renders a 3D character rig in a
-- ViewportFrame with a code-driven idle animation, the six equipment slots laid
-- out around it (icon + slot level), a grid of unequipped gear sorted by rarity
-- (descending, icons only), and a detail popup with stats + Equip/Unequip. All
-- state is read from DataController (server-authoritative); equip/unequip only
-- send intent via the EquipGear/UnequipGear remotes and re-render on ProfileUpdated.

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Config = Shared:WaitForChild("Config")
local Logic = Shared:WaitForChild("Logic")
local Slots = require(Config:WaitForChild("Slots"))
local Gear = require(Config:WaitForChild("Gear"))
local Rarities = require(Config:WaitForChild("Rarities"))
local Balance = require(Config:WaitForChild("Balance"))
local GearStats = require(Logic:WaitForChild("GearStats"))
local InventoryLayout = require(Logic:WaitForChild("InventoryLayout"))
local MergeLogic = require(Logic:WaitForChild("MergeLogic"))

local EquipmentTab = {}

local deps: any = nil
local UIFactory: any = nil
local Responsive: any = nil
local AssetResolver: any = nil
local MenuShell: any = nil
local DataController: any = nil
local equipRemote: RemoteEvent? = nil
local unequipRemote: RemoteEvent? = nil
local levelSlotRemote: RemoteEvent? = nil
local mergeGearRemote: RemoteEvent? = nil
local autoMergeGearRemote: RemoteEvent? = nil

-- Built instances kept for re-render / re-layout.
local rootFrame: Frame? = nil
local characterArea: Frame? = nil
local leftColumn: Frame? = nil
local rightColumn: Frame? = nil
local viewport: ViewportFrame? = nil
local gridFrame: ScrollingFrame? = nil
local gridLayout: UIGridLayout? = nil
local gridPadding: UIPadding? = nil
local sideDivider: Frame? = nil

type SlotWidget = {
	button: TextButton,
	icon: Frame,
	glyph: TextLabel,
	levelLabel: TextLabel,
	stroke: UIStroke,
}
local slotWidgets: { [string]: SlotWidget } = {}
local gridCells: { Instance } = {}
local emptyCells: { Instance } = {}
local gridItemCount = 0
-- Latest computed unequipped tile size (px); equipped slots match this.
local inventoryCellSize = 0

-- Idle-animation state. `rigModel` is the player's loaded avatar (R-E1); when it
-- fails to load we fall back to the blocky placeholder parts in `rigParts`.
local rigModel: Model? = nil
local rigParts: { [string]: BasePart } = {}
local rigConnection: RBXScriptConnection? = nil
local rigStartClock = 0

-- Detail popup elements + current selection context.
local popupOverlay: Frame? = nil
local popupCard: Frame? = nil
local popupTitle: TextLabel? = nil
local popupSubtitle: TextLabel? = nil
local popupStat: TextLabel? = nil
local popupSlotLevel: TextLabel? = nil
local popupActionButton: TextButton? = nil
local popupUpgradeButton: TextButton? = nil
local popupCostLabel: TextLabel? = nil
local popupSelectedUid: string? = nil
local popupAction: (() -> ())? = nil
local popupUpgradeAction: (() -> ())? = nil

-- Merge panel elements + selection state (R-E9). `mergeSelected` holds up to
-- three chosen gear uids; the list is filtered to the first pick's type+rarity.
local mergeTriggerButton: TextButton? = nil
local inventoryTitle: TextLabel? = nil
local mergeOverlay: Frame? = nil
local mergeCard: Frame? = nil
local mergeInputButtons: { TextButton } = {}
local mergeOutputFrame: Frame? = nil
local mergeConfirmButton: TextButton? = nil
local mergeListFrame: ScrollingFrame? = nil
local mergeListCells: { Instance } = {}
local mergeSelected: { string } = {}
-- What the merge/auto button currently does: "auto", "manual", or "disabled".
local mergeButtonMode = "disabled"

-- ==================== small helpers ====================

local function abbrev(name: string): string
	return string.upper(string.sub(name, 1, 3))
end

-- Blend `color` toward the panel color to produce a muted fill.
local function dim(color: Color3, alpha: number): Color3
	return color:Lerp(UIFactory.Theme.Panel, alpha)
end

-- Destroy previously-built gear (item) cells (leaves layout/padding intact).
local function clearGrid()
	for _, cell in gridCells do
		cell:Destroy()
	end
	table.clear(gridCells)
end

-- Destroy the placeholder empty tiles.
local function clearEmpties()
	for _, cell in emptyCells do
		cell:Destroy()
	end
	table.clear(emptyCells)
end

-- ==================== rig + idle animation ====================

local function makePart(name: string, size: Vector3, color: Color3): BasePart
	local part = Instance.new("Part")
	part.Name = name
	part.Size = size
	part.Color = color
	part.Anchored = true
	part.CanCollide = false
	part.CanQuery = false
	part.CanTouch = false
	part.CastShadow = false
	part.Material = Enum.Material.SmoothPlastic
	part.TopSurface = Enum.SurfaceType.Smooth
	part.BottomSurface = Enum.SurfaceType.Smooth
	return part
end

-- Position the viewport camera to frame `model` from the front, using its
-- bounding box so avatars of any scale fit. Views along the rig's own facing
-- direction so we always see the avatar's face.
local function frameCamera(vp: ViewportFrame, model: Model)
	local camera = vp.CurrentCamera
	if not camera then
		return
	end
	local boundingCFrame, size = model:GetBoundingBox()
	local center = boundingCFrame.Position
	local extent = math.max(size.X, size.Y)
	local distance = (extent / 2) / math.tan(math.rad(camera.FieldOfView / 2)) * 1.4

	local root = model:FindFirstChild("HumanoidRootPart")
	local front = if root and root:IsA("BasePart") then root.CFrame.LookVector else Vector3.new(0, 0, -1)
	camera.CFrame = CFrame.lookAt(center + front * distance, center)
end

-- Blocky placeholder rig used when the real avatar cannot be loaded (offline /
-- API disabled). Kept as a graceful fallback so the tab is never empty.
local function buildBlockyRig(vp: ViewportFrame)
	local camera = vp.CurrentCamera or Instance.new("Camera")
	camera.CFrame = CFrame.lookAt(Vector3.new(0, 0.2, 9), Vector3.new(0, -0.3, 0))
	camera.Parent = vp
	vp.CurrentCamera = camera

	local bodyColor = AssetResolver.resolve("rig/idle").color
	local model = Instance.new("Model")
	model.Name = "BlockyRig"

	rigParts.torso = makePart("Torso", Vector3.new(2, 2, 1), UIFactory.Theme.Accent)
	rigParts.head = makePart("Head", Vector3.new(1.2, 1.2, 1.2), Color3.fromRGB(232, 210, 180))
	rigParts.leftArm = makePart("LeftArm", Vector3.new(0.8, 2, 0.8), bodyColor)
	rigParts.rightArm = makePart("RightArm", Vector3.new(0.8, 2, 0.8), bodyColor)
	rigParts.leftLeg = makePart("LeftLeg", Vector3.new(0.9, 2, 0.9), dim(bodyColor, 0.35))
	rigParts.rightLeg = makePart("RightLeg", Vector3.new(0.9, 2, 0.9), dim(bodyColor, 0.35))

	for _, part in rigParts do
		part.Parent = model
	end
	model.Parent = vp
end

-- Default R15 idle animation clip. We use this rather than the player's equipped
-- HumanoidDescription.IdleAnimation because that id points to an animation
-- PACKAGE (bundle), which is not a directly loadable AnimationClip — the engine
-- only resolves packages when a character spawns normally, so LoadAnimation on it
-- fails ("AnimationClip loaded is not valid"). This default is a plain, valid clip.
local DEFAULT_IDLE_ANIMATION = 507766388

-- Load and loop the idle animation. Requires the model to sit under a WorldModel
-- so the Animator evaluates inside the ViewportFrame.
local function playIdle(model: Model)
	local humanoid = model:FindFirstChildOfClass("Humanoid")
	if not humanoid then
		return
	end
	local animator = humanoid:FindFirstChildOfClass("Animator")
	if not animator then
		animator = Instance.new("Animator")
		animator.Parent = humanoid
	end

	local animation = Instance.new("Animation")
	animation.AnimationId = "rbxassetid://" .. tostring(DEFAULT_IDLE_ANIMATION)

	local ok, track = pcall(function()
		return (animator :: Animator):LoadAnimation(animation)
	end)
	if ok and track then
		track.Looped = true
		track:Play()
	end
end

-- Parent the loaded avatar into a WorldModel inside the viewport, anchor its
-- root, play its idle animation, and frame the camera. A 1-to-1 avatar display.
local function setupPlayerRig(vp: ViewportFrame, model: Model)
	model.Name = "PlayerRig"

	local worldModel = Instance.new("WorldModel")
	worldModel.Name = "RigWorld"
	worldModel.Parent = vp

	model.Parent = worldModel
	model:PivotTo(CFrame.identity)

	local root = model:FindFirstChild("HumanoidRootPart")
	if root and root:IsA("BasePart") then
		root.Anchored = true
	end

	rigModel = model
	playIdle(model)
	frameCamera(vp, model)
end

-- Build the character rig: try the player's real avatar first (R-E1), then fall
-- back to the placeholder. Runs async so bootstrap is not blocked by the fetch.
local function buildRig(vp: ViewportFrame)
	local camera = Instance.new("Camera")
	camera.FieldOfView = 40
	camera.CFrame = CFrame.lookAt(Vector3.new(0, 0.2, 9), Vector3.new(0, -0.3, 0))
	camera.Parent = vp
	vp.CurrentCamera = camera

	vp.Ambient = Color3.fromRGB(190, 194, 205)
	vp.LightColor = Color3.fromRGB(255, 255, 255)
	vp.LightDirection = Vector3.new(-0.4, -1, -0.6).Unit

	rigStartClock = os.clock()

	task.spawn(function()
		local player = Players.LocalPlayer
		local ok, model = pcall(function()
			return Players:CreateHumanoidModelFromUserId(player.UserId)
		end)
		if ok and typeof(model) == "Instance" and model:IsA("Model") then
			setupPlayerRig(vp, model)
		else
			warn("[EquipmentTab] avatar load failed, using placeholder rig:", model)
			buildBlockyRig(vp)
		end
	end)
end

-- Per-frame idle pose. The real avatar plays its own looping idle animation, so
-- nothing is done here for it; only the blocky fallback is code-animated.
local function animateRig()
	if rigModel then
		return
	end
	if not rigParts.torso then
		return
	end
	local t = os.clock() - rigStartClock
	local bob = math.sin(t * 2) * 0.1
	local yaw = math.sin(t * 0.8) * math.rad(12)
	local pivot = CFrame.new(0, bob, 0) * CFrame.Angles(0, yaw, 0)
	rigParts.torso.CFrame = pivot
	rigParts.head.CFrame = pivot * CFrame.new(0, 1.6, 0)
	rigParts.leftLeg.CFrame = pivot * CFrame.new(-0.5, -2, 0)
	rigParts.rightLeg.CFrame = pivot * CFrame.new(0.5, -2, 0)

	local swing = math.sin(t * 2) * math.rad(10)
	rigParts.leftArm.CFrame = pivot * CFrame.new(-1.4, 0.9, 0) * CFrame.Angles(swing, 0, 0) * CFrame.new(0, -1, 0)
	rigParts.rightArm.CFrame = pivot * CFrame.new(1.4, 0.9, 0) * CFrame.Angles(-swing, 0, 0) * CFrame.new(0, -1, 0)
end

-- ==================== detail popup ====================

local function closePopup()
	popupSelectedUid = nil
	popupAction = nil
	popupUpgradeAction = nil
	if popupOverlay then
		popupOverlay.Visible = false
	end
end

-- Fill the popup with a gear instance's details and wire its action button to
-- Equip (if unequipped) or Unequip (if currently in its slot). Returns false if
-- the gear can no longer be resolved (e.g. it was removed).
local function populatePopup(gearUid: string): boolean
	local data = DataController.get()
	if not data then
		return false
	end
	local inst = data.gear[gearUid]
	if not inst then
		return false
	end
	local gearDef = Gear.get(inst.id)
	local slotDef = gearDef and Slots.get(gearDef.slot)
	if not (gearDef and slotDef) then
		return false
	end

	local slotId = slotDef.id
	local slotLevel = data.slotLevels[slotId] or 1
	local rarityDef = Rarities.get(inst.rarity)
	local rarityColor = rarityDef and rarityDef.color or UIFactory.Theme.Text

	local titleLabel = popupTitle :: TextLabel
	titleLabel.Text = gearDef.name
	titleLabel.TextColor3 = rarityColor

	local subtitleLabel = popupSubtitle :: TextLabel
	subtitleLabel.Text = ("%s  -  %s"):format(inst.rarity, slotDef.name)

	local statLabel = popupStat :: TextLabel
	local gearStat = GearStats.compute(inst.id, inst.rarity, slotLevel)
	if gearStat then
		statLabel.Text = ("%s: %s"):format(GearStats.statLabel(gearStat.stat), GearStats.format(gearStat))
	else
		statLabel.Text = ""
	end

	local slotLevelLabel = popupSlotLevel :: TextLabel
	slotLevelLabel.Text = ("Slot Level %d"):format(slotLevel)

	local isEquipped = data.equipped[slotId] == gearUid
	local button = popupActionButton :: TextButton
	local upgrade = popupUpgradeButton :: TextButton
	local costLabel = popupCostLabel :: TextLabel
	if isEquipped then
		button.Text = "Unequip"
		button.BackgroundColor3 = UIFactory.Theme.Negative
		popupAction = function()
			if unequipRemote then
				unequipRemote:FireServer(slotId)
			end
			closePopup()
		end

		-- Upgrade (level up the slot) is only offered for the equipped piece.
		-- Free for now; stays open so the new level/stat update live (R-E8).
		upgrade.Visible = true
		if slotLevel >= Balance.SlotLevels.maxLevel then
			upgrade.Text = "Max Level"
			upgrade.AutoButtonColor = false
			upgrade.BackgroundColor3 = UIFactory.Theme.PanelAlt
			popupUpgradeAction = nil
			costLabel.Visible = false
		else
			upgrade.Text = "Upgrade (Free)"
			upgrade.AutoButtonColor = true
			upgrade.BackgroundColor3 = UIFactory.Theme.Accent
			popupUpgradeAction = function()
				if levelSlotRemote then
					levelSlotRemote:FireServer(slotId)
				end
			end

			-- Balance vs. upgrade cost (coins for now; Slot Materials to be added
			-- alongside once that economy step lands).
			local coins = (data.currencies and data.currencies.coins) or 0
			local upgradeCost = Balance.SlotLevels.baseCost + (slotLevel - 1) * Balance.SlotLevels.costPerLevel
			costLabel.Text = ("You have %d Coins  /  Upgrade: %d Coins"):format(coins, upgradeCost)
			costLabel.Visible = true
		end
	else
		button.Text = "Equip"
		button.BackgroundColor3 = UIFactory.Theme.Positive
		popupAction = function()
			if equipRemote then
				equipRemote:FireServer(gearUid)
			end
			closePopup()
		end

		upgrade.Visible = false
		popupUpgradeAction = nil
		costLabel.Visible = false
	end

	popupSelectedUid = gearUid
	return true
end

local function openPopup(gearUid: string)
	if populatePopup(gearUid) and popupOverlay then
		popupOverlay.Visible = true
	end
end

-- If the popup is open, refresh it against the latest data (or close if stale).
local function refreshPopup()
	if popupOverlay and popupOverlay.Visible and popupSelectedUid then
		if not populatePopup(popupSelectedUid) then
			closePopup()
		end
	end
end

local function buildPopup(parent: Frame)
	local Theme = UIFactory.Theme
	local overlay = UIFactory.frame({
		Name = "DetailPopup",
		BackgroundColor3 = Color3.fromRGB(0, 0, 0),
		BackgroundTransparency = 0.45,
		Size = UDim2.fromScale(1, 1),
		Visible = false,
		ZIndex = 20,
	})
	overlay.Parent = parent
	popupOverlay = overlay

	-- Click-off backdrop closes the popup.
	local backdrop = UIFactory.textButton({
		Name = "Backdrop",
		BackgroundTransparency = 1,
		Text = "",
		AutoButtonColor = false,
		Size = UDim2.fromScale(1, 1),
		ZIndex = 20,
	})
	backdrop.Parent = overlay
	backdrop.Activated:Connect(closePopup)

	local card = UIFactory.frame({
		Name = "Card",
		BackgroundColor3 = Theme.Panel,
		AnchorPoint = Vector2.new(0.5, 0.5),
		Position = UDim2.fromScale(0.5, 0.5),
		Size = UDim2.fromOffset(320, 0),
		AutomaticSize = Enum.AutomaticSize.Y,
		ZIndex = 21,
	})
	card.Parent = overlay
	popupCard = card
	UIFactory.corner(12).Parent = card
	UIFactory.stroke({ Color = Theme.Stroke }).Parent = card
	UIFactory.padding(18).Parent = card

	local layout = UIFactory.listLayout({
		FillDirection = Enum.FillDirection.Vertical,
		HorizontalAlignment = Enum.HorizontalAlignment.Center,
		Padding = UDim.new(0, 10),
	})
	layout.Parent = card

	local title = UIFactory.textLabel({
		Name = "Title",
		LayoutOrder = 1,
		Size = UDim2.new(1, -30, 0, 30),
		Font = Theme.FontBold,
		TextSize = 24,
		TextXAlignment = Enum.TextXAlignment.Left,
		ZIndex = 21,
	})
	title.Parent = card
	popupTitle = title

	local subtitle = UIFactory.textLabel({
		Name = "Subtitle",
		LayoutOrder = 2,
		Size = UDim2.new(1, 0, 0, 20),
		TextColor3 = Theme.SubText,
		TextSize = 16,
		TextXAlignment = Enum.TextXAlignment.Left,
		ZIndex = 21,
	})
	subtitle.Parent = card
	popupSubtitle = subtitle

	local divider = UIFactory.frame({
		Name = "Divider",
		LayoutOrder = 3,
		BackgroundColor3 = Theme.Stroke,
		Size = UDim2.new(1, 0, 0, 1),
		ZIndex = 21,
	})
	divider.Parent = card

	local stat = UIFactory.textLabel({
		Name = "Stat",
		LayoutOrder = 4,
		Size = UDim2.new(1, 0, 0, 24),
		Font = Theme.FontBold,
		TextColor3 = Theme.Positive,
		TextSize = 20,
		TextXAlignment = Enum.TextXAlignment.Left,
		ZIndex = 21,
	})
	stat.Parent = card
	popupStat = stat

	local slotLevel = UIFactory.textLabel({
		Name = "SlotLevel",
		LayoutOrder = 5,
		Size = UDim2.new(1, 0, 0, 20),
		TextColor3 = Theme.SubText,
		TextSize = 16,
		TextXAlignment = Enum.TextXAlignment.Left,
		ZIndex = 21,
	})
	slotLevel.Parent = card
	popupSlotLevel = slotLevel

	local action = UIFactory.textButton({
		Name = "Action",
		LayoutOrder = 6,
		Size = UDim2.new(1, 0, 0, 46),
		TextSize = 20,
		Text = "Equip",
		ZIndex = 21,
	})
	action.Parent = card
	UIFactory.corner(10).Parent = action
	popupActionButton = action
	action.Activated:Connect(function()
		if popupAction then
			popupAction()
		end
	end)

	-- Upgrade button: levels the slot (equipped gear only). Hidden by default.
	local upgrade = UIFactory.textButton({
		Name = "Upgrade",
		LayoutOrder = 7,
		Size = UDim2.new(1, 0, 0, 46),
		BackgroundColor3 = Theme.Accent,
		TextSize = 20,
		Text = "Upgrade (Free)",
		Visible = false,
		ZIndex = 21,
	})
	upgrade.Parent = card
	UIFactory.corner(10).Parent = upgrade
	popupUpgradeButton = upgrade
	upgrade.Activated:Connect(function()
		if popupUpgradeAction then
			popupUpgradeAction()
		end
	end)

	-- Balance / upgrade-cost line, shown at the very bottom below the Upgrade
	-- button when an equipped piece is selected.
	local cost = UIFactory.textLabel({
		Name = "UpgradeCost",
		LayoutOrder = 8,
		Size = UDim2.new(1, 0, 0, 0),
		AutomaticSize = Enum.AutomaticSize.Y,
		TextWrapped = true,
		TextColor3 = Theme.SubText,
		TextSize = 14,
		TextXAlignment = Enum.TextXAlignment.Center,
		Visible = false,
		ZIndex = 21,
	})
	cost.Parent = card
	popupCostLabel = cost

	-- Close (X) button, top-right of the card.
	local close = UIFactory.textButton({
		Name = "Close",
		AnchorPoint = Vector2.new(1, 0),
		Position = UDim2.new(1, -8, 0, 8),
		Size = UDim2.fromOffset(30, 30),
		BackgroundColor3 = Theme.PanelAlt,
		Text = "X",
		TextSize = 18,
		ZIndex = 22,
	})
	close.Parent = card
	UIFactory.corner(8).Parent = close
	close.Activated:Connect(closePopup)
end

-- ==================== slots ====================

local function buildSlotButton(slotId: string): TextButton
	local Theme = UIFactory.Theme
	local slotDef = Slots.get(slotId)

	local button = UIFactory.textButton({
		Name = slotId,
		BackgroundColor3 = Theme.Panel,
		AutoButtonColor = false,
		Text = "",
		Size = UDim2.fromOffset(72, 72),
		LayoutOrder = slotDef and slotDef.row or 1,
	})
	UIFactory.corner(10).Parent = button
	UIFactory.aspectRatio(1).Parent = button
	local stroke = UIFactory.stroke({ Color = Theme.Stroke })
	stroke.Parent = button

	local icon = UIFactory.frame({
		Name = "Icon",
		BackgroundColor3 = Theme.PanelAlt,
		AnchorPoint = Vector2.new(0.5, 0.5),
		Position = UDim2.fromScale(0.5, 0.5),
		Size = UDim2.fromScale(0.82, 0.82),
	})
	icon.Parent = button
	UIFactory.corner(8).Parent = icon

	local glyph = UIFactory.textLabel({
		Name = "Glyph",
		Size = UDim2.fromScale(1, 1),
		Font = Theme.FontBold,
		TextColor3 = Theme.Text,
		TextSize = 16,
		Text = slotDef and abbrev(slotDef.name) or "?",
	})
	glyph.Parent = icon

	-- Slot-level badge (bottom-middle). Level is tied to the slot (R-E3) and is
	-- only shown while a gear is equipped in this slot (set in renderSlots).
	local badge = UIFactory.frame({
		Name = "LevelBadge",
		BackgroundColor3 = Theme.Background,
		AnchorPoint = Vector2.new(0.5, 1),
		Position = UDim2.new(0.5, 0, 1, -3),
		Size = UDim2.fromOffset(34, 16),
		Visible = false,
	})
	badge.Parent = button
	UIFactory.corner(6).Parent = badge
	local levelLabel = UIFactory.textLabel({
		Name = "Level",
		Size = UDim2.fromScale(1, 1),
		Font = Theme.FontBold,
		TextColor3 = Theme.Accent,
		TextSize = 12,
		Text = "Lv 1",
	})
	levelLabel.Parent = badge

	slotWidgets[slotId] = {
		button = button,
		icon = icon,
		glyph = glyph,
		badge = badge,
		levelLabel = levelLabel,
		stroke = stroke,
	}

	button.Activated:Connect(function()
		local data = DataController.get()
		if not data then
			return
		end
		local equippedUid = data.equipped[slotId]
		if equippedUid then
			openPopup(equippedUid)
		end
	end)

	return button
end

-- ==================== grid cell ====================

local function buildGridCell(gearUid: string, inst: any, order: number): ImageButton
	local Theme = UIFactory.Theme
	local rarityDef = Rarities.get(inst.rarity)
	local rarityColor = rarityDef and rarityDef.color or Theme.SubText
	local gearDef = Gear.get(inst.id)
	local slotDef = gearDef and Slots.get(gearDef.slot)

	local cell = UIFactory.imageButton({
		Name = gearUid,
		BackgroundColor3 = dim(rarityColor, 0.35),
		BackgroundTransparency = 0,
		LayoutOrder = order,
		AutoButtonColor = true,
	})
	UIFactory.corner(8).Parent = cell
	UIFactory.stroke({ Color = rarityColor, Thickness = 2 }).Parent = cell

	local glyph = UIFactory.textLabel({
		Size = UDim2.fromScale(1, 1),
		Font = Theme.FontBold,
		TextColor3 = Theme.AccentText,
		TextSize = 18,
		Text = slotDef and abbrev(slotDef.name) or "?",
	})
	glyph.Parent = cell

	cell.Activated:Connect(function()
		openPopup(gearUid)
	end)

	return cell
end

-- A non-interactive placeholder tile that signals free inventory capacity (R-E4).
local function buildEmptyCell(order: number): Frame
	local Theme = UIFactory.Theme
	local cell = UIFactory.frame({
		Name = "Empty",
		BackgroundColor3 = Theme.Background,
		BackgroundTransparency = 0.3,
		LayoutOrder = order,
	})
	UIFactory.corner(8).Parent = cell
	local stroke = UIFactory.stroke({ Color = Theme.Stroke })
	stroke.Transparency = 0.35
	stroke.Parent = cell

	local plus = UIFactory.textLabel({
		Size = UDim2.fromScale(1, 1),
		Font = Theme.FontBold,
		TextColor3 = Theme.SubText,
		TextTransparency = 0.55,
		TextSize = 22,
		Text = "+",
	})
	plus.Parent = cell
	return cell
end

-- Size the grid to a fixed column count for the current width, then top up the
-- last row(s) with empty tiles. Called on data change and whenever the grid
-- resizes (so column count / empty count stay correct).
local function layoutGrid()
	if not (gridFrame and gridLayout) then
		return
	end
	local grid = gridFrame :: ScrollingFrame
	local gLayout = gridLayout :: UIGridLayout

	local compact = Responsive.isCompact()
	local pad = Responsive.scaled(10)
	local edge = Responsive.scaled(8)
	local targetTile = Responsive.scaled(if compact then 84 else 96)

	local width = grid.AbsoluteSize.X
	if width <= 0 then
		return
	end

	-- Keep an inner buffer on all sides and reserve the scrollbar so no tile is
	-- clipped by the frame edge (the reported issue). Then derive the column
	-- count from a target size and size cells to fill the buffered width exactly.
	if gridPadding then
		local udim = UDim.new(0, edge)
		gridPadding.PaddingLeft = udim
		gridPadding.PaddingRight = udim
		gridPadding.PaddingTop = udim
		gridPadding.PaddingBottom = udim
	end

	local available = width - edge * 2 - grid.ScrollBarThickness
	if available <= 0 then
		return
	end

	local columns = InventoryLayout.columnCount(available, targetTile, pad)
	local cell = InventoryLayout.cellSize(available, columns, pad)
	gLayout.CellPadding = UDim2.fromOffset(pad, pad)
	gLayout.CellSize = UDim2.fromOffset(cell, cell)
	inventoryCellSize = cell

	-- Fill out complete rows with empty tiles so the inventory reads as having
	-- spare capacity. A new row of empties only appears once items overflow the
	-- current rows (or the minimum needed to fill the visible area). See
	-- Shared/Logic/InventoryLayout (unit-tested in tests/LogicTests).
	clearEmpties()
	local height = grid.AbsoluteSize.Y - edge * 2
	local minRows = math.max(1, math.floor((height + pad) / (cell + pad)))
	local plan = InventoryLayout.plan(gridItemCount, columns, minRows)
	for i = 1, plan.emptyCount do
		local tile = buildEmptyCell(gridItemCount + i)
		tile.Parent = grid
		table.insert(emptyCells, tile)
	end
end

-- ==================== render ====================

local function renderSlots(data: any)
	for slotId, widget in slotWidgets do
		local Theme = UIFactory.Theme
		local level = data.slotLevels[slotId] or 1
		widget.levelLabel.Text = ("Lv %d"):format(level)

		local equippedUid = data.equipped[slotId]
		local inst = equippedUid and data.gear[equippedUid]
		-- Only surface the slot level when something is actually equipped here.
		widget.badge.Visible = inst ~= nil
		if inst then
			local rarityDef = Rarities.get(inst.rarity)
			local rarityColor = rarityDef and rarityDef.color or Theme.SubText
			widget.icon.BackgroundColor3 = dim(rarityColor, 0.25)
			widget.stroke.Color = rarityColor
			widget.stroke.Thickness = 2
			widget.glyph.TextColor3 = Theme.AccentText
		else
			widget.icon.BackgroundColor3 = Theme.PanelAlt
			widget.stroke.Color = Theme.Stroke
			widget.stroke.Thickness = 1
			widget.glyph.TextColor3 = Theme.SubText
		end
	end
end

local function renderGrid(data: any)
	clearGrid()

	-- Collect currently-equipped uids so they are excluded from the grid (R-E4).
	local equippedSet: { [string]: boolean } = {}
	for _, uid in data.equipped do
		if uid then
			equippedSet[uid] = true
		end
	end

	local list: { { uid: string, inst: any } } = {}
	for uid, inst in data.gear do
		if not equippedSet[uid] then
			table.insert(list, { uid = uid, inst = inst })
		end
	end

	-- Rarity descending, then uid for a stable order (R-E4).
	table.sort(list, function(a, b)
		local ra = Rarities.orderOf(a.inst.rarity)
		local rb = Rarities.orderOf(b.inst.rarity)
		if ra ~= rb then
			return ra > rb
		end
		return a.uid < b.uid
	end)

	for index, entry in list do
		local cell = buildGridCell(entry.uid, entry.inst, index)
		cell.Parent = gridFrame
		table.insert(gridCells, cell)
	end

	gridItemCount = #list
	layoutGrid()
end

-- ==================== merge panel ====================

local MERGE_COUNT = MergeLogic.COUNT

-- Forward declarations so handlers and refreshers can reference each other.
local refreshMergeSlots: () -> ()
local refreshMergeList: () -> ()

-- The type + rarity filter implied by the first selected gear, or nil if none.
local function currentMergeFilter(): { slot: string, rarity: string }?
	local data = DataController.get()
	local firstUid = mergeSelected[1]
	if not (data and firstUid) then
		return nil
	end
	local inst = data.gear[firstUid]
	if not inst then
		return nil
	end
	local slot = MergeLogic.slotOf(inst)
	if not slot then
		return nil
	end
	return { slot = slot, rarity = inst.rarity }
end

local function isSelected(uid: string): boolean
	for _, u in mergeSelected do
		if u == uid then
			return true
		end
	end
	return false
end

-- Paint an input/output tile to show a gear instance, or an empty placeholder.
local function styleGearTile(container: GuiObject, inst: any, emptyGlyph: string)
	local Theme = UIFactory.Theme
	local glyph = container:FindFirstChild("Glyph") :: TextLabel?
	local stroke = container:FindFirstChildWhichIsA("UIStroke") :: UIStroke?
	if inst then
		local gearDef = Gear.get(inst.id)
		local slotDef = gearDef and Slots.get(gearDef.slot)
		local rarityDef = Rarities.get(inst.rarity)
		local rarityColor = rarityDef and rarityDef.color or Theme.SubText
		container.BackgroundColor3 = dim(rarityColor, 0.35)
		if stroke then
			stroke.Color = rarityColor
			stroke.Thickness = 2
		end
		if glyph then
			glyph.Text = slotDef and abbrev(slotDef.name) or "?"
			glyph.TextColor3 = Theme.AccentText
		end
	else
		container.BackgroundColor3 = Theme.Background
		if stroke then
			stroke.Color = Theme.Stroke
			stroke.Thickness = 1
		end
		if glyph then
			glyph.Text = emptyGlyph
			glyph.TextColor3 = Theme.SubText
		end
	end
end

-- True if the player owns at least one auto-mergeable set (3 same type+rarity).
local function autoMergeAvailable(): boolean
	local data = DataController.get()
	if not data then
		return false
	end
	local instances: { { uid: string, id: string, rarity: string } } = {}
	for uid, inst in data.gear do
		table.insert(instances, { uid = uid, id = inst.id, rarity = inst.rarity })
	end
	return #MergeLogic.planAuto(instances).removeUids > 0
end

function refreshMergeSlots()
	local Theme = UIFactory.Theme
	local data = DataController.get()

	for index, button in mergeInputButtons do
		local uid = mergeSelected[index]
		local inst = uid and data and data.gear[uid] or nil
		styleGearTile(button, inst, "+")
	end

	-- Output preview = the upgrade of the first pick (all picks share type+rarity).
	local firstUid = mergeSelected[1]
	local firstInst = firstUid and data and data.gear[firstUid] or nil
	local preview = MergeLogic.resultFor(firstInst)
	if mergeOutputFrame then
		styleGearTile(mergeOutputFrame, preview, "?")
	end

	-- Button behaviour depends on whether a manual selection has started:
	--  * 0 picked  -> "Auto Merge Equipment" (enabled only if something can merge)
	--  * 1-2 picked -> "Select N gear" (disabled; manual flow in progress)
	--  * 3 picked   -> "Merge" (enabled when the upgrade is valid)
	local confirm = mergeConfirmButton
	if confirm then
		if #mergeSelected == 0 then
			confirm.Text = "Auto Merge Equipment"
			if autoMergeAvailable() then
				confirm.BackgroundColor3 = Theme.Accent
				confirm.AutoButtonColor = true
				mergeButtonMode = "auto"
			else
				confirm.BackgroundColor3 = Theme.PanelAlt
				confirm.AutoButtonColor = false
				mergeButtonMode = "disabled"
			end
		elseif #mergeSelected == MERGE_COUNT and preview ~= nil then
			confirm.Text = "Merge"
			confirm.BackgroundColor3 = Theme.Positive
			confirm.AutoButtonColor = true
			mergeButtonMode = "manual"
		elseif #mergeSelected < MERGE_COUNT then
			confirm.Text = ("Select %d gear"):format(MERGE_COUNT - #mergeSelected)
			confirm.BackgroundColor3 = Theme.PanelAlt
			confirm.AutoButtonColor = false
			mergeButtonMode = "disabled"
		else
			confirm.Text = "Cannot merge"
			confirm.BackgroundColor3 = Theme.PanelAlt
			confirm.AutoButtonColor = false
			mergeButtonMode = "disabled"
		end
	end
end

local function addMergeSelection(uid: string)
	if #mergeSelected >= MERGE_COUNT or isSelected(uid) then
		return
	end
	local data = DataController.get()
	if not data then
		return
	end
	local inst = data.gear[uid]
	if not inst then
		return
	end
	-- After the first pick, enforce matching type + rarity.
	local filter = currentMergeFilter()
	if filter and (MergeLogic.slotOf(inst) ~= filter.slot or inst.rarity ~= filter.rarity) then
		return
	end
	table.insert(mergeSelected, uid)
	refreshMergeSlots()
	refreshMergeList()
end

local function removeMergeSelection(index: number)
	if not mergeSelected[index] then
		return
	end
	table.remove(mergeSelected, index)
	refreshMergeSlots()
	refreshMergeList()
end

local function clearMergeList()
	for _, cell in mergeListCells do
		cell:Destroy()
	end
	table.clear(mergeListCells)
end

-- One selectable gear tile in the merge list; labels equipped pieces "Equipped".
local function buildMergeListTile(uid: string, inst: any, equipped: boolean): TextButton
	local Theme = UIFactory.Theme
	local rarityDef = Rarities.get(inst.rarity)
	local rarityColor = rarityDef and rarityDef.color or Theme.SubText
	local gearDef = Gear.get(inst.id)
	local slotDef = gearDef and Slots.get(gearDef.slot)

	local cell = UIFactory.textButton({
		Name = uid,
		BackgroundColor3 = dim(rarityColor, 0.35),
		AutoButtonColor = true,
		Text = "",
	})
	UIFactory.corner(8).Parent = cell
	UIFactory.stroke({ Color = rarityColor, Thickness = 2 }).Parent = cell

	local glyph = UIFactory.textLabel({
		Size = UDim2.fromScale(1, 1),
		Font = Theme.FontBold,
		TextColor3 = Theme.AccentText,
		TextSize = 18,
		Text = slotDef and abbrev(slotDef.name) or "?",
	})
	glyph.Parent = cell

	if equipped then
		local tag = UIFactory.textLabel({
			Name = "EquippedTag",
			AnchorPoint = Vector2.new(0.5, 1),
			Position = UDim2.new(0.5, 0, 1, -2),
			Size = UDim2.new(1, -6, 0, 14),
			BackgroundColor3 = Theme.Background,
			BackgroundTransparency = 0.15,
			Font = Theme.FontBold,
			TextColor3 = Theme.Positive,
			TextSize = 11,
			Text = "Equipped",
		})
		UIFactory.corner(4).Parent = tag
		tag.Parent = cell
	end

	cell.Activated:Connect(function()
		addMergeSelection(uid)
	end)
	return cell
end

function refreshMergeList()
	clearMergeList()
	local data = DataController.get()
	if not (data and mergeListFrame) then
		return
	end

	local equippedSet: { [string]: boolean } = {}
	for _, uid in data.equipped do
		if uid then
			equippedSet[uid] = true
		end
	end

	local filter = currentMergeFilter()
	local list: { { uid: string, inst: any } } = {}
	for uid, inst in data.gear do
		if isSelected(uid) then
			continue
		end
		-- Skip gear that cannot be upgraded (e.g. Legendary has no higher tier).
		if not Rarities.next(inst.rarity) then
			continue
		end
		if filter and (MergeLogic.slotOf(inst) ~= filter.slot or inst.rarity ~= filter.rarity) then
			continue
		end
		table.insert(list, { uid = uid, inst = inst })
	end

	-- Rarity descending, then slot, then uid for a stable, grouped order.
	table.sort(list, function(a, b)
		local ra = Rarities.orderOf(a.inst.rarity)
		local rb = Rarities.orderOf(b.inst.rarity)
		if ra ~= rb then
			return ra > rb
		end
		local sa = MergeLogic.slotOf(a.inst) or ""
		local sb = MergeLogic.slotOf(b.inst) or ""
		if sa ~= sb then
			return sa < sb
		end
		return a.uid < b.uid
	end)

	for _, entry in list do
		local cell = buildMergeListTile(entry.uid, entry.inst, equippedSet[entry.uid] == true)
		cell.Parent = mergeListFrame
		table.insert(mergeListCells, cell)
	end
end

local function closeMergePanel()
	table.clear(mergeSelected)
	if mergeOverlay then
		mergeOverlay.Visible = false
	end
end

local function openMergePanel()
	table.clear(mergeSelected)
	if mergeOverlay then
		mergeOverlay.Visible = true
	end
	refreshMergeSlots()
	refreshMergeList()
end

-- Keep the panel consistent after any profile change (e.g. a successful merge
-- removes the consumed uids). Prunes stale selections, then re-renders.
local function refreshMergePanel()
	if not (mergeOverlay and mergeOverlay.Visible) then
		return
	end
	local data = DataController.get()
	if data then
		local kept: { string } = {}
		for _, uid in mergeSelected do
			if data.gear[uid] then
				table.insert(kept, uid)
			end
		end
		mergeSelected = kept
	end
	refreshMergeSlots()
	refreshMergeList()
end

local function render()
	local data = DataController.get()
	if not data then
		return
	end
	renderSlots(data)
	renderGrid(data)
	refreshPopup()
	refreshMergePanel()
end

-- ==================== layout (responsive) ====================

-- Number of equipment slots on the busier side (used to bound slot tile size).
local function maxSlotsPerSide(): number
	local counts: { [string]: number } = {}
	for _, slotId in Slots.Order do
		local def = Slots.get(slotId)
		if def then
			counts[def.side] = (counts[def.side] or 0) + 1
		end
	end
	local most = 1
	for _, n in counts do
		most = math.max(most, n)
	end
	return most
end

-- Spread a side's slots evenly from the top to the bottom of its column and size
-- each tile to `tile`. Using AnchorPoint.Y == Position.Y scale keeps every tile
-- inside the column bounds (top tile top-aligned, bottom tile bottom-aligned).
local function layoutColumn(side: string, tile: number)
	local sideSlots: { { id: string, row: number } } = {}
	for _, slotId in Slots.Order do
		local def = Slots.get(slotId)
		if def and def.side == side then
			table.insert(sideSlots, { id = slotId, row = def.row })
		end
	end
	table.sort(sideSlots, function(a, b)
		return a.row < b.row
	end)

	local n = #sideSlots
	for i, entry in sideSlots do
		local widget = slotWidgets[entry.id]
		if widget then
			local f = if n > 1 then (i - 1) / (n - 1) else 0.5
			widget.button.AnchorPoint = Vector2.new(0.5, f)
			widget.button.Position = UDim2.new(0.5, 0, f, 0)
			widget.button.Size = UDim2.fromOffset(tile, tile)
		end
	end
end

local function layout()
	if not (rootFrame and characterArea and viewport and gridFrame and gridLayout) then
		return
	end
	local compact = Responsive.isCompact()
	local gap = Responsive.scaled(12)

	local gridArea = rootFrame:FindFirstChild("InventoryArea") :: Frame?
	local divider = sideDivider

	-- Phone/compact: stack character (top) over inventory (bottom).
	-- Desktop: character on the LEFT, gear grid on the RIGHT.
	-- A divider line makes a clear cut between the two sides.
	if compact then
		local charHeightScale = 0.5
		characterArea.Position = UDim2.fromScale(0, 0)
		characterArea.Size = UDim2.new(1, 0, charHeightScale, -gap)
		if gridArea then
			gridArea.Position = UDim2.new(0, 0, charHeightScale, 0)
			gridArea.Size = UDim2.new(1, 0, 1 - charHeightScale, 0)
		end
		if divider then
			divider.AnchorPoint = Vector2.new(0.5, 0.5)
			divider.Position = UDim2.fromScale(0.5, charHeightScale)
			divider.Size = UDim2.new(1, 0, 0, 1)
		end
	else
		local charWidthScale = 0.5
		characterArea.Position = UDim2.fromScale(0, 0)
		characterArea.Size = UDim2.new(charWidthScale, -gap, 1, 0)
		if gridArea then
			gridArea.Position = UDim2.new(charWidthScale, gap, 0, 0)
			gridArea.Size = UDim2.new(1 - charWidthScale, -gap, 1, 0)
		end
		if divider then
			divider.AnchorPoint = Vector2.new(0.5, 0.5)
			divider.Position = UDim2.fromScale(charWidthScale, 0.5)
			divider.Size = UDim2.new(0, 1, 1, 0)
		end
	end

	-- Viewport fills the character area; the equipped-gear columns sit ON TOP of
	-- its edges so the gear overlaps the rig and reads as one panel (R-E1/R-E2).
	viewport.Position = UDim2.fromScale(0, 0)
	viewport.Size = UDim2.fromScale(1, 1)

	-- Refresh the inventory first so equipped tiles can match its cell size.
	layoutGrid()

	-- Buffers keep the gear off the viewport edges; columns span top->bottom.
	local vBuffer = Responsive.scaled(16)
	local edgeBuffer = Responsive.scaled(12)
	local perSide = maxSlotsPerSide()
	local charH = characterArea.AbsoluteSize.Y
	local colH = math.max(0, charH - vBuffer * 2)

	-- Match the unequipped tile size, but never so large that the slots would
	-- overlap within the available column height.
	local target = if inventoryCellSize > 0 then inventoryCellSize else Responsive.scaled(76)
	local maxTile = if colH > 0 then colH / perSide else target
	local tile = math.clamp(math.min(target, maxTile), Responsive.scaled(44), Responsive.scaled(140))

	if leftColumn and rightColumn then
		leftColumn.AnchorPoint = Vector2.new(0, 0.5)
		leftColumn.Position = UDim2.new(0, edgeBuffer, 0.5, 0)
		leftColumn.Size = UDim2.new(0, tile, 1, -vBuffer * 2)
		rightColumn.AnchorPoint = Vector2.new(1, 0.5)
		rightColumn.Position = UDim2.new(1, -edgeBuffer, 0.5, 0)
		rightColumn.Size = UDim2.new(0, tile, 1, -vBuffer * 2)
		layoutColumn("Left", tile)
		layoutColumn("Right", tile)
	end

	if popupCard then
		popupCard.Size = UDim2.fromOffset(Responsive.scaled(320), 0)
	end
end

-- ==================== build ====================

-- One clickable input slot in the merge row; clicking removes its selection.
local function buildMergeInputTile(index: number): TextButton
	local Theme = UIFactory.Theme
	local btn = UIFactory.textButton({
		Name = "Input" .. index,
		BackgroundColor3 = Theme.Background,
		AutoButtonColor = false,
		Text = "",
	})
	UIFactory.corner(10).Parent = btn
	UIFactory.stroke({ Color = Theme.Stroke }).Parent = btn
	local glyph = UIFactory.textLabel({
		Name = "Glyph",
		Size = UDim2.fromScale(1, 1),
		Font = Theme.FontBold,
		TextColor3 = Theme.SubText,
		TextSize = 24,
		Text = "+",
	})
	glyph.Parent = btn
	btn.Activated:Connect(function()
		removeMergeSelection(index)
	end)
	return btn
end

-- The full-screen merge modal: 3 input slots -> upgraded preview, a merge button,
-- and a filtered list of the player's gear (R-E9).
local function buildMergePanel(parent: Instance)
	local Theme = UIFactory.Theme

	local overlay = UIFactory.frame({
		Name = "MergeOverlay",
		BackgroundColor3 = Color3.new(0, 0, 0),
		BackgroundTransparency = 0.45,
		Size = UDim2.fromScale(1, 1),
		Visible = false,
		ZIndex = 30,
	})
	overlay.Parent = parent
	mergeOverlay = overlay

	-- Backdrop blocks clicks to the tab behind. It does NOT close the panel —
	-- only the X button does (per request).
	local backdrop = UIFactory.textButton({
		Name = "Backdrop",
		BackgroundTransparency = 1,
		AutoButtonColor = false,
		Text = "",
		Size = UDim2.fromScale(1, 1),
		ZIndex = 30,
	})
	backdrop.Parent = overlay

	local cardW = Responsive.scaled(660)
	local cardH = Responsive.scaled(720)
	local card = UIFactory.frame({
		Name = "Card",
		BackgroundColor3 = Theme.Panel,
		AnchorPoint = Vector2.new(0.5, 0.5),
		Position = UDim2.fromScale(0.5, 0.5),
		Size = UDim2.fromOffset(cardW, cardH),
		-- Absorb clicks so hitting empty card space doesn't reach the click-off
		-- backdrop behind it (only the X should close the panel).
		Active = true,
		ZIndex = 31,
	})
	card.Parent = overlay
	mergeCard = card
	UIFactory.corner(14).Parent = card
	UIFactory.stroke({ Color = Theme.Stroke }).Parent = card
	UIFactory.padding(Responsive.scaled(22)).Parent = card

	local title = UIFactory.textLabel({
		Name = "Title",
		Position = UDim2.fromOffset(0, 0),
		Size = UDim2.new(1, -34, 0, 30),
		Font = Theme.FontBold,
		TextColor3 = Theme.Text,
		TextSize = 24,
		TextXAlignment = Enum.TextXAlignment.Left,
		Text = "Merge Equipment",
		ZIndex = 31,
	})
	title.Parent = card

	local close = UIFactory.textButton({
		Name = "Close",
		AnchorPoint = Vector2.new(1, 0),
		Position = UDim2.new(1, 0, 0, 0),
		Size = UDim2.fromOffset(30, 30),
		BackgroundColor3 = Theme.PanelAlt,
		Text = "X",
		TextSize = 18,
		ZIndex = 32,
	})
	UIFactory.corner(8).Parent = close
	close.Parent = card
	close.Activated:Connect(closeMergePanel)

	local subtitle = UIFactory.textLabel({
		Name = "Subtitle",
		Position = UDim2.fromOffset(0, 36),
		Size = UDim2.new(1, 0, 0, 20),
		TextColor3 = Theme.SubText,
		TextSize = 15,
		TextXAlignment = Enum.TextXAlignment.Left,
		Text = "Pick 3 gear of the same type and rarity to upgrade them.",
		ZIndex = 31,
	})
	subtitle.Parent = card

	-- Merge row: three input slots, an arrow, and the upgraded preview.
	local rowH = Responsive.scaled(96)
	local row = UIFactory.frame({
		Name = "MergeRow",
		BackgroundTransparency = 1,
		Position = UDim2.fromOffset(0, 64),
		Size = UDim2.new(1, 0, 0, rowH),
		ZIndex = 31,
	})
	row.Parent = card
	local rowLayout = UIFactory.listLayout({
		FillDirection = Enum.FillDirection.Horizontal,
		HorizontalAlignment = Enum.HorizontalAlignment.Center,
		VerticalAlignment = Enum.VerticalAlignment.Center,
		Padding = UDim.new(0, Responsive.scaled(10)),
	})
	rowLayout.Parent = row

	table.clear(mergeInputButtons)
	for i = 1, MERGE_COUNT do
		local btn = buildMergeInputTile(i)
		btn.Size = UDim2.fromOffset(rowH, rowH)
		btn.LayoutOrder = i
		btn.ZIndex = 31
		btn.Parent = row
		table.insert(mergeInputButtons, btn)
	end

	local arrow = UIFactory.textLabel({
		Name = "Arrow",
		LayoutOrder = MERGE_COUNT + 1,
		Size = UDim2.fromOffset(Responsive.scaled(36), rowH),
		Font = Theme.FontBold,
		TextColor3 = Theme.SubText,
		TextSize = 28,
		Text = ">",
		ZIndex = 31,
	})
	arrow.Parent = row

	local output = UIFactory.frame({
		Name = "Output",
		LayoutOrder = MERGE_COUNT + 2,
		BackgroundColor3 = Theme.Background,
		Size = UDim2.fromOffset(rowH, rowH),
		ZIndex = 31,
	})
	UIFactory.corner(10).Parent = output
	UIFactory.stroke({ Color = Theme.Accent, Thickness = 2 }).Parent = output
	local oglyph = UIFactory.textLabel({
		Name = "Glyph",
		Size = UDim2.fromScale(1, 1),
		Font = Theme.FontBold,
		TextColor3 = Theme.SubText,
		TextSize = 24,
		Text = "?",
		ZIndex = 31,
	})
	oglyph.Parent = output
	output.Parent = row
	mergeOutputFrame = output

	local confirmY = 64 + rowH + 18
	local confirm = UIFactory.textButton({
		Name = "Confirm",
		Position = UDim2.fromOffset(0, confirmY),
		Size = UDim2.new(1, 0, 0, 50),
		BackgroundColor3 = Theme.PanelAlt,
		AutoButtonColor = false,
		TextSize = 20,
		Text = "Auto Merge Equipment",
		ZIndex = 31,
	})
	UIFactory.corner(10).Parent = confirm
	confirm.Parent = card
	mergeConfirmButton = confirm
	confirm.Activated:Connect(function()
		if mergeButtonMode == "manual" then
			if mergeGearRemote then
				mergeGearRemote:FireServer(table.clone(mergeSelected))
			end
		elseif mergeButtonMode == "auto" then
			if autoMergeGearRemote then
				autoMergeGearRemote:FireServer()
			end
		end
	end)

	local dividerY = confirmY + 50 + 18
	local divider = UIFactory.frame({
		Name = "Divider",
		Position = UDim2.fromOffset(0, dividerY),
		BackgroundColor3 = Theme.Stroke,
		Size = UDim2.new(1, 0, 0, 1),
		ZIndex = 31,
	})
	divider.Parent = card

	local listLabelY = dividerY + 8
	local listLabel = UIFactory.textLabel({
		Name = "ListLabel",
		Position = UDim2.fromOffset(0, listLabelY),
		Size = UDim2.new(1, 0, 0, 18),
		Font = Theme.FontBold,
		TextColor3 = Theme.Text,
		TextSize = 15,
		TextXAlignment = Enum.TextXAlignment.Left,
		Text = "Your Gear",
		ZIndex = 31,
	})
	listLabel.Parent = card

	local listY = listLabelY + 26
	local list = UIFactory.scrolling({
		Name = "GearList",
		Position = UDim2.fromOffset(0, listY),
		Size = UDim2.new(1, 0, 1, -listY),
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		ScrollingDirection = Enum.ScrollingDirection.Y,
		ZIndex = 31,
	})
	list.Parent = card
	mergeListFrame = list
	local listGrid = UIFactory.gridLayout({
		CellSize = UDim2.fromOffset(Responsive.scaled(84), Responsive.scaled(84)),
		CellPadding = UDim2.fromOffset(Responsive.scaled(12), Responsive.scaled(12)),
		HorizontalAlignment = Enum.HorizontalAlignment.Left,
	})
	listGrid.Parent = list
	-- Spacing buffer + scrollbar reservation so tiles never clip the edges,
	-- matching the inventory grid.
	local listBuffer = Responsive.scaled(12)
	local listPad = UIFactory.create("UIPadding", {
		PaddingLeft = UDim.new(0, listBuffer),
		PaddingRight = UDim.new(0, listBuffer + list.ScrollBarThickness),
		PaddingTop = UDim.new(0, listBuffer),
		PaddingBottom = UDim.new(0, listBuffer),
	})
	listPad.Parent = list
end

local function buildColumn(name: string, side: "Left" | "Right"): Frame
	-- Slots are positioned manually in layout() (spread top->bottom), so no
	-- UIListLayout here.
	return UIFactory.frame({
		Name = name,
		BackgroundTransparency = 1,
		Size = UDim2.new(0, 76, 1, 0),
		Position = if side == "Left" then UDim2.fromScale(0, 0) else UDim2.new(1, 0, 0, 0),
		AnchorPoint = if side == "Left" then Vector2.new(0, 0) else Vector2.new(1, 0),
	})
end

function EquipmentTab.init(injected: any)
	deps = injected
	UIFactory = deps.Util.UIFactory
	Responsive = deps.Util.Responsive
	AssetResolver = deps.Util.AssetResolver
	MenuShell = deps.UI.MenuShell
	DataController = deps.Controllers.DataController
	equipRemote = deps.Remotes.getEvent("EquipGear")
	unequipRemote = deps.Remotes.getEvent("UnequipGear")
	levelSlotRemote = deps.Remotes.getEvent("LevelSlot")
	mergeGearRemote = deps.Remotes.getEvent("MergeGear")
	autoMergeGearRemote = deps.Remotes.getEvent("AutoMergeGear")
end

function EquipmentTab.start()
	local content = MenuShell.getContentFrame("Equipment")
	if not content then
		warn("[EquipmentTab] Equipment content frame missing")
		return
	end

	-- Remove the shell placeholder now that we render the real view.
	local placeholder = content:FindFirstChild("Placeholder")
	if placeholder then
		placeholder:Destroy()
	end

	local Theme = UIFactory.Theme
	local root = UIFactory.frame({
		Name = "EquipmentRoot",
		BackgroundTransparency = 1,
		Size = UDim2.fromScale(1, 1),
	})
	root.Parent = content
	rootFrame = root

	-- Character area: rig viewport flanked by two slot columns.
	local charArea = UIFactory.frame({
		Name = "CharacterArea",
		BackgroundTransparency = 1,
		Size = UDim2.new(1, 0, 0.58, 0),
	})
	charArea.Parent = root
	characterArea = charArea

	local vp = UIFactory.create("ViewportFrame", {
		Name = "RigViewport",
		BackgroundColor3 = Theme.Panel,
		BorderSizePixel = 0,
		Size = UDim2.fromScale(1, 1),
	})
	vp.Parent = charArea
	viewport = vp
	UIFactory.corner(12).Parent = vp
	UIFactory.stroke({ Color = Theme.Stroke }).Parent = vp
	buildRig(vp)

	leftColumn = buildColumn("LeftSlots", "Left")
	rightColumn = buildColumn("RightSlots", "Right")
	leftColumn.Parent = charArea
	rightColumn.Parent = charArea

	-- Place the six slots per Config/Slots (Left: Helmet/Weapon/Belt;
	-- Right: Charm/Chestplate/Boots) ordered top->bottom by `row` (R-E2).
	for _, slotId in Slots.Order do
		local slotDef = Slots.get(slotId)
		if slotDef then
			local button = buildSlotButton(slotId)
			button.Parent = if slotDef.side == "Left" then leftColumn else rightColumn
		end
	end

	-- Inventory area: title + unequipped gear grid.
	local inventoryArea = UIFactory.frame({
		Name = "InventoryArea",
		BackgroundTransparency = 1,
		Size = UDim2.new(1, 0, 0.42, 0),
		Position = UDim2.fromScale(0, 0.58),
	})
	inventoryArea.Parent = root

	-- Header stack: Merge Equipment button ON TOP, then the Inventory title, then
	-- the grid — so the button is obvious and the inventory shifts down (R-E9).
	local btnH = Responsive.scaled(44)
	local titleH = Responsive.scaled(24)
	local gap = Responsive.scaled(10)
	local titleY = btnH + gap
	local headerH = titleY + titleH + gap

	local mergeButton = UIFactory.textButton({
		Name = "MergeButton",
		Position = UDim2.fromOffset(0, 0),
		Size = UDim2.new(1, 0, 0, btnH),
		BackgroundColor3 = Theme.Accent,
		Font = Theme.FontBold,
		TextColor3 = Theme.AccentText,
		TextSize = 20,
		Text = "Merge Equipment",
	})
	UIFactory.corner(10).Parent = mergeButton
	mergeButton.Parent = inventoryArea
	mergeTriggerButton = mergeButton
	mergeButton.Activated:Connect(openMergePanel)

	local invTitle = UIFactory.textLabel({
		Name = "Title",
		Position = UDim2.fromOffset(0, titleY),
		Size = UDim2.new(1, 0, 0, titleH),
		Font = Theme.FontBold,
		TextColor3 = Theme.Text,
		TextSize = 18,
		TextXAlignment = Enum.TextXAlignment.Left,
		Text = "Inventory",
	})
	invTitle.Parent = inventoryArea
	inventoryTitle = invTitle

	local grid = UIFactory.scrolling({
		Name = "GearGrid",
		Position = UDim2.new(0, 0, 0, headerH),
		Size = UDim2.new(1, 0, 1, -headerH),
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		ScrollingDirection = Enum.ScrollingDirection.Y,
	})
	grid.Parent = inventoryArea
	gridFrame = grid

	local gLayout = UIFactory.gridLayout({
		HorizontalAlignment = Enum.HorizontalAlignment.Left,
	})
	gLayout.Parent = grid
	gridLayout = gLayout

	-- Inner buffer so tiles never touch/clip the grid edges (updated in layoutGrid).
	local gridPad = UIFactory.padding(Responsive.scaled(8))
	gridPad.Parent = grid
	gridPadding = gridPad

	-- Divider giving a clear cut between the character side and the inventory.
	sideDivider = UIFactory.frame({
		Name = "SideDivider",
		BackgroundColor3 = Theme.Stroke,
		Size = UDim2.new(0, 1, 1, 0),
	})
	sideDivider.Parent = root

	-- Re-flow inventory (columns + empty tiles) whenever the grid resizes.
	grid:GetPropertyChangedSignal("AbsoluteSize"):Connect(layoutGrid)
	-- Re-run layout when the character area gets real dimensions, so equipped
	-- slot tiles size/spread correctly.
	charArea:GetPropertyChangedSignal("AbsoluteSize"):Connect(layout)

	buildPopup(root)
	buildMergePanel(root)

	layout()
	Responsive.onChanged(function()
		layout()
	end)

	-- Re-render on any authoritative profile change (equip/unequip/slot level).
	DataController.onChanged(function()
		render()
	end)
	if DataController.get() then
		render()
	end

	-- Drive the idle animation only while the Equipment tab is visible.
	rigConnection = RunService.RenderStepped:Connect(function()
		if MenuShell.getActiveTab() == "Equipment" then
			animateRig()
		end
	end)
end

return EquipmentTab
]]></ProtectedString>
							<string name="ScriptGuid">{0CCD95D6-401E-4672-83F2-57BE1014450D}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">EquipmentTab</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ac7</UniqueId>
						</Properties>
					</Item>
					<Item class="ModuleScript" referent="RBXB49FD06EE8D54CACA8CDC62B32AF1B40">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- BattleTab: the Battle view (R-B1..R-B3). The default view shows the player's
-- current chapter with a Start Battle button (R-B1). Clicking the chapter card
-- (the "level", NOT the Start Battle button) opens a scrollable list of every
-- chapter, ordered so scrolling UP reveals upcoming/locked chapters and scrolling
-- DOWN reveals beaten ones (R-B2); the list auto-centers on the current chapter.
-- Start Battle sends intent via the StartBattle remote; the server owns all combat
-- state. Progress is read from DataController (server-authoritative): a chapter is
-- BEATEN when its id <= progress.chaptersCompleted, CURRENT when it equals
-- progress.currentChapter, and LOCKED otherwise. The default view re-renders on
-- ProfileUpdated, so it advances automatically once a chapter is completed (R-B3).

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Config = Shared:WaitForChild("Config")
local Chapters = require(Config:WaitForChild("Chapters"))

local BattleTab = {}

local deps: any = nil
local UIFactory: any = nil
local Responsive: any = nil
local MenuShell: any = nil
local DataController: any = nil
local startBattleRemote: RemoteEvent? = nil

-- Default-view widgets (kept for re-render / re-layout).
local rootFrame: Frame? = nil
local chapterCard: TextButton? = nil
local cardNumberLabel: TextLabel? = nil
local cardNameLabel: TextLabel? = nil
local cardSubtitleLabel: TextLabel? = nil
local startButton: TextButton? = nil

-- Chapter-list overlay widgets.
local listOverlay: Frame? = nil
local listScroll: ScrollingFrame? = nil
type RowWidget = {
	frame: TextButton,
	stroke: UIStroke,
	title: TextLabel,
	status: TextLabel,
}
local rowWidgets: { [number]: RowWidget } = {}

-- ==================== progress helpers ====================

type ChapterStatus = "beaten" | "current" | "locked"

-- Where a chapter sits in the player's progression (server-authoritative).
local function chapterStatus(data: any, id: number): ChapterStatus
	local progress = data.progress or {}
	local completed = progress.chaptersCompleted or 0
	local current = progress.currentChapter or 1
	if id <= completed then
		return "beaten"
	elseif id == current then
		return "current"
	end
	return "locked"
end

-- The chapter the default view should feature: the current one, clamped to the
-- range of defined chapters (guards a currentChapter that ran past the last one
-- after everything is beaten).
local function displayChapterId(data: any): number
	local current = (data.progress and data.progress.currentChapter) or 1
	return math.clamp(current, 1, Chapters.count())
end

-- ==================== default view render ====================

local function render()
	local data = DataController.get()
	if not data then
		return
	end

	local id = displayChapterId(data)
	local def = Chapters.get(id)
	local status = chapterStatus(data, id)
	local allBeaten = (data.progress and data.progress.chaptersCompleted or 0) >= Chapters.count()

	if cardNumberLabel then
		cardNumberLabel.Text = ("CHAPTER %d / %d"):format(id, Chapters.count())
	end
	if cardNameLabel then
		cardNameLabel.Text = def and def.name or ("Chapter " .. id)
	end
	if cardSubtitleLabel then
		local stageCount = def and #def.stages or 0
		cardSubtitleLabel.Text = ("%d Stages   -   Tap to view all chapters"):format(stageCount)
	end

	if startButton then
		local Theme = UIFactory.Theme
		if allBeaten and status ~= "current" then
			-- Every chapter cleared: allow replaying the final chapter.
			startButton.Text = "Replay Chapter"
		else
			startButton.Text = "Start Battle"
		end
		startButton.BackgroundColor3 = Theme.Accent
		startButton.AutoButtonColor = true
	end
end

-- ==================== chapter list overlay ====================

local function closeList()
	if listOverlay then
		listOverlay.Visible = false
	end
end

-- Repaint every row from the latest progression data.
local function renderRows(data: any)
	local Theme = UIFactory.Theme
	for id, widget in rowWidgets do
		local def = Chapters.get(id)
		local status = chapterStatus(data, id)
		widget.title.Text = ("Chapter %d   -   %s"):format(id, def and def.name or "?")

		if status == "beaten" then
			widget.frame.BackgroundColor3 = Theme.Panel
			widget.stroke.Color = Theme.Positive
			widget.status.Text = "CLEARED"
			widget.status.TextColor3 = Theme.Positive
			widget.title.TextColor3 = Theme.Text
		elseif status == "current" then
			widget.frame.BackgroundColor3 = Theme.PanelAlt
			widget.stroke.Color = Theme.Accent
			widget.status.Text = "CURRENT"
			widget.status.TextColor3 = Theme.Accent
			widget.title.TextColor3 = Theme.Text
		else
			widget.frame.BackgroundColor3 = Theme.Panel
			widget.stroke.Color = Theme.Stroke
			widget.status.Text = "LOCKED"
			widget.status.TextColor3 = Theme.SubText
			widget.title.TextColor3 = Theme.SubText
		end
	end
end

-- Scroll the list so the current chapter sits centered in the viewport, giving
-- headroom to scroll up (locked) and down (beaten) as required by R-B2.
local function centerOnCurrent(data: any)
	local scroll = listScroll
	if not scroll then
		return
	end
	local row = rowWidgets[displayChapterId(data)]
	if not row then
		return
	end
	local frame = row.frame
	local offset = (frame.AbsolutePosition.Y - scroll.AbsolutePosition.Y) + scroll.CanvasPosition.Y
	local target = offset - (scroll.AbsoluteWindowSize.Y - frame.AbsoluteSize.Y) / 2
	local maxScroll = math.max(0, scroll.AbsoluteCanvasSize.Y - scroll.AbsoluteWindowSize.Y)
	scroll.CanvasPosition = Vector2.new(0, math.clamp(target, 0, maxScroll))
end

local function openList()
	local data = DataController.get()
	if not (data and listOverlay) then
		return
	end
	renderRows(data)
	listOverlay.Visible = true
	-- Defer so absolute sizes/positions are valid before we center the scroll.
	task.defer(function()
		if listOverlay and listOverlay.Visible then
			centerOnCurrent(data)
		end
	end)
end

-- Build one chapter row. Rows are informational + close the list on click; the
-- player only ever battles their current chapter via Start Battle.
local function buildRow(id: number): RowWidget
	local Theme = UIFactory.Theme
	-- Descending LayoutOrder puts the highest (locked/upcoming) chapters at the
	-- TOP and chapter 1 (beaten) at the BOTTOM (R-B2).
	local button = UIFactory.textButton({
		Name = "Chapter" .. id,
		LayoutOrder = Chapters.count() - id,
		BackgroundColor3 = Theme.Panel,
		AutoButtonColor = true,
		Size = UDim2.new(1, 0, 0, Responsive.scaled(64)),
		Text = "",
	})
	UIFactory.corner(10).Parent = button
	local stroke = UIFactory.stroke({ Color = Theme.Stroke, Thickness = 2 })
	stroke.Parent = button
	UIFactory.padding(Responsive.scaled(12)).Parent = button

	local title = UIFactory.textLabel({
		Name = "Title",
		AnchorPoint = Vector2.new(0, 0.5),
		Position = UDim2.fromScale(0, 0.5),
		Size = UDim2.new(0.68, 0, 1, 0),
		Font = Theme.FontBold,
		TextSize = Responsive.scaled(18),
		TextXAlignment = Enum.TextXAlignment.Left,
		TextTruncate = Enum.TextTruncate.AtEnd,
	})
	title.Parent = button

	local status = UIFactory.textLabel({
		Name = "Status",
		AnchorPoint = Vector2.new(1, 0.5),
		Position = UDim2.fromScale(1, 0.5),
		Size = UDim2.new(0.3, 0, 1, 0),
		Font = Theme.FontBold,
		TextSize = Responsive.scaled(14),
		TextXAlignment = Enum.TextXAlignment.Right,
	})
	status.Parent = button

	button.Activated:Connect(closeList)

	return {
		frame = button,
		stroke = stroke,
		title = title,
		status = status,
	}
end

local function buildListOverlay(parent: Instance)
	local Theme = UIFactory.Theme
	local overlay = UIFactory.frame({
		Name = "ChapterList",
		BackgroundColor3 = Color3.fromRGB(0, 0, 0),
		BackgroundTransparency = 0.45,
		Size = UDim2.fromScale(1, 1),
		Visible = false,
		ZIndex = 20,
	})
	overlay.Parent = parent
	listOverlay = overlay

	-- Click-off backdrop closes the list.
	local backdrop = UIFactory.textButton({
		Name = "Backdrop",
		BackgroundTransparency = 1,
		Text = "",
		AutoButtonColor = false,
		Size = UDim2.fromScale(1, 1),
		ZIndex = 20,
	})
	backdrop.Parent = overlay
	backdrop.Activated:Connect(closeList)

	local card = UIFactory.frame({
		Name = "Card",
		BackgroundColor3 = Theme.Background,
		AnchorPoint = Vector2.new(0.5, 0.5),
		Position = UDim2.fromScale(0.5, 0.5),
		Size = UDim2.new(0.92, 0, 0.86, 0),
		ZIndex = 21,
	})
	card.Parent = overlay
	UIFactory.corner(14).Parent = card
	UIFactory.stroke({ Color = Theme.Stroke }).Parent = card
	UIFactory.padding(Responsive.scaled(16)).Parent = card
	UIFactory.create("UISizeConstraint", {
		MaxSize = Vector2.new(Responsive.scaled(460), math.huge),
	}).Parent = card

	local headerH = Responsive.scaled(40)

	local header = UIFactory.textLabel({
		Name = "Header",
		Position = UDim2.fromScale(0, 0),
		Size = UDim2.new(1, -Responsive.scaled(40), 0, headerH),
		Font = Theme.FontBold,
		TextSize = Responsive.scaled(24),
		TextXAlignment = Enum.TextXAlignment.Left,
		Text = "Chapters",
		ZIndex = 21,
	})
	header.Parent = card

	local close = UIFactory.textButton({
		Name = "Close",
		AnchorPoint = Vector2.new(1, 0),
		Position = UDim2.fromScale(1, 0),
		Size = UDim2.fromOffset(headerH, headerH),
		BackgroundColor3 = Theme.PanelAlt,
		Text = "X",
		TextSize = Responsive.scaled(16),
		ZIndex = 22,
	})
	UIFactory.corner(8).Parent = close
	close.Parent = card
	close.Activated:Connect(closeList)

	local scroll = UIFactory.scrolling({
		Name = "ChapterScroll",
		Position = UDim2.new(0, 0, 0, headerH + Responsive.scaled(10)),
		Size = UDim2.new(1, 0, 1, -(headerH + Responsive.scaled(10))),
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		ScrollingDirection = Enum.ScrollingDirection.Y,
		ZIndex = 21,
	})
	scroll.Parent = card
	listScroll = scroll
	local layout = UIFactory.listLayout({
		FillDirection = Enum.FillDirection.Vertical,
		Padding = UDim.new(0, Responsive.scaled(8)),
		SortOrder = Enum.SortOrder.LayoutOrder,
	})
	layout.Parent = scroll
	UIFactory.create("UIPadding", {
		PaddingRight = UDim.new(0, scroll.ScrollBarThickness + Responsive.scaled(4)),
	}).Parent = scroll

	table.clear(rowWidgets)
	for _, id in Chapters.Order do
		local widget = buildRow(id)
		widget.frame.Parent = scroll
		widget.frame.ZIndex = 21
		rowWidgets[id] = widget
	end
end

-- ==================== start battle ====================

local function onStartBattle()
	local data = DataController.get()
	if not (data and startBattleRemote) then
		return
	end
	-- Send intent only; the server validates and drives combat (server-authoritative).
	startBattleRemote:FireServer(displayChapterId(data))
end

-- ==================== build ====================

function BattleTab.init(injected: any)
	deps = injected
	UIFactory = deps.Util.UIFactory
	Responsive = deps.Util.Responsive
	MenuShell = deps.UI.MenuShell
	DataController = deps.Controllers.DataController
	startBattleRemote = deps.Remotes.getEvent("StartBattle")
end

function BattleTab.start()
	local content = MenuShell.getContentFrame("Battle")
	if not content then
		warn("[BattleTab] Battle content frame missing")
		return
	end

	-- Remove the shell placeholder now that we render the real view.
	local placeholder = content:FindFirstChild("Placeholder")
	if placeholder then
		placeholder:Destroy()
	end

	local Theme = UIFactory.Theme
	local root = UIFactory.frame({
		Name = "BattleRoot",
		BackgroundTransparency = 1,
		Size = UDim2.fromScale(1, 1),
	})
	root.Parent = content
	rootFrame = root

	local startH = Responsive.scaled(60)
	local gap = Responsive.scaled(12)

	-- Chapter card fills the space above the Start Battle button. The whole card
	-- is the clickable "level" that opens the chapter list (R-B2).
	local card = UIFactory.textButton({
		Name = "ChapterCard",
		BackgroundColor3 = Theme.Panel,
		AutoButtonColor = false,
		Position = UDim2.fromScale(0, 0),
		Size = UDim2.new(1, 0, 1, -(startH + gap)),
		Text = "",
	})
	UIFactory.corner(16).Parent = card
	UIFactory.stroke({ Color = Theme.Stroke }).Parent = card
	UIFactory.padding(Responsive.scaled(20)).Parent = card
	card.Parent = root
	chapterCard = card

	local stack = UIFactory.frame({
		Name = "Stack",
		BackgroundTransparency = 1,
		AnchorPoint = Vector2.new(0.5, 0.5),
		Position = UDim2.fromScale(0.5, 0.5),
		Size = UDim2.fromScale(1, 1),
	})
	stack.Parent = card
	local stackLayout = UIFactory.listLayout({
		FillDirection = Enum.FillDirection.Vertical,
		HorizontalAlignment = Enum.HorizontalAlignment.Center,
		VerticalAlignment = Enum.VerticalAlignment.Center,
		Padding = UDim.new(0, Responsive.scaled(10)),
	})
	stackLayout.Parent = stack

	local numberLabel = UIFactory.textLabel({
		Name = "ChapterNumber",
		LayoutOrder = 1,
		AutomaticSize = Enum.AutomaticSize.XY,
		Size = UDim2.fromOffset(0, Responsive.scaled(22)),
		Font = Theme.FontBold,
		TextColor3 = Theme.Accent,
		TextSize = Responsive.scaled(18),
		Text = "CHAPTER 1",
	})
	numberLabel.Parent = stack
	cardNumberLabel = numberLabel

	local nameLabel = UIFactory.textLabel({
		Name = "ChapterName",
		LayoutOrder = 2,
		AutomaticSize = Enum.AutomaticSize.Y,
		Size = UDim2.new(1, 0, 0, Responsive.scaled(44)),
		Font = Theme.FontBold,
		TextColor3 = Theme.Text,
		TextSize = Responsive.scaled(40),
		TextWrapped = true,
		Text = "...",
	})
	nameLabel.Parent = stack
	cardNameLabel = nameLabel

	local subtitleLabel = UIFactory.textLabel({
		Name = "Subtitle",
		LayoutOrder = 3,
		AutomaticSize = Enum.AutomaticSize.Y,
		Size = UDim2.new(1, 0, 0, Responsive.scaled(20)),
		TextColor3 = Theme.SubText,
		TextSize = Responsive.scaled(16),
		TextWrapped = true,
		Text = "",
	})
	subtitleLabel.Parent = stack
	cardSubtitleLabel = subtitleLabel

	card.Activated:Connect(openList)

	-- Start Battle button pinned to the bottom, full width (R-B1).
	local start = UIFactory.textButton({
		Name = "StartButton",
		AnchorPoint = Vector2.new(0, 1),
		Position = UDim2.fromScale(0, 1),
		Size = UDim2.new(1, 0, 0, startH),
		BackgroundColor3 = Theme.Accent,
		TextSize = Responsive.scaled(26),
		Text = "Start Battle",
	})
	UIFactory.corner(12).Parent = start
	start.Parent = root
	startButton = start
	start.Activated:Connect(onStartBattle)

	-- Chapter list overlay lives above the default view within this tab.
	buildListOverlay(root)

	-- Re-render on any authoritative profile change (advances to the next chapter
	-- after one is beaten, per R-B3).
	DataController.onChanged(function()
		render()
		if listOverlay and listOverlay.Visible then
			local data = DataController.get()
			if data then
				renderRows(data)
			end
		end
	end)
	if DataController.get() then
		render()
	end
end

return BattleTab
]]></ProtectedString>
							<string name="ScriptGuid">{39B16022-0FF1-4ADB-A4F7-01BA040ED44D}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">BattleTab</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ac9</UniqueId>
						</Properties>
					</Item>
					<Item class="ModuleScript" referent="RBXEE069EB478F742FF8DC613B68C79A8BD">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- TalentsTab: the Talents view (R-T1..R-T4). Shows every talent as a square,
-- rarity-tinted tile displaying just its name + level; clicking a tile opens a
-- detail popup with the current stat boost, the next-level preview, and a plain-
-- language description. A Roll Talent button is pinned to the bottom of the tab
-- and spends coins to rarity-weighted +1 one talent. All state is read from
-- DataController (server-authoritative); the Roll button only sends intent via the
-- RollTalent remote and the view re-renders on ProfileUpdated. A small banner
-- reports the last roll by diffing talent levels between replicated profiles.

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Config = Shared:WaitForChild("Config")
local Logic = Shared:WaitForChild("Logic")
local Talents = require(Config:WaitForChild("Talents"))
local Rarities = require(Config:WaitForChild("Rarities"))
local Balance = require(Config:WaitForChild("Balance"))
local TalentLogic = require(Logic:WaitForChild("TalentLogic"))

local TalentsTab = {}

local deps: any = nil
local UIFactory: any = nil
local Responsive: any = nil
local AssetResolver: any = nil
local MenuShell: any = nil
local DataController: any = nil
local rollTalentRemote: RemoteEvent? = nil

-- The coin cost per roll (R-T2), read from Balance so tuning stays in config.
local ROLL_COST = Balance.Economy.talentRollCost

-- Built instances kept for re-render / re-layout.
local rootFrame: Frame? = nil
local headerFrame: Frame? = nil
local coinLabel: TextLabel? = nil
local rollButton: TextButton? = nil
local bannerLabel: TextLabel? = nil
local gridFrame: ScrollingFrame? = nil
local gridLayout: UIGridLayout? = nil
local gridPadding: UIPadding? = nil

-- Square tiles only show a name + level; details live in the popup below.
type TileWidget = {
	frame: Frame,
	stroke: UIStroke,
	name: TextLabel,
	level: TextLabel,
}
local tileWidgets: { [string]: TileWidget } = {}

-- Detail popup elements + which talent is currently shown (for live refresh).
local popupOverlay: Frame? = nil
local popupTitle: TextLabel? = nil
local popupSubtitle: TextLabel? = nil
local popupStat: TextLabel? = nil
local popupNext: TextLabel? = nil
local popupDesc: TextLabel? = nil
local popupSelectedId: string? = nil

-- Snapshot of talent levels from the previous render, used to detect which talent
-- a roll leveled up (so the banner + row flash can point it out).
local lastLevels: { [string]: number } = {}
local bannerResetToken = 0

-- ==================== small helpers ====================

-- Blend `color` toward the panel color to produce a muted tile fill (mirrors the
-- gear-tile treatment in EquipmentTab so the two grids feel consistent).
local function dim(color: Color3, alpha: number): Color3
	return color:Lerp(UIFactory.Theme.Panel, alpha)
end

local function coinBalance(data: any): number
	return (data.currencies and data.currencies.coins) or 0
end

-- ==================== roll button ====================

-- Enable / label the Roll button from the latest data: disabled when the player
-- cannot afford a roll or every talent is already maxed.
local function updateRollButton(data: any)
	local button = rollButton
	if not button then
		return
	end
	local Theme = UIFactory.Theme
	local coins = coinBalance(data)
	local _, poolWeight = TalentLogic.buildPool(data.talents)

	if poolWeight <= 0 then
		button.Text = "All Talents Maxed"
		button.BackgroundColor3 = Theme.PanelAlt
		button.AutoButtonColor = false
	elseif coins < ROLL_COST then
		button.Text = ("Roll Talent  (%d C)"):format(ROLL_COST)
		button.BackgroundColor3 = Theme.PanelAlt
		button.AutoButtonColor = false
	else
		button.Text = ("Roll Talent  (%d C)"):format(ROLL_COST)
		button.BackgroundColor3 = Theme.Accent
		button.AutoButtonColor = true
	end
end

-- True if a roll is currently allowed (affordable + something to roll).
local function canRoll(data: any): boolean
	local _, poolWeight = TalentLogic.buildPool(data.talents)
	return poolWeight > 0 and coinBalance(data) >= ROLL_COST
end

-- ==================== result banner ====================

-- Show the last-roll banner for a moment, then fade back to the idle hint.
local function flashBanner(text: string)
	local banner = bannerLabel
	if not banner then
		return
	end
	banner.Text = text
	banner.TextColor3 = UIFactory.Theme.Positive

	bannerResetToken += 1
	local token = bannerResetToken
	task.delay(3, function()
		if bannerResetToken == token and bannerLabel then
			bannerLabel.Text = ("Each roll costs %d coins and levels a random talent."):format(ROLL_COST)
			bannerLabel.TextColor3 = UIFactory.Theme.SubText
		end
	end)
end

-- ==================== detail popup ====================

local function closePopup()
	popupSelectedId = nil
	if popupOverlay then
		popupOverlay.Visible = false
	end
end

-- Fill the popup with a talent's stat boost + description. Returns false if the
-- talent can no longer be resolved.
local function populatePopup(talentId: string): boolean
	local data = DataController.get()
	local def = Talents.get(talentId)
	if not (data and def) then
		return false
	end
	local level = data.talents[talentId] or 0
	local rarityDef = Rarities.get(def.rarity)
	local rarityColor = rarityDef and rarityDef.color or UIFactory.Theme.Text

	local title = popupTitle :: TextLabel
	title.Text = def.name
	title.TextColor3 = rarityColor

	local subtitle = popupSubtitle :: TextLabel
	subtitle.Text = ("%s  -  %s  -  Lv %d"):format(def.rarity, TalentLogic.effectLabel(def.effect), level)

	local stat = popupStat :: TextLabel
	stat.Text = ("Now: %s"):format(TalentLogic.formatBonus(talentId, level))

	local nextLabel = popupNext :: TextLabel
	if TalentLogic.isMaxed(talentId, level) then
		nextLabel.Text = "Maxed out"
		nextLabel.TextColor3 = UIFactory.Theme.Accent
	else
		nextLabel.Text = ("Next level: %s"):format(TalentLogic.formatBonus(talentId, level + 1))
		nextLabel.TextColor3 = UIFactory.Theme.SubText
	end

	local desc = popupDesc :: TextLabel
	desc.Text = def.desc

	popupSelectedId = talentId
	return true
end

local function openPopup(talentId: string)
	if populatePopup(talentId) and popupOverlay then
		popupOverlay.Visible = true
	end
end

-- Keep an open popup in sync with the latest data (e.g. after a roll levels it up).
local function refreshPopup()
	if popupOverlay and popupOverlay.Visible and popupSelectedId then
		if not populatePopup(popupSelectedId) then
			closePopup()
		end
	end
end

local function buildPopup(parent: Instance)
	local Theme = UIFactory.Theme
	local overlay = UIFactory.frame({
		Name = "DetailPopup",
		BackgroundColor3 = Color3.fromRGB(0, 0, 0),
		BackgroundTransparency = 0.45,
		Size = UDim2.fromScale(1, 1),
		Visible = false,
		ZIndex = 20,
	})
	overlay.Parent = parent
	popupOverlay = overlay

	-- Click-off backdrop closes the popup.
	local backdrop = UIFactory.textButton({
		Name = "Backdrop",
		BackgroundTransparency = 1,
		Text = "",
		AutoButtonColor = false,
		Size = UDim2.fromScale(1, 1),
		ZIndex = 20,
	})
	backdrop.Parent = overlay
	backdrop.Activated:Connect(closePopup)

	local card = UIFactory.frame({
		Name = "Card",
		BackgroundColor3 = Theme.Panel,
		AnchorPoint = Vector2.new(0.5, 0.5),
		Position = UDim2.fromScale(0.5, 0.5),
		Size = UDim2.fromOffset(Responsive.scaled(340), 0),
		AutomaticSize = Enum.AutomaticSize.Y,
		ZIndex = 21,
	})
	card.Parent = overlay
	UIFactory.corner(12).Parent = card
	UIFactory.stroke({ Color = Theme.Stroke }).Parent = card
	UIFactory.padding(Responsive.scaled(18)).Parent = card

	local layout = UIFactory.listLayout({
		FillDirection = Enum.FillDirection.Vertical,
		Padding = UDim.new(0, Responsive.scaled(10)),
	})
	layout.Parent = card

	local title = UIFactory.textLabel({
		Name = "Title",
		LayoutOrder = 1,
		Size = UDim2.new(1, -Responsive.scaled(34), 0, Responsive.scaled(32)),
		Font = Theme.FontBold,
		TextSize = Responsive.scaled(26),
		TextXAlignment = Enum.TextXAlignment.Left,
		ZIndex = 21,
	})
	title.Parent = card
	popupTitle = title

	local subtitle = UIFactory.textLabel({
		Name = "Subtitle",
		LayoutOrder = 2,
		Size = UDim2.new(1, 0, 0, Responsive.scaled(20)),
		TextColor3 = Theme.SubText,
		TextSize = Responsive.scaled(16),
		TextXAlignment = Enum.TextXAlignment.Left,
		ZIndex = 21,
	})
	subtitle.Parent = card
	popupSubtitle = subtitle

	local divider = UIFactory.frame({
		Name = "Divider",
		LayoutOrder = 3,
		BackgroundColor3 = Theme.Stroke,
		Size = UDim2.new(1, 0, 0, 1),
		ZIndex = 21,
	})
	divider.Parent = card

	local stat = UIFactory.textLabel({
		Name = "Stat",
		LayoutOrder = 4,
		Size = UDim2.new(1, 0, 0, Responsive.scaled(26)),
		Font = Theme.FontBold,
		TextColor3 = Theme.Positive,
		TextSize = Responsive.scaled(22),
		TextXAlignment = Enum.TextXAlignment.Left,
		ZIndex = 21,
	})
	stat.Parent = card
	popupStat = stat

	local nextLabel = UIFactory.textLabel({
		Name = "Next",
		LayoutOrder = 5,
		Size = UDim2.new(1, 0, 0, Responsive.scaled(20)),
		TextColor3 = Theme.SubText,
		TextSize = Responsive.scaled(16),
		TextXAlignment = Enum.TextXAlignment.Left,
		ZIndex = 21,
	})
	nextLabel.Parent = card
	popupNext = nextLabel

	local desc = UIFactory.textLabel({
		Name = "Description",
		LayoutOrder = 6,
		Size = UDim2.new(1, 0, 0, 0),
		AutomaticSize = Enum.AutomaticSize.Y,
		TextColor3 = Theme.Text,
		TextSize = Responsive.scaled(16),
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Top,
		TextWrapped = true,
		ZIndex = 21,
	})
	desc.Parent = card
	popupDesc = desc

	-- Close (X) button, top-right of the card.
	local close = UIFactory.textButton({
		Name = "Close",
		AnchorPoint = Vector2.new(1, 0),
		Position = UDim2.new(1, -Responsive.scaled(8), 0, Responsive.scaled(8)),
		Size = UDim2.fromOffset(Responsive.scaled(30), Responsive.scaled(30)),
		BackgroundColor3 = Theme.PanelAlt,
		Text = "X",
		TextSize = Responsive.scaled(16),
		ZIndex = 22,
	})
	UIFactory.corner(8).Parent = close
	close.Parent = card
	close.Activated:Connect(closePopup)
end

-- ==================== tiles ====================

-- Build one square talent tile: a rarity-tinted button showing only the talent
-- name + level. Clicking it opens the detail popup. Sized by the grid layout.
local function buildTile(talentId: string, order: number): TileWidget
	local Theme = UIFactory.Theme
	local def = Talents.get(talentId)
	local rarityDef = def and Rarities.get(def.rarity)
	local rarityColor = rarityDef and rarityDef.color or Theme.SubText

	local button = UIFactory.create("TextButton", {
		Name = talentId,
		BackgroundColor3 = dim(rarityColor, 0.4),
		BorderSizePixel = 0,
		AutoButtonColor = true,
		Text = "",
		LayoutOrder = order,
	})
	UIFactory.corner(12).Parent = button
	local stroke = UIFactory.stroke({ Color = rarityColor, Thickness = 2 })
	stroke.Parent = button
	UIFactory.padding(Responsive.scaled(8)).Parent = button

	-- Talent name: big, fills the upper portion (TextScaled shrinks it on small
	-- tiles, the size constraint caps it on large ones).
	local nameLabel = UIFactory.textLabel({
		Name = "Name",
		AnchorPoint = Vector2.new(0.5, 0),
		Position = UDim2.fromScale(0.5, 0),
		Size = UDim2.new(1, 0, 0.66, 0),
		Font = Theme.FontBold,
		TextColor3 = Theme.Text,
		TextScaled = true,
		TextWrapped = true,
		Text = def and def.name or talentId,
	})
	nameLabel.Parent = button
	UIFactory.create("UITextSizeConstraint", { MaxTextSize = Responsive.scaled(36) }).Parent = nameLabel

	-- Level, centered along the bottom edge: plain white text, no background.
	local levelLabel = UIFactory.textLabel({
		Name = "Level",
		AnchorPoint = Vector2.new(0.5, 1),
		Position = UDim2.fromScale(0.5, 1),
		Size = UDim2.new(1, 0, 0.26, 0),
		Font = Theme.FontBold,
		TextColor3 = Color3.fromRGB(255, 255, 255),
		TextScaled = true,
		Text = "Level 0",
	})
	levelLabel.Parent = button
	UIFactory.create("UITextSizeConstraint", { MaxTextSize = Responsive.scaled(28) }).Parent = levelLabel

	button.Activated:Connect(function()
		openPopup(talentId)
	end)

	return {
		frame = button,
		stroke = stroke,
		name = nameLabel,
		level = levelLabel,
	}
end

-- ==================== render ====================

local function renderTiles(data: any)
	for _, talentId in Talents.Order do
		local widget = tileWidgets[talentId]
		if widget then
			local level = data.talents[talentId] or 0
			widget.level.Text = ("Level %d"):format(level)
		end
	end
end

-- Detect which talent leveled up since the last render and flash it (R-T2 feedback).
local function reportRoll(data: any)
	-- Skip the very first render (no prior snapshot to diff against).
	if next(lastLevels) ~= nil then
		for _, talentId in Talents.Order do
			local before = lastLevels[talentId] or 0
			local now = data.talents[talentId] or 0
			if now > before then
				local def = Talents.get(talentId)
				flashBanner(("Rolled %s  ->  Lv %d!"):format(def and def.name or talentId, now))
				local widget = tileWidgets[talentId]
				if widget then
					local rarityColor = (def and Rarities.get(def.rarity) and Rarities.get(def.rarity).color)
						or UIFactory.Theme.Accent
					-- Thicken the tile border briefly, then settle back to the
					-- normal rarity outline.
					widget.stroke.Color = UIFactory.Theme.Accent
					widget.stroke.Thickness = 4
					task.delay(3, function()
						if widget.stroke then
							widget.stroke.Color = rarityColor
							widget.stroke.Thickness = 2
						end
					end)
				end
				break
			end
		end
	end

	-- Refresh the snapshot for the next diff.
	for _, talentId in Talents.Order do
		lastLevels[talentId] = data.talents[talentId] or 0
	end
end

-- Fit the tile grid to the current width with SQUARE cells: a fixed 6 columns on
-- desktop/tablet and 4 on phone (smaller tiles), sizing each cell so width == height.
local function layoutGrid()
	local grid = gridFrame
	local layout = gridLayout
	if not grid or not layout then
		return
	end
	local width = grid.AbsoluteSize.X
	if width <= 0 then
		return
	end
	local buffer = Responsive.scaled(4)
	local pad = Responsive.scaled(8)
	local columns = Responsive.isCompact() and 4 or 6
	local avail = width - buffer * 2 - grid.ScrollBarThickness
	local cell = math.max(1, math.floor((avail - (columns - 1) * pad) / columns))
	layout.CellSize = UDim2.fromOffset(cell, cell) -- square
	layout.CellPadding = UDim2.fromOffset(pad, pad)
	if gridPadding then
		gridPadding.PaddingLeft = UDim.new(0, buffer)
		gridPadding.PaddingRight = UDim.new(0, buffer + grid.ScrollBarThickness)
		gridPadding.PaddingTop = UDim.new(0, buffer)
		gridPadding.PaddingBottom = UDim.new(0, buffer)
	end
end

local function render()
	local data = DataController.get()
	if not data then
		return
	end
	if coinLabel then
		coinLabel.Text = tostring(coinBalance(data))
	end
	renderTiles(data)
	updateRollButton(data)
	reportRoll(data)
	refreshPopup()
end

-- ==================== build ====================

function TalentsTab.init(injected: any)
	deps = injected
	UIFactory = deps.Util.UIFactory
	Responsive = deps.Util.Responsive
	AssetResolver = deps.Util.AssetResolver
	MenuShell = deps.UI.MenuShell
	DataController = deps.Controllers.DataController
	rollTalentRemote = deps.Remotes.getEvent("RollTalent")
end

function TalentsTab.start()
	local content = MenuShell.getContentFrame("Talents")
	if not content then
		warn("[TalentsTab] Talents content frame missing")
		return
	end

	-- Remove the shell placeholder now that we render the real view.
	local placeholder = content:FindFirstChild("Placeholder")
	if placeholder then
		placeholder:Destroy()
	end

	local Theme = UIFactory.Theme
	local root = UIFactory.frame({
		Name = "TalentsRoot",
		BackgroundTransparency = 1,
		Size = UDim2.fromScale(1, 1),
	})
	root.Parent = content
	rootFrame = root

	-- Layout bands: coin pill on top, tile grid in the middle, and the banner +
	-- Roll button pinned to the BOTTOM of the tab.
	local coinH = Responsive.scaled(42)
	local rollH = Responsive.scaled(56)
	local bannerH = Responsive.scaled(22)
	local gap = Responsive.scaled(10)
	local topH = coinH
	local bottomH = bannerH + gap + rollH

	-- Coin pill (icon + amount), top-right.
	local coinVisual = AssetResolver.resolve("currency/coins")
	local coinPill = UIFactory.frame({
		Name = "CoinPill",
		BackgroundColor3 = Theme.Panel,
		AnchorPoint = Vector2.new(1, 0),
		Position = UDim2.new(1, 0, 0, 0),
		Size = UDim2.fromOffset(Responsive.scaled(150), coinH),
	})
	coinPill.Parent = root
	headerFrame = coinPill
	UIFactory.corner(coinH // 2).Parent = coinPill
	UIFactory.stroke({ Color = Theme.Stroke }).Parent = coinPill

	local coinIcon = UIFactory.frame({
		Name = "Icon",
		BackgroundColor3 = coinVisual.color,
		AnchorPoint = Vector2.new(0, 0.5),
		Position = UDim2.new(0, Responsive.scaled(5), 0.5, 0),
		Size = UDim2.fromOffset(coinH - Responsive.scaled(10), coinH - Responsive.scaled(10)),
	})
	coinIcon.Parent = coinPill
	UIFactory.corner(999).Parent = coinIcon
	local coinIconGlyph = UIFactory.textLabel({
		Size = UDim2.fromScale(1, 1),
		Font = Theme.FontBold,
		TextColor3 = Theme.AccentText,
		TextSize = Responsive.scaled(16),
		Text = coinVisual.label,
	})
	coinIconGlyph.Parent = coinIcon

	local coinAmount = UIFactory.textLabel({
		Name = "Amount",
		AnchorPoint = Vector2.new(1, 0.5),
		Position = UDim2.new(1, -Responsive.scaled(12), 0.5, 0),
		Size = UDim2.new(1, -(coinH + Responsive.scaled(12)), 1, 0),
		Font = Theme.FontBold,
		TextColor3 = Theme.Text,
		TextSize = Responsive.scaled(20),
		TextXAlignment = Enum.TextXAlignment.Right,
		Text = "0",
	})
	coinAmount.Parent = coinPill
	coinLabel = coinAmount

	-- Roll button, pinned to the very bottom, full width.
	local roll = UIFactory.textButton({
		Name = "RollButton",
		AnchorPoint = Vector2.new(0, 1),
		Position = UDim2.fromScale(0, 1),
		Size = UDim2.new(1, 0, 0, rollH),
		BackgroundColor3 = Theme.Accent,
		TextSize = Responsive.scaled(24),
		Text = ("Roll Talent  (%d C)"):format(ROLL_COST),
	})
	UIFactory.corner(10).Parent = roll
	roll.Parent = root
	rollButton = roll
	roll.Activated:Connect(function()
		local data = DataController.get()
		if data and canRoll(data) and rollTalentRemote then
			rollTalentRemote:FireServer()
		end
	end)

	-- Banner: last-roll result or the idle hint, just above the Roll button.
	local banner = UIFactory.textLabel({
		Name = "Banner",
		AnchorPoint = Vector2.new(0, 1),
		Position = UDim2.new(0, 0, 1, -(rollH + gap)),
		Size = UDim2.new(1, 0, 0, bannerH),
		TextColor3 = Theme.SubText,
		TextSize = Responsive.scaled(15),
		TextXAlignment = Enum.TextXAlignment.Center,
		Text = ("Each roll costs %d coins and levels a random talent."):format(ROLL_COST),
	})
	banner.Parent = root
	bannerLabel = banner

	-- Talent tile grid, filling the space between the coin pill and the footer.
	local grid = UIFactory.scrolling({
		Name = "TalentGrid",
		Position = UDim2.new(0, 0, 0, topH + gap),
		Size = UDim2.new(1, 0, 1, -(topH + gap + bottomH + gap)),
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		ScrollingDirection = Enum.ScrollingDirection.Y,
	})
	grid.Parent = root
	gridFrame = grid
	local gLayout = UIFactory.gridLayout({
		HorizontalAlignment = Enum.HorizontalAlignment.Left,
		SortOrder = Enum.SortOrder.LayoutOrder,
	})
	gLayout.Parent = grid
	gridLayout = gLayout
	local gPad = UIFactory.create("UIPadding", {
		PaddingRight = UDim.new(0, grid.ScrollBarThickness + Responsive.scaled(4)),
	})
	gPad.Parent = grid
	gridPadding = gPad

	-- Build tiles in rarity-DESCENDING order (highest rarity first), tie-breaking
	-- by id so the arrangement is stable.
	local ordered = table.clone(Talents.Order)
	table.sort(ordered, function(a, b)
		local da, db = Talents.get(a), Talents.get(b)
		local ra = da and Rarities.orderOf(da.rarity) or 0
		local rb = db and Rarities.orderOf(db.rarity) or 0
		if ra ~= rb then
			return ra > rb
		end
		return a < b
	end)

	table.clear(tileWidgets)
	for order, talentId in ordered do
		local widget = buildTile(talentId, order)
		widget.frame.Parent = grid
		tileWidgets[talentId] = widget
	end

	-- Detail popup lives above everything else in the tab.
	buildPopup(root)

	-- Re-flow the grid whenever its width or the device scale changes.
	grid:GetPropertyChangedSignal("AbsoluteSize"):Connect(layoutGrid)
	Responsive.onChanged(layoutGrid)
	layoutGrid()

	-- Re-render on any authoritative profile change (roll result / coin change).
	DataController.onChanged(function()
		render()
	end)
	if DataController.get() then
		render()
	end
end

return TalentsTab
]]></ProtectedString>
							<string name="ScriptGuid">{D48C6CE1-C42A-4E3A-BD4F-B48403866CE5}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">TalentsTab</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000acb</UniqueId>
						</Properties>
					</Item>
					<Item class="ModuleScript" referent="RBXC8D24F7039DA4039A70048BD5C9097CA">
						<Properties>
							<Content name="LinkedSource"><null></null></Content>
							<ProtectedString name="Source"><![CDATA[--!strict
-- CombatUI: dice, lock/reroll, enemy select, HP bars, traits. Stub for Step 1.

local CombatUI = {}

local deps: any = nil

function CombatUI.init(injected: any)
	deps = injected
end

function CombatUI.start() end

return CombatUI
]]></ProtectedString>
							<string name="ScriptGuid">{95A2D7F6-B309-428E-869F-F49F111171C6}</string>
							<BinaryString name="AttributesSerialize"></BinaryString>
							<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
							<bool name="DefinesCapabilities">false</bool>
							<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
							<string name="Name">CombatUI</string>
							<int64 name="SourceAssetId">-1</int64>
							<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
							<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000acd</UniqueId>
						</Properties>
					</Item>
				</Item>
			</Item>
		</Item>
		<Item class="StarterCharacterScripts" referent="RBX2EE3CAD2F25747AE991E2694C498170F">
			<Properties>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">StarterCharacterScripts</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003bc</UniqueId>
			</Properties>
		</Item>
	</Item>
	<Item class="StarterPack" referent="RBXE7368AAA1BA14B27824C166AC00D5A6E">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">StarterPack</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000360</UniqueId>
		</Properties>
	</Item>
	<Item class="StarterGui" referent="RBXA7B70128402E4A438EA116F0DFFC988F">
		<Properties>
			<token name="ClipsDescendantsSupportsRotation">0</token>
			<bool name="ResetPlayerGuiOnSpawn">true</bool>
			<token name="RtlTextSupport">0</token>
			<token name="ScreenOrientation">2</token>
			<bool name="ShowDevelopmentGui">true</bool>
			<Ref name="StudioDefaultStyleSheet">null</Ref>
			<Ref name="StudioInsertWidgetLayerCollectorAutoLinkStyleSheet">null</Ref>
			<token name="VirtualCursorMode">0</token>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">StarterGui</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000361</UniqueId>
		</Properties>
	</Item>
	<Item class="LocalizationService" referent="RBXF0FAC5889AE84DFFB92D549B214FBFFC">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">LocalizationService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000363</UniqueId>
		</Properties>
	</Item>
	<Item class="TeleportService" referent="RBX962F4AD438834140842266468A652A62">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">Teleport Service</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000367</UniqueId>
		</Properties>
	</Item>
	<Item class="PhysicsService" referent="RBX395DA97F4D384D7CABC10F06BD07E0AB">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">PhysicsService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000369</UniqueId>
		</Properties>
	</Item>
	<Item class="InsertService" referent="RBX0D82D07313214D2D97E3C88920DB7BF0">
		<Properties>
			<bool name="AllowInsertFreeModels">false</bool>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">InsertService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000036c</UniqueId>
		</Properties>
		<Item class="StringValue" referent="RBXCC40F3DD95B040C2A48D847932D0DA01">
			<Properties>
				<string name="Value">{617C86A8-9170-445B-85F5-ECDACC8DA235}</string>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">InsertionHash</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000829</UniqueId>
			</Properties>
		</Item>
	</Item>
	<Item class="GamePassService" referent="RBX4B15FD04457F4BF8A1849946230FE3F6">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">GamePassService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000036d</UniqueId>
		</Properties>
	</Item>
	<Item class="Debris" referent="RBXEFE3F8386C5B40539B88EC56FA5746BC">
		<Properties>
			<int name="MaxItems">1000</int>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">Debris</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000036e</UniqueId>
		</Properties>
	</Item>
	<Item class="CookiesService" referent="RBX847CA897A32E416BA8CE5A2DAA19F98E">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">CookiesService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000036f</UniqueId>
		</Properties>
	</Item>
	<Item class="Selection" referent="RBX6D10C9F0BB704C459C34F9A68681BBB8">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">Selection</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000370</UniqueId>
		</Properties>
	</Item>
	<Item class="VRService" referent="RBXB667C7AFB35A4EE6AB32D5379F648A08">
		<Properties>
			<token name="AutomaticScaling">0</token>
			<bool name="AvatarGestures">false</bool>
			<token name="ControllerModels">1</token>
			<bool name="FadeOutViewOnCollision">true</bool>
			<token name="LaserPointer">1</token>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">VRService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000374</UniqueId>
		</Properties>
	</Item>
	<Item class="ContextActionService" referent="RBX8991336B21134A7EA74925FC59ABFAC3">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">ContextActionService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000375</UniqueId>
		</Properties>
	</Item>
	<Item class="ScriptService" referent="RBXC1C3CCB297CF46509B0843A41BD2B48E">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">ScriptService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000376</UniqueId>
		</Properties>
	</Item>
	<Item class="AssetService" referent="RBX4CE798D93A0F42338C9A01B3095C2084">
		<Properties>
			<bool name="AllowInsertFreeAssets">false</bool>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">AssetService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000377</UniqueId>
		</Properties>
	</Item>
	<Item class="TouchInputService" referent="RBX5420BE6211794A6E82FF1D08D2DFF9D2">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">TouchInputService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000379</UniqueId>
		</Properties>
	</Item>
	<Item class="AvatarSettings" referent="RBXF751730F203E4585A9D7A47450B883B2">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">AvatarSettings</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000037f</UniqueId>
		</Properties>
	</Item>
	<Item class="Packages" referent="RBX317BBB028D3140619048CD1E6E1EE2FB">
		<Properties>
			<bool name="IsDehydrated">false</bool>
			<int name="ShellPackagesCount">0</int>
			<int name="SkippedInstancesCount">0</int>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">Packages</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000383</UniqueId>
		</Properties>
	</Item>
	<Item class="GuidRegistryService" referent="RBXF2C4452DD04744D8860B56D74D646DCE">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">GuidRegistryService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000384</UniqueId>
		</Properties>
	</Item>
	<Item class="ServiceVisibilityService" referent="RBX02A479A6127A4C37B9EBF4AFFE0132E1">
		<Properties>
			<BinaryString name="HiddenServices">AAAAAA==</BinaryString>
			<BinaryString name="VisibleServices">AAAAAA==</BinaryString>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">ServiceVisibilityService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000393</UniqueId>
		</Properties>
	</Item>
	<Item class="LuaWebService" referent="RBX14B66D2CA0294A75BE41B21FF3126D43">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">LuaWebService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000038b</UniqueId>
		</Properties>
	</Item>
	<Item class="ProcessInstancePhysicsService" referent="RBX35D5AB3D173F4D33A3CE543932D7A4B1">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">ProcessInstancePhysicsService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000038c</UniqueId>
		</Properties>
	</Item>
	<Item class="ReplicatedStorage" referent="1">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">ReplicatedStorage</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000038d</UniqueId>
		</Properties>
		<Item class="Folder" referent="2">
			<Properties>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">Shared</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003af</UniqueId>
			</Properties>
			<Item class="ModuleScript" referent="3">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[return function()
	print("Hello, world!")
end
]]></ProtectedString>
					<string name="ScriptGuid">{D7DA6276-C80E-4A5D-9CE6-BC5D27FB1781}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
					<string name="Name">Hello</string>
					<int64 name="SourceAssetId">-1</int64>
					<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
					<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003b0</UniqueId>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBX9AC3691F024C4486B469965DFEE6B894">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[--!strict
-- Loader: small dependency-injection helper.
-- Requires every ModuleScript child of a folder and returns a name -> module map,
-- then can init/start those modules in a defined dependency order.

local Loader = {}

export type LifecycleModule = {
	init: ((deps: any) -> ())?,
	start: (() -> ())?,
	[any]: any,
}

-- Require all ModuleScript children of a container, returning a name -> module map.
function Loader.requireChildren(container: Instance): { [string]: any }
	local modules: { [string]: any } = {}
	for _, child in container:GetChildren() do
		if child:IsA("ModuleScript") then
			modules[child.Name] = require(child :: ModuleScript)
			print(("[Loader] loaded %s"):format(child.Name))
		end
	end
	return modules
end

-- Produce a name list ordered by `order` first (only names that exist),
-- then any remaining modules in arbitrary order.
local function orderedNames(modules: { [string]: any }, order: { string }?): { string }
	local names: { string } = {}
	local seen: { [string]: boolean } = {}

	if order then
		for _, name in order do
			if modules[name] ~= nil then
				table.insert(names, name)
				seen[name] = true
			end
		end
	end

	for name in modules do
		if not seen[name] then
			table.insert(names, name)
		end
	end

	return names
end

-- Call module.init(deps) on each module that defines it, in dependency order.
-- Each module is isolated in a pcall so one failure does not abort the rest;
-- the offending module and phase are logged for diagnosis.
function Loader.initModules(modules: { [string]: any }, deps: any, order: { string }?)
	for _, name in orderedNames(modules, order) do
		local module = modules[name]
		if type(module) == "table" and type(module.init) == "function" then
			local ok, err = pcall(module.init, deps)
			if ok then
				print(("[Loader] init %s"):format(name))
			else
				warn(("[Loader] init FAILED for %s: %s"):format(name, tostring(err)))
			end
		end
	end
end

-- Call module.start() on each module that defines it, in dependency order.
-- Each module is isolated in a pcall so one failure does not abort the rest.
function Loader.startModules(modules: { [string]: any }, order: { string }?)
	for _, name in orderedNames(modules, order) do
		local module = modules[name]
		if type(module) == "table" and type(module.start) == "function" then
			local ok, err = pcall(module.start)
			if ok then
				print(("[Loader] start %s"):format(name))
			else
				warn(("[Loader] start FAILED for %s: %s"):format(name, tostring(err)))
			end
		end
	end
end

return Loader
]]></ProtectedString>
					<string name="ScriptGuid">{210FAABD-7866-4100-91C2-BC941224B63F}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
					<string name="Name">Loader</string>
					<int64 name="SourceAssetId">-1</int64>
					<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
					<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000a9c</UniqueId>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBX106320A118A3478FA473F88783E2BCA7">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[--!strict
-- Remotes: central registry of RemoteEvent/RemoteFunction names so client and
-- server never mistype a networking string. The server creates the instances
-- via Remotes.init(); both sides fetch by name via getEvent/getFunction.

local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Remotes = {}

local CONTAINER_NAME = "Remotes"
local WAIT_TIMEOUT = 10

-- Request (client -> server) and Replication (server -> client) events.
local EVENT_NAMES: { string } = {
	-- Request (client -> server)
	"StartBattle",
	"RollDice",
	"ToggleLockDie",
	"Reroll",
	"AttackEnemy",
	"EquipGear",
	"UnequipGear",
	"LevelSlot",
	"MergeGear",
	"AutoMergeGear",
	"RollTalent",
	"OpenChest",
	"ChooseTrait",
	-- Replication (server -> client)
	"ProfileUpdated",
	"CombatStateUpdated",
	"CombatEnded",
	"TraitOffer",
	"Toast",
}

-- RemoteFunctions (request/response). None required yet; reserved for future use.
local FUNCTION_NAMES: { string } = {}

local container: Folder? = nil

local function getContainer(): Folder
	if container then
		return container
	end

	if RunService:IsServer() then
		local existing = ReplicatedStorage:FindFirstChild(CONTAINER_NAME)
		if existing and existing:IsA("Folder") then
			container = existing
		else
			local folder = Instance.new("Folder")
			folder.Name = CONTAINER_NAME
			folder.Parent = ReplicatedStorage
			container = folder
		end
	else
		local found = ReplicatedStorage:WaitForChild(CONTAINER_NAME, WAIT_TIMEOUT)
		assert(
			found and found:IsA("Folder"),
			("Remotes container %q did not replicate within %d seconds; did the server call Remotes.init()?"):format(
				CONTAINER_NAME,
				WAIT_TIMEOUT
			)
		)
		container = found :: Folder
	end

	return container :: Folder
end

-- Server-only: create every declared RemoteEvent/RemoteFunction instance.
function Remotes.init()
	assert(RunService:IsServer(), "Remotes.init() must be called on the server")
	local folder = getContainer()

	for _, name in EVENT_NAMES do
		if not folder:FindFirstChild(name) then
			local event = Instance.new("RemoteEvent")
			event.Name = name
			event.Parent = folder
		end
	end

	for _, name in FUNCTION_NAMES do
		if not folder:FindFirstChild(name) then
			local fn = Instance.new("RemoteFunction")
			fn.Name = name
			fn.Parent = folder
		end
	end
end

function Remotes.getEvent(name: string): RemoteEvent
	local remote = getContainer():WaitForChild(name, WAIT_TIMEOUT)
	assert(remote and remote:IsA("RemoteEvent"), ("RemoteEvent %q not found"):format(name))
	return remote :: RemoteEvent
end

function Remotes.getFunction(name: string): RemoteFunction
	local remote = getContainer():WaitForChild(name, WAIT_TIMEOUT)
	assert(remote and remote:IsA("RemoteFunction"), ("RemoteFunction %q not found"):format(name))
	return remote :: RemoteFunction
end

return Remotes
]]></ProtectedString>
					<string name="ScriptGuid">{CB907C13-247A-4C14-9FAD-8A68C19192E9}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
					<string name="Name">Remotes</string>
					<int64 name="SourceAssetId">-1</int64>
					<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
					<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000a9f</UniqueId>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBXE6172D44B3CA497EBE357C6183630148">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[--!strict
-- Types: shared Luau type aliases used across client, server, and shared logic.
-- This module exports types only; it returns an empty table at runtime.

-- Persisted player profile (see ARCHITECTURE.md 5.1).
export type GlobalData = {
	level: number,
	xp: number,
}

export type Currencies = {
	coins: number,
	gems: number,
	materials: number,
	keys: number,
}

export type GearInstance = {
	id: string,
	rarity: string,
}

export type Progress = {
	currentChapter: number,
	chaptersCompleted: number,
}

export type PlayerData = {
	version: number,
	global: GlobalData,
	currencies: Currencies,
	startingDiceUpgrade: number,
	gear: { [string]: GearInstance },
	equipped: { [string]: string? },
	slotLevels: { [string]: number },
	talents: { [string]: number },
	progress: Progress,
	stats: { [string]: number },
}

-- Transient per-player combat state (see ARCHITECTURE.md 5.2).
export type CombatPhase = "PlayerTurn" | "EnemyTurn" | "TraitPick" | "Won" | "Lost"

export type Die = {
	value: number,
	locked: boolean,
}

export type CombatEnemy = {
	uid: string,
	archetype: string,
	hp: number,
	maxHp: number,
	shield: number,
}

export type CombatPlayer = {
	hp: number,
	maxHp: number,
	shield: number,
}

export type CombatState = {
	chapterId: number,
	stageIndex: number,
	phase: CombatPhase,
	enemies: { CombatEnemy },
	player: CombatPlayer,
	perChapter: { level: number, xp: number },
	dice: { Die },
	unlockedDice: number,
	rerollsLeft: number,
	activeTraits: { string },
}

return {}
]]></ProtectedString>
					<string name="ScriptGuid">{3E993215-4BCD-4270-B7DC-4A0F93428F20}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
					<string name="Name">Types</string>
					<int64 name="SourceAssetId">-1</int64>
					<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
					<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000aa1</UniqueId>
				</Properties>
			</Item>
			<Item class="Folder" referent="RBX7EDF4E932FBE4B60BB4E5FC7A5189E23">
				<Properties>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
					<string name="Name">Config</string>
					<int64 name="SourceAssetId">-1</int64>
					<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
					<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000aa3</UniqueId>
				</Properties>
				<Item class="ModuleScript" referent="RBX2BEBCFE6F7D94986983B8672A0CF0DE7">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- Rarities: rarity order + per-rarity stat multiplier, roll weight, and color.
-- Pure data (see ARCHITECTURE.md 5, R-E6). Rarity scales the magnitude of a
-- slot's buff and biases weighted rolls (rarer = less likely).

export type RarityDef = {
	id: string,
	name: string,
	order: number, -- 1 = lowest
	multiplier: number, -- scales gear base stat magnitude
	weight: number, -- weighted-roll likelihood (higher = more common)
	color: Color3, -- placeholder tint for AssetResolver
}

local Rarities = {}

-- Low -> high (R-E6).
Rarities.Order = { "Common", "Uncommon", "Rare", "Epic", "Legendary" }

Rarities.Defs = {
	Common = {
		id = "Common",
		name = "Common",
		order = 1,
		multiplier = 1.0,
		weight = 100,
		color = Color3.fromRGB(170, 170, 170),
	},
	Uncommon = {
		id = "Uncommon",
		name = "Uncommon",
		order = 2,
		multiplier = 1.5,
		weight = 50,
		color = Color3.fromRGB(80, 200, 90),
	},
	Rare = {
		id = "Rare",
		name = "Rare",
		order = 3,
		multiplier = 2.25,
		weight = 20,
		color = Color3.fromRGB(60, 140, 240),
	},
	Epic = {
		id = "Epic",
		name = "Epic",
		order = 4,
		multiplier = 3.5,
		weight = 6,
		color = Color3.fromRGB(170, 80, 230),
	},
	Legendary = {
		id = "Legendary",
		name = "Legendary",
		order = 5,
		multiplier = 5.0,
		weight = 2,
		color = Color3.fromRGB(245, 175, 45),
	},
} :: { [string]: RarityDef }

-- Lookup helper (pure).
function Rarities.get(id: string): RarityDef?
	return Rarities.Defs[id]
end

-- Numeric sort order for a rarity id (0 if unknown).
function Rarities.orderOf(id: string): number
	local def = Rarities.Defs[id]
	return def and def.order or 0
end

-- The next rarity up from `id` (e.g. Common -> Uncommon), or nil if `id` is the
-- highest rarity or unknown. Used by gear merging (R-E9).
function Rarities.next(id: string): string?
	local def = Rarities.Defs[id]
	if not def then
		return nil
	end
	return Rarities.Order[def.order + 1]
end

return Rarities
]]></ProtectedString>
						<string name="ScriptGuid">{23F16F8A-F2F5-42E3-879B-09E87BFB30BC}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Rarities</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000f27</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX33F2FF8CEF7D4E3997E51B5DAE10789C">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- Slots: the 6 equipment slots, the stat each provides, and UI placement.
-- Pure data (R-E2, R-E7). Layout: Left column top->bottom Helmet, Weapon, Belt;
-- Right column top->bottom Charm, Chestplate, Boots.

-- Canonical stat identifiers used across gear, talents, traits, and combat.
export type StatType =
	"hp"
	| "attack"
	| "critChance"
	| "dodgeChance"
	| "damageReduction"
	| "healAmount"

export type SlotDef = {
	id: string,
	name: string,
	stat: StatType,
	side: "Left" | "Right",
	row: number, -- 1 = top
}

local Slots = {}

-- All stat types a slot/gear/talent may modify (R-E7).
Slots.StatTypes = {
	"hp",
	"attack",
	"critChance",
	"dodgeChance",
	"damageReduction",
	"healAmount",
}

Slots.Order = { "Helmet", "Weapon", "Belt", "Charm", "Chestplate", "Boots" }

Slots.Defs = {
	Helmet = { id = "Helmet", name = "Helmet", stat = "hp", side = "Left", row = 1 },
	Weapon = { id = "Weapon", name = "Weapon", stat = "attack", side = "Left", row = 2 },
	Belt = { id = "Belt", name = "Belt", stat = "healAmount", side = "Left", row = 3 },
	Charm = { id = "Charm", name = "Charm", stat = "critChance", side = "Right", row = 1 },
	Chestplate = { id = "Chestplate", name = "Chestplate", stat = "damageReduction", side = "Right", row = 2 },
	Boots = { id = "Boots", name = "Boots", stat = "dodgeChance", side = "Right", row = 3 },
} :: { [string]: SlotDef }

function Slots.get(id: string): SlotDef?
	return Slots.Defs[id]
end

return Slots
]]></ProtectedString>
						<string name="ScriptGuid">{AE3A61D0-1FA0-4C3A-8108-9CA74426B45A}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Slots</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000f29</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX168B39D4E6B54B7A97E948F601B05E1A">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- Currencies: currency ids + display metadata (R-Y1).
-- CurrencyService mutates balances generically by reading these ids.

export type CurrencyDef = {
	id: string,
	name: string,
	abbrev: string,
	premium: boolean, -- purchasable with Robux
	icon: string, -- logical asset id for AssetResolver
	description: string,
}

local Currencies = {}

Currencies.Order = { "coins", "gems", "materials", "keys" }

Currencies.Defs = {
	coins = {
		id = "coins",
		name = "Coins",
		abbrev = "C",
		premium = false,
		icon = "currency/coins",
		description = "Spent on talent rolls.",
	},
	gems = {
		id = "gems",
		name = "Gems",
		abbrev = "G",
		premium = true,
		icon = "currency/gems",
		description = "Premium currency for the shop and chests.",
	},
	materials = {
		id = "materials",
		name = "Slot Materials",
		abbrev = "M",
		premium = false,
		icon = "currency/materials",
		description = "Spent to level up equipment slots.",
	},
	keys = {
		id = "keys",
		name = "Keys",
		abbrev = "K",
		premium = false,
		icon = "currency/keys",
		description = "One key opens one chest roll.",
	},
} :: { [string]: CurrencyDef }

function Currencies.get(id: string): CurrencyDef?
	return Currencies.Defs[id]
end

return Currencies
]]></ProtectedString>
						<string name="ScriptGuid">{2B1CB8D8-0C5F-4802-B0B4-A26964A97EED}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Currencies</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000f2b</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX8FC27D5A40E6401CBF4F2C730E54FE2A">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- Balance: tunable constants for combat, dice, progression, and economy.
-- Every number here is a PLACEHOLDER meant to be tuned later; systems read
-- from this module so balancing never requires touching logic code.

local Balance = {}

-- === Dice (R-C8, R-C11) ===
Balance.Dice = {
	faces = 6, -- die values are 1..faces
	startingDice = 1, -- dice at chapter entry before upgrades
	maxDice = 5, -- hard cap across all sources
	baseRerolls = 3, -- rerolls granted at the start of each turn
}

-- Hand multipliers (R-C12). Names map to DiceHands detection results.
Balance.HandMultipliers = {
	highCard = 1.0,
	onePair = 1.5,
	twoPair = 2.0,
	threeOfAKind = 2.5,
	straight = 3.0,
	fullHouse = 4.0,
	fourOfAKind = 5.0,
	fiveOfAKind = 8.0,
}

-- === Player base stats (R-C14..R-C18) ===
Balance.Player = {
	baseHp = 100,
	baseAttackFlat = 0, -- flat added to dice total before multipliers
	baseCritChance = 0.05, -- 0..1
	baseCritMultiplier = 2.0, -- crit deals x2 (R-C14)
	baseDodgeChance = 0.0, -- 0..1 (R-C16)
	baseDamageReduction = 0.0, -- 0..1 (R-C15)
	baseHealAmount = 0,
	baseLifesteal = 0.0, -- 0..1 of damage dealt
	-- Safety clamps so stacking never breaks resolution.
	maxCritChance = 1.0,
	maxDodgeChance = 0.9,
	maxDamageReduction = 0.9,
}

-- === Gear (magnitude of a Common-rarity piece per stat, before rarity/slot scaling) ===
Balance.GearBaseStat = {
	hp = 20,
	attack = 5,
	critChance = 0.03,
	dodgeChance = 0.03,
	damageReduction = 0.02,
	healAmount = 8,
}

-- === Slot leveling (R-E8) ===
Balance.SlotLevels = {
	maxLevel = 20,
	-- Buff multiplier applied to equipped gear = 1 + (level-1) * bonusPerLevel.
	bonusPerLevel = 0.10,
	-- Material cost to go from level L to L+1 = baseCost + (L-1) * costPerLevel.
	baseCost = 5,
	costPerLevel = 5,
}

-- === Per-chapter (transient) leveling & dice unlock (R-C21) ===
Balance.PerChapter = {
	xpPerEnemyKill = 10,
	xpPerBossKill = 40,
	-- XP needed to reach level L+1 = baseXp + (L-1) * xpPerLevel.
	baseXp = 20,
	xpPerLevel = 15,
	-- Each per-chapter level-up unlocks one more die (up to Balance.Dice.maxDice).
	diceUnlockPerLevel = 1,
}

-- === Global (persistent) account leveling (R-C22) ===
Balance.Global = {
	xpPerChapterComplete = 100,
	baseXp = 100,
	xpPerLevel = 50,
	-- Rewards granted per global level-up.
	statPointsPerLevel = 1,
	talentRollsPerLevel = 1,
	-- Starting-dice upgrade every N global levels (permanent, still capped at 5).
	startingDiceEveryLevels = 5,
}

-- === Enemy / boss scaling ===
Balance.Enemy = {
	-- Boss stages multiply the base archetype stats (R-C2).
	bossHpMultiplier = 4.0,
	bossAttackMultiplier = 2.0,
	-- Per-chapter difficulty ramp applied to enemy stats (chapterIndex based).
	hpPerChapter = 0.25, -- +25% hp per chapter beyond the first
	attackPerChapter = 0.15,
}

-- === Economy (R-Y*) ===
Balance.Economy = {
	talentRollCost = 50, -- coins per talent roll (R-T2)
	chestKeyCost = 1, -- keys per chest roll (R-Y2)
}

return Balance
]]></ProtectedString>
						<string name="ScriptGuid">{329DB42F-F3CC-4EF0-8729-50245829F73A}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Balance</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000f2d</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX01567E3D45054CFE852E39ECC35A60DE">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- Gear: gear definitions (id, slot, rarity, base stat) (R-E1..R-E7).
-- baseStat is the Common-rarity magnitude for the slot's stat; runtime scales it
-- by the piece's rarity multiplier (Rarities) and the slot's level (Balance).
-- If baseStat is nil, systems fall back to Balance.GearBaseStat[slot.stat].

export type GearDef = {
	id: string,
	name: string,
	slot: string, -- Slots.Defs key
	rarity: string, -- Rarities.Defs key
	baseStat: number?, -- override for the slot's stat magnitude
	icon: string, -- logical asset id for AssetResolver
}

local Gear = {}

Gear.Defs = {
	-- Helmet (hp)
	IronHelm = { id = "IronHelm", name = "Iron Helm", slot = "Helmet", rarity = "Common", baseStat = 20, icon = "gear/helmet" },
	SteelHelm = { id = "SteelHelm", name = "Steel Helm", slot = "Helmet", rarity = "Uncommon", baseStat = 20, icon = "gear/helmet" },
	RunicHelm = { id = "RunicHelm", name = "Runic Helm", slot = "Helmet", rarity = "Rare", baseStat = 20, icon = "gear/helmet" },

	-- Weapon (attack)
	WoodSword = { id = "WoodSword", name = "Wooden Sword", slot = "Weapon", rarity = "Common", baseStat = 5, icon = "gear/weapon" },
	IronSword = { id = "IronSword", name = "Iron Sword", slot = "Weapon", rarity = "Uncommon", baseStat = 5, icon = "gear/weapon" },
	FlameBlade = { id = "FlameBlade", name = "Flame Blade", slot = "Weapon", rarity = "Rare", baseStat = 5, icon = "gear/weapon" },
	DragonFang = { id = "DragonFang", name = "Dragon Fang", slot = "Weapon", rarity = "Epic", baseStat = 5, icon = "gear/weapon" },

	-- Belt (healAmount)
	LeatherBelt = { id = "LeatherBelt", name = "Leather Belt", slot = "Belt", rarity = "Common", baseStat = 8, icon = "gear/belt" },
	HealerSash = { id = "HealerSash", name = "Healer's Sash", slot = "Belt", rarity = "Rare", baseStat = 8, icon = "gear/belt" },

	-- Charm (critChance)
	LuckyCharm = { id = "LuckyCharm", name = "Lucky Charm", slot = "Charm", rarity = "Common", baseStat = 0.03, icon = "gear/charm" },
	CritGem = { id = "CritGem", name = "Crit Gem", slot = "Charm", rarity = "Epic", baseStat = 0.03, icon = "gear/charm" },

	-- Chestplate (damageReduction)
	LeatherVest = { id = "LeatherVest", name = "Leather Vest", slot = "Chestplate", rarity = "Common", baseStat = 0.02, icon = "gear/chestplate" },
	PlateArmor = { id = "PlateArmor", name = "Plate Armor", slot = "Chestplate", rarity = "Rare", baseStat = 0.02, icon = "gear/chestplate" },
	Aegis = { id = "Aegis", name = "Aegis", slot = "Chestplate", rarity = "Legendary", baseStat = 0.02, icon = "gear/chestplate" },

	-- Boots (dodgeChance)
	Sandals = { id = "Sandals", name = "Sandals", slot = "Boots", rarity = "Common", baseStat = 0.03, icon = "gear/boots" },
	SwiftBoots = { id = "SwiftBoots", name = "Swift Boots", slot = "Boots", rarity = "Uncommon", baseStat = 0.03, icon = "gear/boots" },
	PhantomBoots = { id = "PhantomBoots", name = "Phantom Boots", slot = "Boots", rarity = "Epic", baseStat = 0.03, icon = "gear/boots" },
} :: { [string]: GearDef }

function Gear.get(id: string): GearDef?
	return Gear.Defs[id]
end

-- All gear ids for a given slot (unordered).
function Gear.idsForSlot(slotId: string): { string }
	local ids: { string } = {}
	for id, def in Gear.Defs do
		if def.slot == slotId then
			table.insert(ids, id)
		end
	end
	return ids
end

return Gear
]]></ProtectedString>
						<string name="ScriptGuid">{348C518B-08B9-45F5-AE93-3AC3251ABE2F}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Gear</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000f2f</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBXC46680DFDDE54E42A1AAD02B1A7E25BC">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- Talents: passive, permanent talent definitions (R-T1..R-T4).
-- All talents exist from the start at level 0; a rarity-weighted roll (R-T2)
-- increases one talent's level by 1. Effect value scales as perLevel * level.
-- Levels are uncapped except extraDice, which caps at maxLevel = 4 (the 5th die).

-- Effect categories (R-T4).
export type TalentEffect =
	"flatAttack"
	| "pctAttack"
	| "critChance"
	| "extraReroll"
	| "extraDice"
	| "hp"
	| "lifesteal"

export type TalentDef = {
	id: string,
	name: string,
	rarity: string, -- Rarities.Defs key (biases roll weight)
	effect: TalentEffect,
	perLevel: number, -- value added per level
	maxLevel: number?, -- nil = uncapped
	icon: string,
	desc: string, -- plain-language explanation shown in the detail popup
}

local Talents = {}

Talents.Order = {
	"Sharpen",
	"Vitality",
	"Berserk",
	"Precision",
	"LuckyHand",
	"Vampiric",
	"Multiroll",
}

Talents.Defs = {
	Sharpen = { id = "Sharpen", name = "Sharpen", rarity = "Common", effect = "flatAttack", perLevel = 2, icon = "talent/flatAttack", desc = "Permanently adds flat attack power to every hit." },
	Vitality = { id = "Vitality", name = "Vitality", rarity = "Common", effect = "hp", perLevel = 10, icon = "talent/hp", desc = "Raises your maximum health so you survive longer." },
	Berserk = { id = "Berserk", name = "Berserk", rarity = "Uncommon", effect = "pctAttack", perLevel = 0.05, icon = "talent/pctAttack", desc = "Increases your total attack damage by a percentage." },
	Precision = { id = "Precision", name = "Precision", rarity = "Rare", effect = "critChance", perLevel = 0.02, icon = "talent/critChance", desc = "Improves your chance to land a critical hit for bonus damage." },
	LuckyHand = { id = "LuckyHand", name = "Lucky Hand", rarity = "Legendary", effect = "extraReroll", perLevel = 1, icon = "talent/extraReroll", desc = "Grants an extra dice reroll each battle to chase better hands." },
	Vampiric = { id = "Vampiric", name = "Vampiric", rarity = "Rare", effect = "lifesteal", perLevel = 0.03, icon = "talent/lifesteal", desc = "Heals you for a portion of the damage you deal." },
	Multiroll = { id = "Multiroll", name = "Multiroll", rarity = "Legendary", effect = "extraDice", perLevel = 1, maxLevel = 4, icon = "talent/extraDice", desc = "Rolls an additional die each turn (up to +4 dice)." },
} :: { [string]: TalentDef }

function Talents.get(id: string): TalentDef?
	return Talents.Defs[id]
end

return Talents
]]></ProtectedString>
						<string name="ScriptGuid">{8A5CFE96-F182-4BB8-BE98-3CCEAD394F4D}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Talents</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000f31</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX4146ED4A243B43ED88A4CEB8567EAEBB">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- Traits: per-battle trait pool offered as a pick-3 when the player reaches 5
-- active dice (R-C23). Traits are temporary and apply for the current battle
-- only. Effects reuse the same categories as talents plus battle-only tweaks.

export type TraitEffect =
	"flatAttack"
	| "pctAttack"
	| "critChance"
	| "critMultiplier"
	| "extraReroll"
	| "lifesteal"
	| "damageReduction"
	| "healAmount"

export type TraitDef = {
	id: string,
	name: string,
	effect: TraitEffect,
	value: number, -- flat amount applied for the battle
	description: string,
	icon: string,
}

local Traits = {}

Traits.Order = {
	"GlassCannon",
	"SteadyHands",
	"KeenEye",
	"Executioner",
	"Bloodthirst",
	"IronSkin",
	"SecondWind",
}

Traits.Defs = {
	GlassCannon = { id = "GlassCannon", name = "Glass Cannon", effect = "pctAttack", value = 0.5, description = "+50% attack for this battle.", icon = "trait/pctAttack" },
	SteadyHands = { id = "SteadyHands", name = "Steady Hands", effect = "extraReroll", value = 2, description = "+2 rerolls each turn this battle.", icon = "trait/extraReroll" },
	KeenEye = { id = "KeenEye", name = "Keen Eye", effect = "critChance", value = 0.15, description = "+15% crit chance this battle.", icon = "trait/critChance" },
	Executioner = { id = "Executioner", name = "Executioner", effect = "critMultiplier", value = 1.0, description = "Crits deal an extra x1 this battle.", icon = "trait/critMultiplier" },
	Bloodthirst = { id = "Bloodthirst", name = "Bloodthirst", effect = "lifesteal", value = 0.2, description = "Heal for 20% of damage dealt.", icon = "trait/lifesteal" },
	IronSkin = { id = "IronSkin", name = "Iron Skin", effect = "damageReduction", value = 0.2, description = "+20% damage reduction this battle.", icon = "trait/damageReduction" },
	SecondWind = { id = "SecondWind", name = "Second Wind", effect = "healAmount", value = 25, description = "+25 heal amount this battle.", icon = "trait/healAmount" },
} :: { [string]: TraitDef }

function Traits.get(id: string): TraitDef?
	return Traits.Defs[id]
end

return Traits
]]></ProtectedString>
						<string name="ScriptGuid">{181ECBF9-B064-4691-B641-CF8C64CEFCED}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Traits</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000f33</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBXFE929FD04FA04A4299C5358ECDCA38C8">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- Enemies: enemy archetype stats + weighted action table (R-C7, R-C2).
-- All enemies share the same action set (attack / heal / shield / nothing);
-- only stats and weights differ. Bosses are ordinary archetypes scaled by
-- Balance.Enemy multipliers at spawn time (see Chapters boss flag).

export type ActionWeights = {
	attack: number,
	heal: number,
	shield: number,
	nothing: number,
}

export type EnemyDef = {
	id: string,
	name: string,
	hp: number,
	attack: number,
	heal: number, -- flat self-heal amount when the heal action is chosen
	shield: number, -- flat shield added when the shield action is chosen
	actions: ActionWeights,
	icon: string,
}

local Enemies = {}

Enemies.Defs = {
	Slime = {
		id = "Slime",
		name = "Slime",
		hp = 30,
		attack = 5,
		heal = 4,
		shield = 3,
		actions = { attack = 70, heal = 10, shield = 10, nothing = 10 },
		icon = "enemy/slime",
	},
	Goblin = {
		id = "Goblin",
		name = "Goblin",
		hp = 40,
		attack = 8,
		heal = 5,
		shield = 4,
		actions = { attack = 80, heal = 5, shield = 10, nothing = 5 },
		icon = "enemy/goblin",
	},
	Archer = {
		id = "Archer",
		name = "Archer",
		hp = 35,
		attack = 12,
		heal = 0,
		shield = 2,
		actions = { attack = 90, heal = 0, shield = 5, nothing = 5 },
		icon = "enemy/archer",
	},
	Golem = {
		id = "Golem",
		name = "Golem",
		hp = 80,
		attack = 7,
		heal = 6,
		shield = 12,
		actions = { attack = 50, heal = 10, shield = 35, nothing = 5 },
		icon = "enemy/golem",
	},
	-- Boss archetypes (spawned on the last stage; Balance multipliers applied).
	BossOgre = {
		id = "BossOgre",
		name = "Ogre Warlord",
		hp = 120,
		attack = 14,
		heal = 10,
		shield = 10,
		actions = { attack = 70, heal = 10, shield = 15, nothing = 5 },
		icon = "enemy/boss_ogre",
	},
	BossLich = {
		id = "BossLich",
		name = "Lich King",
		hp = 100,
		attack = 12,
		heal = 20,
		shield = 8,
		actions = { attack = 55, heal = 25, shield = 15, nothing = 5 },
		icon = "enemy/boss_lich",
	},
	BossDragon = {
		id = "BossDragon",
		name = "Elder Dragon",
		hp = 160,
		attack = 18,
		heal = 12,
		shield = 15,
		actions = { attack = 75, heal = 10, shield = 10, nothing = 5 },
		icon = "enemy/boss_dragon",
	},
} :: { [string]: EnemyDef }

function Enemies.get(id: string): EnemyDef?
	return Enemies.Defs[id]
end

return Enemies
]]></ProtectedString>
						<string name="ScriptGuid">{1005E525-5577-4345-A38B-1EE295B76C9C}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Enemies</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000f35</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX4B035F2187AE43ADABDC06333074DE74">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- Chapters: chapter -> stage -> enemy composition + boss flag (R-C1, R-C2).
-- First build: 3 chapters x 5 stages each; the last stage of every chapter is a
-- boss. Each non-boss stage defines an enemyPool and a min/max count; the server
-- (CombatService) randomly spawns 1-3 enemies drawn from the pool. Boss stages
-- spawn exactly one enemy from the pool, scaled by Balance.Enemy multipliers.

export type StageDef = {
	boss: boolean,
	enemyPool: { string }, -- Enemies.Defs keys
	minEnemies: number,
	maxEnemies: number,
}

export type ChapterDef = {
	id: number,
	name: string,
	stages: { StageDef }, -- length 5; stages[5].boss == true
}

local Chapters = {}

Chapters.Order = { 1, 2, 3 }

Chapters.Defs = {
	[1] = {
		id = 1,
		name = "The Green Meadows",
		stages = {
			{ boss = false, enemyPool = { "Slime" }, minEnemies = 1, maxEnemies = 2 },
			{ boss = false, enemyPool = { "Slime", "Goblin" }, minEnemies = 1, maxEnemies = 3 },
			{ boss = false, enemyPool = { "Goblin", "Archer" }, minEnemies = 2, maxEnemies = 3 },
			{ boss = false, enemyPool = { "Goblin", "Archer", "Golem" }, minEnemies = 2, maxEnemies = 3 },
			{ boss = true, enemyPool = { "BossOgre" }, minEnemies = 1, maxEnemies = 1 },
		},
	},
	[2] = {
		id = 2,
		name = "The Frozen Caverns",
		stages = {
			{ boss = false, enemyPool = { "Goblin", "Archer" }, minEnemies = 1, maxEnemies = 3 },
			{ boss = false, enemyPool = { "Archer", "Golem" }, minEnemies = 2, maxEnemies = 3 },
			{ boss = false, enemyPool = { "Golem" }, minEnemies = 1, maxEnemies = 2 },
			{ boss = false, enemyPool = { "Archer", "Golem" }, minEnemies = 2, maxEnemies = 3 },
			{ boss = true, enemyPool = { "BossLich" }, minEnemies = 1, maxEnemies = 1 },
		},
	},
	[3] = {
		id = 3,
		name = "The Molten Peak",
		stages = {
			{ boss = false, enemyPool = { "Archer", "Golem" }, minEnemies = 2, maxEnemies = 3 },
			{ boss = false, enemyPool = { "Golem" }, minEnemies = 2, maxEnemies = 3 },
			{ boss = false, enemyPool = { "Goblin", "Archer", "Golem" }, minEnemies = 3, maxEnemies = 3 },
			{ boss = false, enemyPool = { "Golem" }, minEnemies = 3, maxEnemies = 3 },
			{ boss = true, enemyPool = { "BossDragon" }, minEnemies = 1, maxEnemies = 1 },
		},
	},
} :: { [number]: ChapterDef }

function Chapters.get(id: number): ChapterDef?
	return Chapters.Defs[id]
end

-- Total number of chapters defined.
function Chapters.count(): number
	return #Chapters.Order
end

return Chapters
]]></ProtectedString>
						<string name="ScriptGuid">{75BB45C2-74BB-4032-BE8D-807500E2B396}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">Chapters</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000f37</UniqueId>
					</Properties>
				</Item>
			</Item>
			<Item class="Folder" referent="RBX0E29519C911A458F8E0D95F5F1303EA1">
				<Properties>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
					<string name="Name">Logic</string>
					<int64 name="SourceAssetId">-1</int64>
					<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
					<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000aa4</UniqueId>
				</Properties>
				<Item class="ModuleScript" referent="RBX081094F85D4D481698A1BA6615FE9A66">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- DiceHands: pure logic to detect the best poker-style hand for 1..5 dice and
-- return its multiplier (R-C12, R-C13). No `game` state is read besides the
-- Balance config, so this module is deterministic and unit-testable.
--
-- Multipliers come from Balance.HandMultipliers; hand names below match its keys.
-- Hands requiring more dice than the player owns are naturally unavailable
-- because they can never satisfy the frequency/shape checks with fewer dice
-- (e.g. two pair needs >=4 dice, four of a kind needs >=4, full house/straight/
-- five of a kind need exactly 5).

local Balance = require(script.Parent.Parent.Config.Balance)

local DiceHands = {}

-- Best-to-worst ranking, used only for documentation/reference. Detection below
-- checks in this order and returns the first match.
DiceHands.Ranking = {
	"fiveOfAKind",
	"fourOfAKind",
	"fullHouse",
	"straight",
	"threeOfAKind",
	"twoPair",
	"onePair",
	"highCard",
}

export type HandResult = {
	hand: string, -- one of the Balance.HandMultipliers keys
	multiplier: number,
}

-- Multiplier for a named hand; falls back to highCard for unknown names.
function DiceHands.multiplierFor(hand: string): number
	local mult = Balance.HandMultipliers[hand]
	if mult == nil then
		return Balance.HandMultipliers.highCard
	end
	return mult
end

local function result(hand: string): HandResult
	return { hand = hand, multiplier = DiceHands.multiplierFor(hand) }
end

-- Detect the best hand for the given dice face values (each 1..faces).
-- `dice` length is expected to be 1..maxDice; empty/nil is treated as highCard.
function DiceHands.evaluate(dice: { number }): HandResult
	local n = #dice
	if n == 0 then
		return result("highCard")
	end

	-- Frequency of each face value.
	local countsByValue: { [number]: number } = {}
	for _, value in dice do
		countsByValue[value] = (countsByValue[value] or 0) + 1
	end

	-- Sorted list of counts (descending) plus distinct-value bookkeeping.
	local freqs: { number } = {}
	local distinct = 0
	local minValue = math.huge
	local maxValue = -math.huge
	for value, count in countsByValue do
		table.insert(freqs, count)
		distinct += 1
		if value < minValue then
			minValue = value
		end
		if value > maxValue then
			maxValue = value
		end
	end
	table.sort(freqs, function(a, b)
		return a > b
	end)

	local top = freqs[1] or 0
	local second = freqs[2] or 0

	-- Best-to-worst checks. Each shape implicitly requires enough dice.
	if top >= 5 then
		return result("fiveOfAKind")
	end
	if top >= 4 then
		return result("fourOfAKind")
	end
	if top >= 3 and second >= 2 then
		-- 3 of one value + 2 of another => exactly 5 dice.
		return result("fullHouse")
	end
	-- Straight: exactly 5 distinct, consecutive values (needs 5 dice by definition).
	if n == 5 and distinct == 5 and (maxValue - minValue) == 4 then
		return result("straight")
	end
	if top >= 3 then
		return result("threeOfAKind")
	end
	if top >= 2 and second >= 2 then
		-- Two distinct values each appearing >=2 => needs >=4 dice.
		return result("twoPair")
	end
	if top >= 2 then
		return result("onePair")
	end

	return result("highCard")
end

return DiceHands
]]></ProtectedString>
						<string name="ScriptGuid">{592A83AD-0C43-4B98-9E52-A195D49A76B7}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">DiceHands</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc5670000115f</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBXDD3D5E021E414652B1336AFA1A03DBF2">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- DamageFormula: pure combat math for player outgoing damage and incoming enemy
-- damage resolution (ARCHITECTURE.md 6; R-C14..R-C18). No `game` state beyond
-- Balance config, so it is deterministic and unit-testable. RNG (crit/dodge) is
-- injectable so callers stay authoritative and tests stay deterministic.
--
-- Player damage ordering (R-C14 / ARCHITECTURE 6):
--   final = (diceTotal + flat) * (1 + pct) * handMultiplier * (crit and critMult or 1)
--   Crit is rolled once per attack, applied AFTER the hand multiplier.
--
-- Incoming damage ordering (R-C15): dodge -> % reduction -> shield buffer -> HP.
--   Overflow past the shield carries to HP in the same hit.

local Balance = require(script.Parent.Parent.Config.Balance)

local DamageFormula = {}

-- ==================== Player outgoing damage ====================

export type PlayerDamageParams = {
	diceTotal: number, -- sum of current dice face values
	flatBonus: number?, -- flat attack added before the % bonus (default 0)
	pctBonus: number?, -- fractional % bonus, e.g. 0.25 = +25% (default 0)
	handMultiplier: number, -- from DiceHands.evaluate().multiplier
	crit: boolean?, -- whether this attack crit (roll via rollCrit); default false
	critMultiplier: number?, -- crit damage factor (default Balance baseCritMultiplier)
	lifesteal: number?, -- fraction of final damage healed to player (default 0)
}

export type PlayerDamageResult = {
	base: number, -- dice total
	adjusted: number, -- after flat then % : (base + flat) * (1 + pct)
	beforeCrit: number, -- after hand multiplier
	crit: boolean, -- whether crit was applied
	damage: number, -- final damage dealt to the enemy
	lifestealHeal: number, -- HP the player heals from this hit
}

-- Compute final player damage. Deterministic: crit is passed in, not rolled here.
function DamageFormula.computePlayerDamage(params: PlayerDamageParams): PlayerDamageResult
	local flat = params.flatBonus or 0
	local pct = params.pctBonus or 0
	local critMult = params.critMultiplier or Balance.Player.baseCritMultiplier
	local crit = params.crit == true
	local lifesteal = params.lifesteal or 0

	local base = params.diceTotal
	local adjusted = (base + flat) * (1 + pct)
	local beforeCrit = adjusted * params.handMultiplier
	local damage = beforeCrit * (if crit then critMult else 1)
	local lifestealHeal = damage * lifesteal

	return {
		base = base,
		adjusted = adjusted,
		beforeCrit = beforeCrit,
		crit = crit,
		damage = damage,
		lifestealHeal = lifestealHeal,
	}
end

-- Roll whether an attack crits. `rng` (optional) returns a float in [0,1); when
-- omitted, math.random() is used. Crit chance is clamped to Balance max.
function DamageFormula.rollCrit(critChance: number, rng: (() -> number)?): boolean
	local chance = math.clamp(critChance, 0, Balance.Player.maxCritChance)
	local roll = if rng then rng() else math.random()
	return roll < chance
end

-- ==================== Incoming enemy damage ====================

export type IncomingParams = {
	incoming: number, -- raw enemy attack value
	damageReduction: number?, -- fractional DR, e.g. 0.2 = -20% (default 0)
	dodged: boolean?, -- whether the hit was dodged (roll via rollDodge); default false
	shield: number, -- current shield buffer
	hp: number, -- current HP
}

export type IncomingResult = {
	dodged: boolean,
	afterReduction: number, -- damage remaining after % reduction (0 if dodged)
	shieldAbsorbed: number, -- amount soaked by the shield
	hpLost: number, -- amount removed from HP
	shield: number, -- remaining shield after the hit
	hp: number, -- remaining HP after the hit (floored at 0)
}

-- Resolve one incoming hit against the player in order: dodge -> % reduction ->
-- shield -> HP, with shield overflow carrying to HP in the same hit.
function DamageFormula.resolveIncomingDamage(params: IncomingParams): IncomingResult
	if params.dodged == true then
		return {
			dodged = true,
			afterReduction = 0,
			shieldAbsorbed = 0,
			hpLost = 0,
			shield = params.shield,
			hp = params.hp,
		}
	end

	local dr = math.clamp(params.damageReduction or 0, 0, Balance.Player.maxDamageReduction)
	local afterReduction = params.incoming * (1 - dr)

	local shieldAbsorbed = math.min(params.shield, afterReduction)
	local remainingShield = params.shield - shieldAbsorbed

	local overflow = afterReduction - shieldAbsorbed
	local hpLost = math.min(params.hp, overflow)
	local remainingHp = params.hp - hpLost

	return {
		dodged = false,
		afterReduction = afterReduction,
		shieldAbsorbed = shieldAbsorbed,
		hpLost = hpLost,
		shield = remainingShield,
		hp = remainingHp,
	}
end

-- Roll whether an incoming attack is dodged (R-C16). `rng` optional as above.
function DamageFormula.rollDodge(dodgeChance: number, rng: (() -> number)?): boolean
	local chance = math.clamp(dodgeChance, 0, Balance.Player.maxDodgeChance)
	local roll = if rng then rng() else math.random()
	return roll < chance
end

return DamageFormula
]]></ProtectedString>
						<string name="ScriptGuid">{355C8207-7E69-479E-AACA-1F50731DFD71}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">DamageFormula</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700001161</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX536E27CF180D4AAC9E422BD83B9798F0">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- GearStats: pure computation of a gear piece's effective stat magnitude given
-- its rarity and the level of the slot it occupies (R-E3, R-E6, R-E7, R-E8).
-- No `game` state beyond Config, so it is deterministic and reusable on both the
-- equipment UI (display) and the server/combat resolution (later steps).
--
-- Magnitude = base * rarityMultiplier * slotLevelMultiplier where:
--   base                 = gear.baseStat override, else Balance.GearBaseStat[stat]
--   rarityMultiplier     = Rarities.Defs[rarity].multiplier
--   slotLevelMultiplier  = 1 + (slotLevel - 1) * Balance.SlotLevels.bonusPerLevel

local Config = script.Parent.Parent.Config
local Gear = require(Config.Gear)
local Slots = require(Config.Slots)
local Rarities = require(Config.Rarities)
local Balance = require(Config.Balance)

local GearStats = {}

export type StatType = Slots.StatType

export type GearStat = {
	stat: StatType,
	value: number, -- effective magnitude (fractional for percent stats)
	isPercent: boolean,
}

-- Stats stored as fractions (0..1) that read as percentages in the UI.
local PERCENT_STATS: { [string]: boolean } = {
	critChance = true,
	dodgeChance = true,
	damageReduction = true,
}

-- True if the stat is expressed as a fraction that should render as a percent.
function GearStats.isPercent(stat: string): boolean
	return PERCENT_STATS[stat] == true
end

-- Common-rarity base magnitude for a gear id: its own override or the slot's
-- default from Balance. Returns 0 for unknown gear.
function GearStats.baseMagnitude(gearId: string): number
	local gearDef = Gear.get(gearId)
	if not gearDef then
		return 0
	end
	if gearDef.baseStat ~= nil then
		return gearDef.baseStat
	end
	local slotDef = Slots.get(gearDef.slot)
	if not slotDef then
		return 0
	end
	return (Balance.GearBaseStat :: { [string]: number })[slotDef.stat] or 0
end

-- Buff multiplier contributed by a slot's level (level 1 = x1.0).
function GearStats.slotMultiplier(slotLevel: number): number
	local level = math.max(1, slotLevel)
	return 1 + (level - 1) * Balance.SlotLevels.bonusPerLevel
end

-- Effective stat a gear piece provides at a given rarity and slot level.
-- Returns nil if the gear id (and thus its slot/stat) cannot be resolved.
function GearStats.compute(gearId: string, rarity: string, slotLevel: number): GearStat?
	local gearDef = Gear.get(gearId)
	if not gearDef then
		return nil
	end
	local slotDef = Slots.get(gearDef.slot)
	if not slotDef then
		return nil
	end

	local rarityDef = Rarities.get(rarity)
	local rarityMult = rarityDef and rarityDef.multiplier or 1

	local base = GearStats.baseMagnitude(gearId)
	local value = base * rarityMult * GearStats.slotMultiplier(slotLevel)

	return {
		stat = slotDef.stat,
		value = value,
		isPercent = GearStats.isPercent(slotDef.stat),
	}
end

-- Human-readable stat name for UI labels (e.g. "critChance" -> "Crit Chance").
local STAT_LABELS: { [string]: string } = {
	hp = "Health",
	attack = "Attack",
	critChance = "Crit Chance",
	dodgeChance = "Dodge Chance",
	damageReduction = "Damage Reduction",
	healAmount = "Heal Amount",
}

function GearStats.statLabel(stat: string): string
	return STAT_LABELS[stat] or stat
end

-- Format an effective magnitude for display: percent stats as "+12.5%", flat
-- stats as "+30" (rounded). Always prefixed with "+" since gear only buffs.
function GearStats.format(gearStat: GearStat): string
	if gearStat.isPercent then
		local pct = gearStat.value * 100
		local rounded = math.floor(pct * 10 + 0.5) / 10
		return ("+%s%%"):format(tostring(rounded))
	end
	return ("+%d"):format(math.floor(gearStat.value + 0.5))
end

return GearStats
]]></ProtectedString>
						<string name="ScriptGuid">{ABBFBA3C-7424-4349-9910-E48263337444}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">GearStats</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc567000017d5</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBXCAE9B417C51A4B4686B5F801F93A0E5A">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- InventoryLayout: pure grid math for the inventory tile display (R-E4).
-- Decides how many columns fit, the cell size that fills the row, and how many
-- placeholder "empty" tiles to show. The empty-tile rule: always fill out to at
-- least `minRows` (the visible area) so the inventory reads as having spare
-- capacity, and once items overflow that, grow by exactly the rows the items
-- require -- so only the last (partial) row is padded with empties, one new row
-- appearing at a time as rows fill. No Instances here so it is unit-testable.

local InventoryLayout = {}

export type FillPlan = {
	columns: number,
	totalRows: number,
	totalTiles: number,
	emptyCount: number,
}

-- How many fixed-size columns fit into `availableWidth` given inter-cell padding.
function InventoryLayout.columnCount(availableWidth: number, targetTile: number, padding: number): number
	if availableWidth <= 0 or targetTile <= 0 then
		return 1
	end
	return math.max(1, math.floor((availableWidth + padding) / (targetTile + padding)))
end

-- Cell size (square) that makes exactly `columns` cells fill `availableWidth`.
function InventoryLayout.cellSize(availableWidth: number, columns: number, padding: number): number
	columns = math.max(1, columns)
	return math.max(1, math.floor((availableWidth - (columns - 1) * padding) / columns))
end

-- Plan the tile grid for `itemCount` items across `columns`, filling at least
-- `minRows`. Returns the total rows/tiles and how many empty placeholders to add.
function InventoryLayout.plan(itemCount: number, columns: number, minRows: number): FillPlan
	columns = math.max(1, math.floor(columns))
	minRows = math.max(1, math.floor(minRows))
	itemCount = math.max(0, math.floor(itemCount))

	local rowsNeeded = math.ceil(itemCount / columns)
	local totalRows = math.max(minRows, rowsNeeded)
	local totalTiles = totalRows * columns
	local emptyCount = math.max(0, totalTiles - itemCount)

	return {
		columns = columns,
		totalRows = totalRows,
		totalTiles = totalTiles,
		emptyCount = emptyCount,
	}
end

return InventoryLayout
]]></ProtectedString>
						<string name="ScriptGuid">{1438BB65-17A8-4147-A5D9-8A1D77CD9DAA}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">InventoryLayout</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700002b2d</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX451BF3A63D9A4AE1B510A896BB7760A3">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- MergeLogic: pure rules for merging gear (R-E9). Three gear of the SAME type
-- (slot) and SAME rarity combine into one gear of the same slot, one rarity
-- higher (Common -> Uncommon, etc.). The result keeps the base id of the first
-- input (so its stat family/icon is preserved) but at the upgraded rarity, since
-- rarity is stored per-instance and drives the stat multiplier.
--
-- Shared so the server (authoritative) and client (preview/enable-state) agree.
-- No Instances/networking here, so it is unit-testable (tests/LogicTests).

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Config = Shared:WaitForChild("Config")
local Gear = require(Config:WaitForChild("Gear"))
local Rarities = require(Config:WaitForChild("Rarities"))

local MergeLogic = {}

-- Number of matching gear required to perform a merge.
MergeLogic.COUNT = 3

export type GearInstance = { id: string, rarity: string }
export type MergeResult = {
	ok: boolean,
	result: GearInstance?, -- the upgraded gear produced on success
	slot: string?, -- shared slot of the inputs (when resolvable)
	error: string?, -- human-readable reason when ok == false
}

-- The gear a single selection would upgrade into, or nil if it cannot be merged
-- (unknown gear, or already at the highest rarity). Used for the output preview.
function MergeLogic.resultFor(instance: GearInstance?): GearInstance?
	if not instance then
		return nil
	end
	local def = Gear.get(instance.id)
	if not def then
		return nil
	end
	local nextRarity = Rarities.next(instance.rarity)
	if not nextRarity then
		return nil
	end
	return { id = instance.id, rarity = nextRarity }
end

-- Slot id shared by an instance (via its gear def), or nil if unresolvable.
function MergeLogic.slotOf(instance: GearInstance?): string?
	if not instance then
		return nil
	end
	local def = Gear.get(instance.id)
	return def and def.slot or nil
end

-- Validate a set of gear instances for merging and, on success, return the
-- upgraded gear. Enforces: exactly COUNT inputs, all resolvable, all the same
-- slot, all the same rarity, and that rarity has a higher tier.
function MergeLogic.evaluate(instances: { GearInstance }): MergeResult
	if #instances ~= MergeLogic.COUNT then
		return { ok = false, error = ("Merging requires exactly %d gear."):format(MergeLogic.COUNT) }
	end

	local firstDef = Gear.get(instances[1].id)
	if not firstDef then
		return { ok = false, error = "Unknown gear." }
	end
	local slot = firstDef.slot
	local rarity = instances[1].rarity

	for index = 2, #instances do
		local inst = instances[index]
		local def = Gear.get(inst.id)
		if not def then
			return { ok = false, error = "Unknown gear." }
		end
		if def.slot ~= slot then
			return { ok = false, slot = slot, error = "All gear must be the same type." }
		end
		if inst.rarity ~= rarity then
			return { ok = false, slot = slot, error = "All gear must be the same rarity." }
		end
	end

	local nextRarity = Rarities.next(rarity)
	if not nextRarity then
		return { ok = false, slot = slot, error = "This rarity cannot be upgraded further." }
	end

	return {
		ok = true,
		slot = slot,
		result = { id = instances[1].id, rarity = nextRarity },
	}
end

export type OwnedGear = { uid: string, id: string, rarity: string }
export type AutoPlan = {
	removeUids: { string }, -- owned uids consumed by the run
	addInstances: { { id: string, rarity: string } }, -- upgraded gear to grant
}

-- Plan an "auto merge": repeatedly combine every set of 3 same-type + same-rarity
-- gear, cascading upward so newly-created gear can merge again in the same run
-- (e.g. 3 Common + 2 Uncommon -> 1 Uncommon -> combines with the 2 to make a
-- Rare). Pure: returns the net uids to remove and the upgraded gear to add, so
-- the same result can be unit-tested and applied authoritatively. (R-E9)
function MergeLogic.planAuto(instances: { OwnedGear }): AutoPlan
	-- bySlot[slot][rarity] = { { uid = string?, id = string }, ... }
	-- A nil uid marks a token produced during this run (synthetic upgrade).
	local bySlot: { [string]: { [string]: { { uid: string?, id: string } } } } = {}
	for _, inst in instances do
		local def = Gear.get(inst.id)
		if not def then
			continue
		end
		local byRarity = bySlot[def.slot]
		if not byRarity then
			byRarity = {}
			bySlot[def.slot] = byRarity
		end
		local bucket = byRarity[inst.rarity]
		if not bucket then
			bucket = {}
			byRarity[inst.rarity] = bucket
		end
		table.insert(bucket, { uid = inst.uid, id = inst.id })
	end

	local removeUids: { string } = {}
	local addInstances: { { id: string, rarity: string } } = {}

	for _, byRarity in bySlot do
		-- Promote low -> high so produced tokens can cascade into higher buckets.
		for orderIdx = 1, #Rarities.Order - 1 do
			local rarity = Rarities.Order[orderIdx]
			local nextRarity = Rarities.Order[orderIdx + 1]
			local bucket = byRarity[rarity]
			if bucket then
				while #bucket >= MergeLogic.COUNT do
					local first = bucket[1]
					for _ = 1, MergeLogic.COUNT do
						local token = table.remove(bucket, 1)
						if token and token.uid then
							table.insert(removeUids, token.uid)
						end
					end
					local upgraded = byRarity[nextRarity]
					if not upgraded then
						upgraded = {}
						byRarity[nextRarity] = upgraded
					end
					table.insert(upgraded, { uid = nil, id = first.id })
				end
			end
		end

		-- Any synthetic tokens still standing are the gear to grant.
		for _, rarity in Rarities.Order do
			local bucket = byRarity[rarity]
			if bucket then
				for _, token in bucket do
					if not token.uid then
						table.insert(addInstances, { id = token.id, rarity = rarity })
					end
				end
			end
		end
	end

	return { removeUids = removeUids, addInstances = addInstances }
end

return MergeLogic
]]></ProtectedString>
						<string name="ScriptGuid">{A1DF4C8A-F2DA-41B3-9F9F-2CDF9F3F8A91}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">MergeLogic</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700002d55</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX96A9FE367C6B4BB480AE2141EB276423">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- TalentLogic: pure, deterministic talent helpers reusable on both client and
-- server (R-T2..R-T4). No `game` state beyond Config, so the weighted roll can be
-- validated server-side and previewed client-side, and it is unit-testable.
--
-- A roll picks ONE talent weighted by its rarity's weight (rarer = less likely),
-- skipping any talent already at its maxLevel (e.g. extraDice caps at level 4).
-- Effect value scales linearly as perLevel * level.

local Config = script.Parent.Parent.Config
local Talents = require(Config.Talents)
local Rarities = require(Config.Rarities)

local TalentLogic = {}

export type TalentEffect = Talents.TalentEffect

-- An entry in the weighted roll pool.
export type PoolEntry = { id: string, weight: number }

-- Injected RNG returns a float in [0, 1). Defaults to math.random when omitted,
-- so tests can pass a deterministic stub (matching DamageFormula's convention).
type Rng = () -> number

-- True if `talentId` is at (or above) its cap and thus cannot be rolled further.
function TalentLogic.isMaxed(talentId: string, level: number): boolean
	local def = Talents.get(talentId)
	if not def then
		return true
	end
	return def.maxLevel ~= nil and level >= (def.maxLevel :: number)
end

-- Build the weighted roll pool from a talents map ({ [talentId]: level }).
-- Excludes maxed talents. Weight is the talent's rarity weight (higher = more
-- common). Iterates in Talents.Order for a stable, deterministic pool.
function TalentLogic.buildPool(talents: { [string]: number }): ({ PoolEntry }, number)
	local pool: { PoolEntry } = {}
	local total = 0
	for _, id in Talents.Order do
		local def = Talents.get(id)
		if def then
			local level = talents[id] or 0
			if not TalentLogic.isMaxed(id, level) then
				local rarityDef = Rarities.get(def.rarity)
				local weight = rarityDef and rarityDef.weight or 1
				if weight > 0 then
					table.insert(pool, { id = id, weight = weight })
					total += weight
				end
			end
		end
	end
	return pool, total
end

-- Pick one talent id from the pool, weighted by rarity (rarer = less likely).
-- Returns nil only when nothing is rollable (all talents maxed). `rng` returns
-- a float in [0, 1); defaults to math.random for production use.
function TalentLogic.pick(talents: { [string]: number }, rng: Rng?): string?
	local roll = rng or math.random
	local pool, total = TalentLogic.buildPool(talents)
	if total <= 0 then
		return nil
	end

	local target = roll() * total
	local cumulative = 0
	for _, entry in pool do
		cumulative += entry.weight
		if target < cumulative then
			return entry.id
		end
	end
	-- Floating-point safety: fall back to the last eligible entry.
	return pool[#pool].id
end

-- The effect magnitude a talent provides at a given level (perLevel * level).
-- Returns 0 for unknown talents.
function TalentLogic.effectValue(talentId: string, level: number): number
	local def = Talents.get(talentId)
	if not def then
		return 0
	end
	return def.perLevel * math.max(0, level)
end

-- ==================== display helpers ====================

-- Per-effect display metadata: a human label and whether it reads as a percent.
local EFFECT_META: { [string]: { label: string, percent: boolean } } = {
	flatAttack = { label = "Attack", percent = false },
	pctAttack = { label = "Attack", percent = true },
	critChance = { label = "Crit Chance", percent = true },
	extraReroll = { label = "Rerolls", percent = false },
	extraDice = { label = "Dice", percent = false },
	hp = { label = "HP", percent = false },
	lifesteal = { label = "Lifesteal", percent = true },
}

-- Human-readable label for an effect category (e.g. "pctAttack" -> "Attack").
function TalentLogic.effectLabel(effect: string): string
	local meta = EFFECT_META[effect]
	return meta and meta.label or effect
end

-- True if the effect is a fraction (0..1) that should render as a percentage.
function TalentLogic.isPercent(effect: string): boolean
	local meta = EFFECT_META[effect]
	return meta ~= nil and meta.percent
end

-- Format a talent's bonus at a level as a signed string, e.g. "+6 Attack",
-- "+15% Attack", "+2% Crit Chance", "+1 Die". Returns "-" for unknown talents.
function TalentLogic.formatBonus(talentId: string, level: number): string
	local def = Talents.get(talentId)
	if not def then
		return "-"
	end
	local value = TalentLogic.effectValue(talentId, level)
	local label = TalentLogic.effectLabel(def.effect)
	if TalentLogic.isPercent(def.effect) then
		local pct = value * 100
		local rounded = math.floor(pct * 10 + 0.5) / 10
		return ("+%s%% %s"):format(tostring(rounded), label)
	end
	return ("+%d %s"):format(math.floor(value + 0.5), label)
end

return TalentLogic
]]></ProtectedString>
						<string name="ScriptGuid">{C7E2840A-3E7B-436B-ACF1-0B9768A37949}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">TalentLogic</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc567000040be</UniqueId>
					</Properties>
				</Item>
			</Item>
		</Item>
	</Item>
	<Item class="ServerScriptService" referent="4">
		<Properties>
			<bool name="LoadStringEnabled">false</bool>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">ServerScriptService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000038e</UniqueId>
		</Properties>
		<Item class="Script" referent="5">
			<Properties>
				<ProtectedString name="Source"><![CDATA[--!strict
-- Server bootstrap: creates remotes, then loads and starts all services in
-- dependency order via the shared Loader.

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Loader = require(Shared:WaitForChild("Loader"))
local Remotes = require(Shared:WaitForChild("Remotes"))

-- Services must init/start in this dependency order.
local SERVICE_ORDER = {
	"DataService",
	"CurrencyService",
	"InventoryService",
	"TalentService",
	"GlobalLevelService",
	"CombatService",
	"ChestService",
	"ShopService",
	"LeaderboardService",
}

print("[Server] bootstrap starting")

-- Create RemoteEvents/RemoteFunctions before any service wires handlers.
Remotes.init()

local services = Loader.requireChildren(script:WaitForChild("Services"))

local deps = {
	Remotes = Remotes,
	Services = services,
}

Loader.initModules(services, deps, SERVICE_ORDER)
Loader.startModules(services, SERVICE_ORDER)

print("[Server] bootstrap complete")
]]></ProtectedString>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<token name="RunContext">0</token>
				<string name="ScriptGuid">{C3EB9CFA-6C19-44B5-968D-A7F0918DF92A}</string>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">Server</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003b4</UniqueId>
			</Properties>
			<Item class="Folder" referent="RBXAD7C21BF0DF84F84AB563596B8F8076E">
				<Properties>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
					<string name="Name">Services</string>
					<int64 name="SourceAssetId">-1</int64>
					<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
					<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000aa5</UniqueId>
				</Properties>
				<Item class="ModuleScript" referent="RBX02A56E2A1C1C49CABCB67B4AF5CA19AD">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- DataService: authoritative persistence layer for one save per user (R-D1..R-D3).
--
-- Responsibilities:
--   * Build a well-defined default PlayerData profile from config (R-D3).
--   * Load a profile on join from DataStore behind a SESSION LOCK so two live
--     servers can never write the same key concurrently (R-D2).
--   * Expose read/mutate accessors so every other service reads and edits state
--     through this single owner, and replicate changes to the owning client.
--   * Autosave on an interval and on player leave, refreshing / releasing the
--     lock as appropriate; flush everything on BindToClose (R-D2).
--   * Fall back to an in-memory MOCK store in Studio (or when DataStore access is
--     unavailable) so the game is fully playable offline.
--
-- Combat state is transient and NOT persisted; only durable results other
-- services write into the profile are saved (ARCHITECTURE.md 7).

local DataStoreService = game:GetService("DataStoreService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Types = require(Shared:WaitForChild("Types"))
local Config = Shared:WaitForChild("Config")
local Currencies = require(Config:WaitForChild("Currencies"))
local Slots = require(Config:WaitForChild("Slots"))
local Talents = require(Config:WaitForChild("Talents"))

type PlayerData = Types.PlayerData

-- A stored record wraps the durable data plus the session lock metadata.
type Lock = { sessionId: string, placeId: number, timestamp: number }
type Record = { data: PlayerData?, lock: Lock? }

-- A live in-memory session for a connected player.
type Session = {
	key: string,
	data: PlayerData,
	saving: boolean,
}

local DataService = {}

-- Fired as (player: Player, data: PlayerData) once a profile is loaded &
-- reconciled. Services that need to react to a fresh profile connect to this.
DataService.ProfileLoaded = Instance.new("BindableEvent")

-- ==================== configuration ====================

local DATASTORE_NAME = "PlayerData_v1"
local KEY_PREFIX = "Player_"
local CURRENT_VERSION = 1

local AUTOSAVE_INTERVAL = 60 -- seconds between periodic saves
local LOCK_STALE_SECONDS = 20 * 60 -- a lock older than this is considered dead and may be stolen
local LOAD_MAX_ATTEMPTS = 6 -- attempts to acquire the lock before giving up
local LOAD_RETRY_DELAY = 5 -- seconds between lock-acquire attempts
local USE_MOCK_IN_STUDIO = true -- Studio always uses the in-memory mock

-- Unique id for THIS server; used to detect whether a lock belongs to us. JobId
-- is empty in Studio/local, so fall back to a generated guid.
local SESSION_ID: string = if game.JobId ~= "" then game.JobId else HttpService:GenerateGUID(false)

-- ==================== state ====================

local deps: any = nil
local profileUpdated: RemoteEvent? = nil
local store: any = nil
local usingMock = false
local sessions: { [Player]: Session } = {}

-- ==================== defaults & reconciliation ====================

-- Deep copy a plain-data value (numbers/strings/bools/tables). Profiles only
-- ever contain JSON-safe data, so this is sufficient and avoids shared refs.
local function deepCopy<T>(value: T): T
	if type(value) ~= "table" then
		return value
	end
	local copy: any = {}
	for k, v in value :: any do
		copy[k] = deepCopy(v)
	end
	return copy
end

-- Starter gear granted to brand-new players so the Equipment tab is usable
-- before chests exist (Step 13 introduces the loot source). Each entry becomes
-- an owned, initially-unequipped instance. Safe to trim once chests land.
local STARTER_GEAR: { { id: string, rarity: string } } = {
	{ id = "WoodSword", rarity = "Common" },
	{ id = "IronSword", rarity = "Uncommon" },
	{ id = "IronHelm", rarity = "Common" },
	{ id = "RunicHelm", rarity = "Rare" },
	{ id = "LeatherBelt", rarity = "Common" },
	{ id = "LeatherVest", rarity = "Common" },
	{ id = "LuckyCharm", rarity = "Common" },
	{ id = "Sandals", rarity = "Common" },
	{ id = "SwiftBoots", rarity = "Uncommon" },

	-- TEST GEAR (R-E9): plentiful same type+rarity sets so gear merging can be
	-- exercised end-to-end. Trim alongside STARTER_GEAR once chests land.
	-- Weapons: enough Common to merge twice, plus Uncommon and Rare sets.
	{ id = "WoodSword", rarity = "Common" },
	{ id = "WoodSword", rarity = "Common" },
	{ id = "WoodSword", rarity = "Common" },
	{ id = "IronSword", rarity = "Common" },
	{ id = "IronSword", rarity = "Common" },
	{ id = "IronSword", rarity = "Uncommon" },
	{ id = "IronSword", rarity = "Uncommon" },
	{ id = "FlameBlade", rarity = "Rare" },
	{ id = "FlameBlade", rarity = "Rare" },
	{ id = "FlameBlade", rarity = "Rare" },
	-- Helmets
	{ id = "IronHelm", rarity = "Common" },
	{ id = "IronHelm", rarity = "Common" },
	{ id = "SteelHelm", rarity = "Uncommon" },
	{ id = "SteelHelm", rarity = "Uncommon" },
	{ id = "SteelHelm", rarity = "Uncommon" },
	-- Boots
	{ id = "Sandals", rarity = "Common" },
	{ id = "Sandals", rarity = "Common" },
	{ id = "SwiftBoots", rarity = "Uncommon" },
	-- Charms
	{ id = "LuckyCharm", rarity = "Common" },
	{ id = "LuckyCharm", rarity = "Common" },
	{ id = "LuckyCharm", rarity = "Uncommon" },
	-- Chestplates
	{ id = "LeatherVest", rarity = "Common" },
	{ id = "LeatherVest", rarity = "Common" },
	{ id = "PlateArmor", rarity = "Rare" },
	-- Belts
	{ id = "LeatherBelt", rarity = "Common" },
	{ id = "LeatherBelt", rarity = "Common" },
	{ id = "HealerSash", rarity = "Rare" },

	-- BULK TEST GEAR: extra pieces so the inventory overflows several rows,
	-- exercising scrolling + one-row-at-a-time growth. Trim with STARTER_GEAR.
	{ id = "WoodSword", rarity = "Common" },
	{ id = "WoodSword", rarity = "Common" },
	{ id = "WoodSword", rarity = "Common" },
	{ id = "IronSword", rarity = "Uncommon" },
	{ id = "IronSword", rarity = "Uncommon" },
	{ id = "IronSword", rarity = "Uncommon" },
	{ id = "FlameBlade", rarity = "Rare" },
	{ id = "FlameBlade", rarity = "Rare" },
	{ id = "DragonFang", rarity = "Epic" },
	{ id = "IronHelm", rarity = "Common" },
	{ id = "IronHelm", rarity = "Common" },
	{ id = "IronHelm", rarity = "Common" },
	{ id = "SteelHelm", rarity = "Uncommon" },
	{ id = "SteelHelm", rarity = "Uncommon" },
	{ id = "RunicHelm", rarity = "Rare" },
	{ id = "RunicHelm", rarity = "Rare" },
	{ id = "Sandals", rarity = "Common" },
	{ id = "Sandals", rarity = "Common" },
	{ id = "Sandals", rarity = "Common" },
	{ id = "SwiftBoots", rarity = "Uncommon" },
	{ id = "SwiftBoots", rarity = "Uncommon" },
	{ id = "PhantomBoots", rarity = "Epic" },
	{ id = "LuckyCharm", rarity = "Common" },
	{ id = "LuckyCharm", rarity = "Common" },
	{ id = "LuckyCharm", rarity = "Common" },
	{ id = "LuckyCharm", rarity = "Uncommon" },
	{ id = "LuckyCharm", rarity = "Uncommon" },
	{ id = "CritGem", rarity = "Epic" },
	{ id = "LeatherVest", rarity = "Common" },
	{ id = "LeatherVest", rarity = "Common" },
	{ id = "LeatherVest", rarity = "Common" },
	{ id = "PlateArmor", rarity = "Rare" },
	{ id = "PlateArmor", rarity = "Rare" },
	{ id = "Aegis", rarity = "Legendary" },
	{ id = "LeatherBelt", rarity = "Common" },
	{ id = "LeatherBelt", rarity = "Common" },
	{ id = "LeatherBelt", rarity = "Uncommon" },
	{ id = "HealerSash", rarity = "Rare" },
	{ id = "HealerSash", rarity = "Rare" },
	{ id = "HealerSash", rarity = "Rare" },
}

-- Build a fresh, well-defined default profile for a brand-new player (R-D3).
-- Derived from config so adding a currency/slot/talent needs no edits here.
local function makeDefaultData(): PlayerData
	local currencies: { [string]: number } = {}
	for _, id in Currencies.Order do
		currencies[id] = 0
	end
	-- TEST CURRENCY: seed coins so talent rolls can be exercised end-to-end
	-- before a real coin source (chapter rewards / chests) lands. Trim once
	-- those economy sources exist (Steps 12-13).
	currencies.coins = 500

	local slotLevels: { [string]: number } = {}
	local equipped: { [string]: string? } = {}
	for _, id in Slots.Order do
		slotLevels[id] = 1 -- slots start at level 1 (R-E3, R-E8)
		equipped[id] = nil -- nothing equipped initially
	end

	local talents: { [string]: number } = {}
	for _, id in Talents.Order do
		talents[id] = 0 -- all talents present from the start at level 0 (R-T1)
	end

	local gear: { [string]: Types.GearInstance } = {}
	for index, entry in STARTER_GEAR do
		gear["starter_" .. index] = { id = entry.id, rarity = entry.rarity }
	end

	return {
		version = CURRENT_VERSION,
		global = { level = 1, xp = 0 },
		currencies = (currencies :: any) :: Types.Currencies,
		startingDiceUpgrade = 0,
		gear = gear,
		equipped = equipped,
		slotLevels = slotLevels,
		talents = talents,
		progress = { currentChapter = 1, chaptersCompleted = 0 },
		-- Seed leaderboard-tracked stats to well-defined starting values (R-L2).
		stats = { highestChapter = 1, highestPlayerLevel = 1 },
	}
end

-- Recursively add any keys present in template but missing in target, without
-- ever overwriting values the player already has. Lets defaults (new currencies,
-- slots, talents, fields) safely appear on old saves.
local function fillMissing(target: { [any]: any }, template: { [any]: any })
	for key, templateValue in template do
		local current = target[key]
		if current == nil then
			target[key] = deepCopy(templateValue)
		elseif type(current) == "table" and type(templateValue) == "table" then
			fillMissing(current, templateValue)
		end
	end
end

-- Take whatever was loaded (possibly nil / an older version) and return a
-- complete, current-version PlayerData. This is the single migration entry
-- point: the `version` field enables future explicit migrations.
local function reconcile(loaded: PlayerData?): PlayerData
	local template = makeDefaultData()
	if type(loaded) ~= "table" then
		return template
	end
	local data = loaded :: PlayerData
	fillMissing(data :: any, template :: any)
	data.version = CURRENT_VERSION
	return data
end

-- ==================== store resolution (real vs mock) ====================

-- In-memory stand-in implementing the tiny slice of the DataStore API we use.
-- Data does not persist across sessions, which is exactly what we want in
-- Studio without API access enabled.
local function createMockStore(): any
	local storage: { [string]: any } = {}
	local mock = {}

	function mock:GetAsync(key: string): any
		return deepCopy(storage[key])
	end

	function mock:SetAsync(key: string, value: any)
		storage[key] = deepCopy(value)
	end

	function mock:UpdateAsync(key: string, transform: (any) -> any): any
		local transformed = transform(deepCopy(storage[key]))
		if transformed ~= nil then
			storage[key] = deepCopy(transformed)
		end
		return deepCopy(storage[key])
	end

	function mock:RemoveAsync(key: string): any
		local previous = storage[key]
		storage[key] = nil
		return previous
	end

	return mock
end

-- Pick the real DataStore when available; otherwise fall back to the mock and
-- log why, so the choice is never silent.
local function resolveStore()
	if RunService:IsStudio() and USE_MOCK_IN_STUDIO then
		usingMock = true
		store = createMockStore()
		print("[DataService] Studio detected -> using in-memory MOCK store (no persistence)")
		return
	end

	local ok, real = pcall(function()
		return DataStoreService:GetDataStore(DATASTORE_NAME)
	end)
	if ok and real then
		store = real
		usingMock = false
		print(("[DataService] using live DataStore %q"):format(DATASTORE_NAME))
	else
		usingMock = true
		store = createMockStore()
		warn(("[DataService] DataStore unavailable (%s) -> using in-memory MOCK store"):format(tostring(real)))
	end
end

-- ==================== lock helpers ====================

-- True if a lock belongs to another server and is still fresh (not stale).
local function isForeignAndFresh(lock: Lock?): boolean
	if not lock then
		return false
	end
	if lock.sessionId == SESSION_ID then
		return false
	end
	return (os.time() - lock.timestamp) < LOCK_STALE_SECONDS
end

local function makeLock(): Lock
	return { sessionId = SESSION_ID, placeId = game.PlaceId, timestamp = os.time() }
end

-- ==================== replication ====================

-- Push the authoritative profile to its owning client (server -> client).
local function replicate(player: Player)
	local session = sessions[player]
	if session and profileUpdated then
		profileUpdated:FireClient(player, session.data)
	end
end

-- ==================== load / save ====================

-- Attempt one atomic lock acquisition via UpdateAsync. Returns the acquired
-- record on success, nil if the key is locked by a live foreign session, and
-- errors are surfaced via the second return value.
local function tryAcquire(key: string): (Record?, string?)
	local acquired: Record? = nil
	local ok, err = pcall(function()
		store:UpdateAsync(key, function(old: Record?): Record?
			local record: Record = old or {}
			if isForeignAndFresh(record.lock) then
				-- Another live server owns this profile; abort the write.
				return nil
			end
			record.lock = makeLock()
			acquired = record
			return record
		end)
	end)
	if not ok then
		return nil, tostring(err)
	end
	return acquired, nil
end

-- Load (or create) a player's profile behind the session lock. Retries while the
-- key is held by a live foreign session; kicks the player if it can never be
-- acquired so we never risk a data-losing overwrite.
local function loadProfile(player: Player)
	local key = KEY_PREFIX .. tostring(player.UserId)
	local record: Record? = nil

	for attempt = 1, LOAD_MAX_ATTEMPTS do
		if player.Parent == nil then
			return -- player left mid-load
		end

		local acquired, err = tryAcquire(key)
		if err then
			warn(("[DataService] load attempt %d for %s failed: %s"):format(attempt, player.Name, err))
		elseif acquired then
			record = acquired
			break
		else
			warn(("[DataService] profile for %s is locked by another session (attempt %d)"):format(player.Name, attempt))
		end

		if attempt < LOAD_MAX_ATTEMPTS then
			task.wait(LOAD_RETRY_DELAY)
		end
	end

	if player.Parent == nil then
		return
	end

	if not record then
		player:Kick("Your saved data is currently in use by another server. Please rejoin in a moment.")
		return
	end

	local data = reconcile(record.data)
	sessions[player] = { key = key, data = data, saving = false }

	print(("[DataService] loaded profile for %s (v%d)"):format(player.Name, data.version))
	DataService.ProfileLoaded:Fire(player, data)
	replicate(player)
end

-- Persist a player's profile. When `release` is true the lock is cleared (used on
-- leave / shutdown); otherwise the lock timestamp is refreshed to keep it alive.
-- We refuse to overwrite if our lock was stolen by a live foreign session.
local function saveProfile(player: Player, release: boolean)
	local session = sessions[player]
	if not session or session.saving then
		return
	end
	session.saving = true

	local ok, err = pcall(function()
		store:UpdateAsync(session.key, function(old: Record?): Record?
			local record: Record = old or {}
			if isForeignAndFresh(record.lock) then
				-- We lost the lock to another live session; do not clobber it.
				return nil
			end
			record.data = session.data
			record.lock = if release then nil else makeLock()
			return record
		end)
	end)

	session.saving = false

	if ok then
		print(("[DataService] saved profile for %s%s"):format(player.Name, if release then " (released)" else ""))
	else
		warn(("[DataService] save FAILED for %s: %s"):format(player.Name, tostring(err)))
	end
end

-- ==================== public accessors ====================

-- True once the player's profile has finished loading.
function DataService.isLoaded(player: Player): boolean
	return sessions[player] ~= nil
end

-- Read the live profile table for a player (nil if not loaded yet). Callers
-- should treat the result as owned by DataService and mutate only via `update`.
function DataService.get(player: Player): PlayerData?
	local session = sessions[player]
	return session and session.data or nil
end

-- Block until the player's profile is loaded (or `timeout` seconds elapse).
function DataService.waitForData(player: Player, timeout: number?): PlayerData?
	local deadline = os.clock() + (timeout or 10)
	while sessions[player] == nil and os.clock() < deadline do
		if player.Parent == nil then
			return nil
		end
		task.wait()
	end
	local session = sessions[player]
	return session and session.data or nil
end

-- Mutate a player's profile through a callback, then replicate the change to the
-- owning client. Returns false if the profile is not loaded. This is the single
-- sanctioned write path for other services.
function DataService.update(player: Player, mutator: (PlayerData) -> ()): boolean
	local session = sessions[player]
	if not session then
		warn(("[DataService] update ignored for %s: profile not loaded"):format(player.Name))
		return false
	end
	mutator(session.data)
	replicate(player)
	return true
end

-- Force an immediate (lock-refreshing) save for a single player.
function DataService.save(player: Player)
	saveProfile(player, false)
end

-- Save every loaded profile (used by autosave and shutdown).
function DataService.saveAll(release: boolean)
	for player in sessions do
		saveProfile(player, release)
	end
end

-- True when running against the in-memory mock (no real persistence).
function DataService.isMock(): boolean
	return usingMock
end

-- ==================== lifecycle ====================

local function onPlayerRemoving(player: Player)
	if sessions[player] then
		saveProfile(player, true)
		sessions[player] = nil
	end
end

function DataService.init(injected: any)
	deps = injected
	if deps and deps.Remotes then
		profileUpdated = deps.Remotes.getEvent("ProfileUpdated")
	end
	resolveStore()
end

function DataService.start()
	-- Load for anyone already present (Studio play-solo joins before start()).
	for _, player in Players:GetPlayers() do
		task.spawn(loadProfile, player)
	end

	Players.PlayerAdded:Connect(function(player)
		loadProfile(player)
	end)
	Players.PlayerRemoving:Connect(onPlayerRemoving)

	-- Periodic autosave (also refreshes each live lock so it never goes stale).
	task.spawn(function()
		while true do
			task.wait(AUTOSAVE_INTERVAL)
			DataService.saveAll(false)
		end
	end)

	-- Flush + release all locks on server shutdown so profiles unlock cleanly.
	game:BindToClose(function()
		DataService.saveAll(true)
	end)
end

return DataService
]]></ProtectedString>
						<string name="ScriptGuid">{45E0B6AE-A543-410E-9D67-F71CFBC5ACAB}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">DataService</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000aa6</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX5C8A53B486654A7D9ABE605C12801016">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- CurrencyService: the sole mutator of player currency balances (coins, gems,
-- materials, keys) with add/spend validation (ARCHITECTURE.md 9, R-Y1). Every
-- balance change flows through here so validation and replication stay in one
-- place; all writes go through DataService.update. Other services (TalentService,
-- and later ChestService/InventoryService) call these instead of touching
-- `profile.currencies` directly. Reads/mutations are keyed by Currencies ids.

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Config = Shared:WaitForChild("Config")
local Currencies = require(Config:WaitForChild("Currencies"))

local CurrencyService = {}

local deps: any = nil
local DataService: any = nil

-- Validate that `currencyId` names a known currency.
local function isValidCurrency(currencyId: string): boolean
	return type(currencyId) == "string" and Currencies.get(currencyId) ~= nil
end

-- Current balance of a currency for a player (0 if unknown / not loaded).
function CurrencyService.get(player: Player, currencyId: string): number
	if not isValidCurrency(currencyId) then
		return 0
	end
	local data = DataService.get(player)
	if not data then
		return 0
	end
	return (data.currencies :: { [string]: number })[currencyId] or 0
end

-- True if the player can afford `amount` of `currencyId` (amount must be >= 0).
function CurrencyService.canAfford(player: Player, currencyId: string, amount: number): boolean
	if type(amount) ~= "number" or amount < 0 then
		return false
	end
	return CurrencyService.get(player, currencyId) >= amount
end

-- Grant `amount` (>= 0) of a currency. Returns true on success.
function CurrencyService.add(player: Player, currencyId: string, amount: number): boolean
	if not isValidCurrency(currencyId) or type(amount) ~= "number" or amount < 0 then
		return false
	end
	if amount == 0 then
		return true
	end
	return DataService.update(player, function(profile)
		local balances = profile.currencies :: { [string]: number }
		balances[currencyId] = (balances[currencyId] or 0) + amount
	end)
end

-- Spend `amount` (>= 0) of a currency if affordable. Returns true only when the
-- deduction actually happened, so callers can gate an action on the result.
function CurrencyService.spend(player: Player, currencyId: string, amount: number): boolean
	if not isValidCurrency(currencyId) or type(amount) ~= "number" or amount < 0 then
		return false
	end
	if amount == 0 then
		return true
	end
	if not CurrencyService.canAfford(player, currencyId, amount) then
		return false
	end
	return DataService.update(player, function(profile)
		local balances = profile.currencies :: { [string]: number }
		balances[currencyId] = (balances[currencyId] or 0) - amount
	end)
end

function CurrencyService.init(injected: any)
	deps = injected
	DataService = deps.Services.DataService
end

function CurrencyService.start() end

return CurrencyService
]]></ProtectedString>
						<string name="ScriptGuid">{C1D7D92D-F91A-4CB2-ABE0-7713E497BA56}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">CurrencyService</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000aa8</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBXB51E9914ADC84CA4876D769EA27D3D2F">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- InventoryService: authoritative gear inventory operations — equip, unequip, and
-- (later) slot leveling (R-E2..R-E8, ARCHITECTURE.md 4/9). All state lives in the
-- player's profile and is mutated only through DataService, which replicates the
-- change back to the owning client. The client sends intent via the EquipGear /
-- UnequipGear remotes; the server validates ownership and legality here.

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Config = Shared:WaitForChild("Config")
local Logic = Shared:WaitForChild("Logic")
local Gear = require(Config:WaitForChild("Gear"))
local Slots = require(Config:WaitForChild("Slots"))
local Balance = require(Config:WaitForChild("Balance"))
local MergeLogic = require(Logic:WaitForChild("MergeLogic"))

local InventoryService = {}

local deps: any = nil
local DataService: any = nil
local equipRemote: RemoteEvent? = nil
local unequipRemote: RemoteEvent? = nil
local levelSlotRemote: RemoteEvent? = nil
local mergeGearRemote: RemoteEvent? = nil
local autoMergeGearRemote: RemoteEvent? = nil
local toastRemote: RemoteEvent? = nil

-- Generate a gear key not already present in the profile.
local function newGearUid(profile: any): string
	local uid
	repeat
		uid = "gear_" .. HttpService:GenerateGUID(false)
	until profile.gear[uid] == nil
	return uid
end

-- Optional player feedback; no-op until a Toast UI consumes it.
local function toast(player: Player, message: string)
	if toastRemote then
		toastRemote:FireClient(player, message)
	end
end

-- Equip an owned gear instance into the slot dictated by its definition. Any
-- piece already in that slot is implicitly unequipped (returns to the grid).
-- Returns true on success. Server-authoritative: validates ownership + slot.
function InventoryService.EquipGear(player: Player, gearUid: string): boolean
	if type(gearUid) ~= "string" then
		return false
	end

	local data = DataService.get(player)
	if not data then
		return false
	end

	local instance = data.gear[gearUid]
	if not instance then
		toast(player, "You do not own that gear.")
		return false
	end

	local gearDef = Gear.get(instance.id)
	if not gearDef then
		return false
	end
	local slotDef = Slots.get(gearDef.slot)
	if not slotDef then
		return false
	end
	local slotId = slotDef.id

	if data.equipped[slotId] == gearUid then
		return true -- already equipped in its slot; nothing to do
	end

	DataService.update(player, function(profile)
		profile.equipped[slotId] = gearUid
	end)
	return true
end

-- Clear whatever is equipped in `slotId` (R-E5). Returns true if the slot is a
-- valid slot id (even if it was already empty).
function InventoryService.UnequipGear(player: Player, slotId: string): boolean
	if type(slotId) ~= "string" or not Slots.get(slotId) then
		return false
	end

	local data = DataService.get(player)
	if not data then
		return false
	end

	if data.equipped[slotId] == nil then
		return true -- already empty
	end

	DataService.update(player, function(profile)
		profile.equipped[slotId] = nil
	end)
	return true
end

-- Raise a slot's level by one, up to Balance.SlotLevels.maxLevel (R-E8). NOTE:
-- Step 13 will make this spend materials via CurrencyService; for now it is free.
function InventoryService.LevelSlot(player: Player, slotId: string): boolean
	if type(slotId) ~= "string" or not Slots.get(slotId) then
		return false
	end

	local data = DataService.get(player)
	if not data then
		return false
	end

	local current = data.slotLevels[slotId] or 1
	if current >= Balance.SlotLevels.maxLevel then
		toast(player, "That slot is already at max level.")
		return false
	end

	DataService.update(player, function(profile)
		local level = profile.slotLevels[slotId] or 1
		profile.slotLevels[slotId] = math.min(level + 1, Balance.SlotLevels.maxLevel)
	end)
	return true
end

-- Merge three owned gear of the same type + rarity into one gear a rarity higher
-- (R-E9). Validated by the shared MergeLogic so client and server agree. Consumed
-- pieces are removed from the inventory (and unequipped if any was equipped); the
-- upgraded gear is added unequipped. Returns true on success.
function InventoryService.MergeGear(player: Player, uids: any): boolean
	if type(uids) ~= "table" then
		return false
	end

	local data = DataService.get(player)
	if not data then
		return false
	end

	-- Collect exactly COUNT distinct, owned uids and resolve their instances.
	local seen: { [string]: boolean } = {}
	local orderedUids: { string } = {}
	local instances: { { id: string, rarity: string } } = {}
	for _, uid in uids do
		if type(uid) ~= "string" or seen[uid] then
			toast(player, "Invalid merge selection.")
			return false
		end
		local inst = data.gear[uid]
		if not inst then
			toast(player, "You do not own that gear.")
			return false
		end
		seen[uid] = true
		table.insert(orderedUids, uid)
		table.insert(instances, { id = inst.id, rarity = inst.rarity })
	end

	if #orderedUids ~= MergeLogic.COUNT then
		toast(player, ("Merging requires exactly %d gear."):format(MergeLogic.COUNT))
		return false
	end

	local evaluation = MergeLogic.evaluate(instances)
	if not (evaluation.ok and evaluation.result) then
		toast(player, evaluation.error or "These gear cannot be merged.")
		return false
	end
	local result = evaluation.result

	DataService.update(player, function(profile)
		-- Remove the consumed pieces, unequipping any that were equipped.
		for _, uid in orderedUids do
			profile.gear[uid] = nil
			for slotId, equippedUid in profile.equipped do
				if equippedUid == uid then
					profile.equipped[slotId] = nil
				end
			end
		end
		-- Add the upgraded gear (unequipped).
		local newUid = newGearUid(profile)
		profile.gear[newUid] = { id = result.id, rarity = result.rarity }
	end)

	local gearDef = Gear.get(result.id)
	toast(player, ("Merged into %s %s!"):format(result.rarity, gearDef and gearDef.name or result.id))
	return true
end

-- Auto-merge every set of 3 same-type + same-rarity gear the player owns,
-- cascading upward (R-E9). Consumed pieces are removed (and unequipped); the
-- upgraded gear is granted unequipped. Returns true if anything was merged.
function InventoryService.AutoMergeGear(player: Player): boolean
	local data = DataService.get(player)
	if not data then
		return false
	end

	local instances: { { uid: string, id: string, rarity: string } } = {}
	for uid, inst in data.gear do
		table.insert(instances, { uid = uid, id = inst.id, rarity = inst.rarity })
	end

	local plan = MergeLogic.planAuto(instances)
	if #plan.removeUids == 0 then
		toast(player, "Nothing to auto-merge.")
		return false
	end

	DataService.update(player, function(profile)
		for _, uid in plan.removeUids do
			profile.gear[uid] = nil
			for slotId, equippedUid in profile.equipped do
				if equippedUid == uid then
					profile.equipped[slotId] = nil
				end
			end
		end
		for _, newInst in plan.addInstances do
			local newUid = newGearUid(profile)
			profile.gear[newUid] = { id = newInst.id, rarity = newInst.rarity }
		end
	end)

	toast(player, ("Auto-merged %d gear into %d."):format(#plan.removeUids, #plan.addInstances))
	return true
end

function InventoryService.init(injected: any)
	deps = injected
	DataService = deps.Services.DataService
	equipRemote = deps.Remotes.getEvent("EquipGear")
	unequipRemote = deps.Remotes.getEvent("UnequipGear")
	levelSlotRemote = deps.Remotes.getEvent("LevelSlot")
	mergeGearRemote = deps.Remotes.getEvent("MergeGear")
	autoMergeGearRemote = deps.Remotes.getEvent("AutoMergeGear")
	toastRemote = deps.Remotes.getEvent("Toast")
end

function InventoryService.start()
	local equip = equipRemote :: RemoteEvent
	local unequip = unequipRemote :: RemoteEvent
	local levelSlot = levelSlotRemote :: RemoteEvent

	equip.OnServerEvent:Connect(function(player: Player, gearUid: any)
		InventoryService.EquipGear(player, gearUid)
	end)
	unequip.OnServerEvent:Connect(function(player: Player, slotId: any)
		InventoryService.UnequipGear(player, slotId)
	end)
	levelSlot.OnServerEvent:Connect(function(player: Player, slotId: any)
		InventoryService.LevelSlot(player, slotId)
	end)

	local merge = mergeGearRemote :: RemoteEvent
	merge.OnServerEvent:Connect(function(player: Player, uids: any)
		InventoryService.MergeGear(player, uids)
	end)

	local autoMerge = autoMergeGearRemote :: RemoteEvent
	autoMerge.OnServerEvent:Connect(function(player: Player)
		InventoryService.AutoMergeGear(player)
	end)
end

return InventoryService
]]></ProtectedString>
						<string name="ScriptGuid">{5768A9D6-C6E4-46DE-B06B-1E5F72F2113C}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">InventoryService</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000aaa</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBXDF204FD1DA2B48E2B9526C9F69495300">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- TalentService: authoritative talent rolls (R-T1..R-T4). A roll costs coins and,
-- when affordable, picks ONE talent weighted by rarity (rarer = less likely) and
-- raises its level by 1. All RNG and validation live here (server-authoritative);
-- the client only sends intent via the RollTalent remote and re-renders on the
-- replicated ProfileUpdated. Coin spending goes through CurrencyService (the sole
-- balance mutator); talent levels are written through DataService.update.

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Config = Shared:WaitForChild("Config")
local Logic = Shared:WaitForChild("Logic")
local Balance = require(Config:WaitForChild("Balance"))
local Talents = require(Config:WaitForChild("Talents"))
local TalentLogic = require(Logic:WaitForChild("TalentLogic"))

local COIN_CURRENCY = "coins"

local TalentService = {}

local deps: any = nil
local DataService: any = nil
local CurrencyService: any = nil
local rollTalentRemote: RemoteEvent? = nil
local toastRemote: RemoteEvent? = nil

-- Optional player feedback; harmless no-op until a Toast UI consumes it.
local function toast(player: Player, message: string)
	if toastRemote then
		toastRemote:FireClient(player, message)
	end
end

-- Roll one talent: spend the coin cost, then rarity-weighted +1 a talent (R-T2).
-- Returns the rolled talent id on success, or nil if the roll was rejected
-- (profile not loaded, nothing rollable, or insufficient coins).
function TalentService.RollTalent(player: Player): string?
	local data = DataService.get(player)
	if not data then
		return nil
	end

	local cost = Balance.Economy.talentRollCost

	-- Nothing to roll if every talent is already at its cap.
	local _, totalWeight = TalentLogic.buildPool(data.talents)
	if totalWeight <= 0 then
		toast(player, "All talents are maxed out.")
		return nil
	end

	-- Affordability is checked before we spend or pick (R-T2).
	if not CurrencyService.canAfford(player, COIN_CURRENCY, cost) then
		toast(player, ("Not enough coins (need %d)."):format(cost))
		return nil
	end

	-- Pick before spending so a nil pick never costs coins.
	local talentId = TalentLogic.pick(data.talents)
	if not talentId then
		return nil
	end

	if not CurrencyService.spend(player, COIN_CURRENCY, cost) then
		-- Balance changed between the check and the spend; abort cleanly.
		return nil
	end

	DataService.update(player, function(profile)
		local level = profile.talents[talentId] or 0
		profile.talents[talentId] = level + 1
	end)

	local def = Talents.get(talentId)
	local newLevel = data.talents[talentId] or 0
	toast(player, ("Rolled %s -> Lv %d"):format(def and def.name or talentId, newLevel))
	return talentId
end

function TalentService.init(injected: any)
	deps = injected
	DataService = deps.Services.DataService
	CurrencyService = deps.Services.CurrencyService
	rollTalentRemote = deps.Remotes.getEvent("RollTalent")
	toastRemote = deps.Remotes.getEvent("Toast")
end

function TalentService.start()
	local roll = rollTalentRemote :: RemoteEvent
	roll.OnServerEvent:Connect(function(player: Player)
		TalentService.RollTalent(player)
	end)
end

return TalentService
]]></ProtectedString>
						<string name="ScriptGuid">{B8B6CA9A-BD20-4412-8322-E6E4F869FEFD}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">TalentService</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000aac</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX15BD02EA8C60419AB91C12E46623347B">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- GlobalLevelService: global XP/level; level-ups grant stats, talent rolls,
-- and permanent starting-dice upgrades. Stub for Step 1.

local GlobalLevelService = {}

local deps: any = nil

function GlobalLevelService.init(injected: any)
	deps = injected
end

function GlobalLevelService.start() end

return GlobalLevelService
]]></ProtectedString>
						<string name="ScriptGuid">{517D45B5-75CB-4D6B-BDFA-4D5030F3B95C}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">GlobalLevelService</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000aae</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBXE1847CA1807B4EC6936B58533F26C1DF">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- CombatService: per-player authoritative combat state machine
-- (chapter/stage/turn flow, dice, damage, enemy actions). Stub for Step 1.

local CombatService = {}

local deps: any = nil

function CombatService.init(injected: any)
	deps = injected
end

function CombatService.start() end

return CombatService
]]></ProtectedString>
						<string name="ScriptGuid">{F220FA79-1E98-4BCF-9A02-CBC34398B86E}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">CombatService</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ab0</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX0B3B5959B2E843448669ACF0FB68FD58">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- ChestService: key spend, weighted loot rolls (single chest, spin-all).
-- Stub for Step 1.

local ChestService = {}

local deps: any = nil

function ChestService.init(injected: any)
	deps = injected
end

function ChestService.start() end

return ChestService
]]></ProtectedString>
						<string name="ScriptGuid">{51B26665-6060-4708-80A0-C3C1FDF386C0}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">ChestService</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ab2</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX49840E970B66467481AE32FD2AD0DB62">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- ShopService: extensible stub for gems/Robux/gamepass/daily shop. Stub for Step 1.

local ShopService = {}

local deps: any = nil

function ShopService.init(injected: any)
	deps = injected
end

function ShopService.start() end

return ShopService
]]></ProtectedString>
						<string name="ScriptGuid">{94DD0F64-E56F-4950-A09F-B1F46C0A990C}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">ShopService</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ab4</UniqueId>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX8B77DBD259C5448EB77A17E7FF184270">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[--!strict
-- LeaderboardService: generic OrderedDataStore stat tracking (lb_<statName>).
-- Stub for Step 1.

local LeaderboardService = {}

local deps: any = nil

function LeaderboardService.init(injected: any)
	deps = injected
end

function LeaderboardService.start() end

return LeaderboardService
]]></ProtectedString>
						<string name="ScriptGuid">{7A220E8B-28DF-4CD4-807D-E70E6CA43095}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
						<string name="Name">LeaderboardService</string>
						<int64 name="SourceAssetId">-1</int64>
						<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
						<UniqueId name="UniqueId">6b5e38b5c27d020a0a5cc56700000ab6</UniqueId>
					</Properties>
				</Item>
			</Item>
		</Item>
		<Item class="Script" referent="RBX8EEEC8D3665D47E49B0E8AC47A1ABA72">
			<Properties>
				<ProtectedString name="Source"><![CDATA[--!strict
-- LogicTests: self-contained server test harness for the pure combat-logic
-- modules (Shared/Logic/DiceHands and Shared/Logic/DamageFormula).
--
-- Runs automatically on server start (mapped to ServerScriptService.LogicTests
-- in default.project.json). It prints a PASS/FAIL summary and errors at the end
-- if anything fails, so failures are impossible to miss in the output.
--
-- Covers: every dice hand for 1..5 dice, hand-ranking tie-breaks, the exact
-- player-damage ordering (flat -> % -> hand -> crit) including crit, lifesteal,
-- deterministic crit/dodge rolls, and incoming-damage resolution
-- (dodge -> % reduction -> shield -> HP with overflow).

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Logic = Shared:WaitForChild("Logic")
local Balance = require(Shared:WaitForChild("Config"):WaitForChild("Balance"))
local DiceHands = require(Logic:WaitForChild("DiceHands"))
local DamageFormula = require(Logic:WaitForChild("DamageFormula"))
local InventoryLayout = require(Logic:WaitForChild("InventoryLayout"))
local MergeLogic = require(Logic:WaitForChild("MergeLogic"))
local TalentLogic = require(Logic:WaitForChild("TalentLogic"))

local M = Balance.HandMultipliers
local EPS = 1e-9

-- ==================== tiny assertion framework ====================

local passed = 0
local failed = 0
local failures: { string } = {}

local function record(ok: boolean, name: string, detail: string?)
	if ok then
		passed += 1
	else
		failed += 1
		local msg = detail and (name .. " -- " .. detail) or name
		table.insert(failures, msg)
		warn("[LogicTests] FAIL: " .. msg)
	end
end

local function approx(a: number, b: number): boolean
	return math.abs(a - b) <= EPS
end

local function expectEqual(actual: any, expected: any, name: string)
	record(actual == expected, name, ("expected %s, got %s"):format(tostring(expected), tostring(actual)))
end

local function expectApprox(actual: number, expected: number, name: string)
	record(approx(actual, expected), name, ("expected ~%s, got %s"):format(tostring(expected), tostring(actual)))
end

-- ==================== DiceHands: every hand ====================

local function expectHand(dice: { number }, hand: string, mult: number)
	local res = DiceHands.evaluate(dice)
	local label = ("hand{%s}"):format(table.concat(dice, ","))
	expectEqual(res.hand, hand, label .. " name")
	expectApprox(res.multiplier, mult, label .. " multiplier")
end

-- 1 die: only high card is possible.
expectHand({ 5 }, "highCard", M.highCard)

-- 2 dice: high card, one pair.
expectHand({ 2, 5 }, "highCard", M.highCard)
expectHand({ 3, 3 }, "onePair", M.onePair)

-- 3 dice: high card, one pair, three of a kind (no two pair / straight yet).
expectHand({ 1, 2, 4 }, "highCard", M.highCard)
expectHand({ 2, 2, 5 }, "onePair", M.onePair)
expectHand({ 4, 4, 4 }, "threeOfAKind", M.threeOfAKind)
expectHand({ 1, 2, 3 }, "highCard", M.highCard) -- straight needs 5 dice

-- 4 dice: adds two pair and four of a kind; full house/straight still need 5.
expectHand({ 1, 2, 3, 5 }, "highCard", M.highCard)
expectHand({ 1, 2, 3, 4 }, "highCard", M.highCard) -- 4-in-a-row is NOT a straight
expectHand({ 5, 5, 2, 6 }, "onePair", M.onePair)
expectHand({ 2, 2, 5, 5 }, "twoPair", M.twoPair)
expectHand({ 3, 3, 3, 6 }, "threeOfAKind", M.threeOfAKind)
expectHand({ 6, 6, 6, 6 }, "fourOfAKind", M.fourOfAKind)

-- 5 dice: all hands available.
expectHand({ 1, 3, 4, 5, 2 }, "straight", M.straight) -- 1-5
expectHand({ 2, 3, 4, 5, 6 }, "straight", M.straight) -- 2-6
expectHand({ 1, 2, 3, 4, 6 }, "highCard", M.highCard) -- distinct but not consecutive
expectHand({ 5, 5, 1, 2, 3 }, "onePair", M.onePair)
expectHand({ 2, 2, 4, 4, 6 }, "twoPair", M.twoPair)
expectHand({ 3, 3, 3, 5, 6 }, "threeOfAKind", M.threeOfAKind)
expectHand({ 5, 5, 5, 2, 2 }, "fullHouse", M.fullHouse)
expectHand({ 6, 6, 6, 6, 1 }, "fourOfAKind", M.fourOfAKind)
expectHand({ 4, 4, 4, 4, 4 }, "fiveOfAKind", M.fiveOfAKind)

-- Ranking tie-breaks (best hand wins).
expectHand({ 6, 6, 6, 6, 6 }, "fiveOfAKind", M.fiveOfAKind) -- five > four
expectHand({ 2, 2, 2, 3, 3 }, "fullHouse", M.fullHouse) -- full house > three of a kind
-- four of a kind (5.0) must beat full house (4.0): 4-of-a-kind + 1 spare.
expectHand({ 1, 1, 1, 1, 5 }, "fourOfAKind", M.fourOfAKind)

-- multiplierFor helper + unknown fallback.
expectApprox(DiceHands.multiplierFor("fiveOfAKind"), M.fiveOfAKind, "multiplierFor five")
expectApprox(DiceHands.multiplierFor("nonsense"), M.highCard, "multiplierFor unknown -> highCard")

-- ==================== DamageFormula: player damage ordering ====================

-- No bonuses, high card: damage == dice total.
do
	local r = DamageFormula.computePlayerDamage({ diceTotal = 7, handMultiplier = M.highCard })
	expectApprox(r.damage, 7, "player no-bonus damage")
	expectEqual(r.crit, false, "player no-bonus crit flag")
end

-- Flat applied BEFORE %, not after: (10 + 10) * (1 + 1.0) = 40, not 10 + 10*2 = 30.
do
	local r = DamageFormula.computePlayerDamage({
		diceTotal = 10,
		flatBonus = 10,
		pctBonus = 1.0,
		handMultiplier = M.highCard,
	})
	expectApprox(r.adjusted, 40, "player flat-before-pct adjusted")
	expectApprox(r.damage, 40, "player flat-before-pct damage")
end

-- Hand multiplier applied after flat/%: (10 + 5) * 1.5 * 2(onePair) = 45.
do
	local r = DamageFormula.computePlayerDamage({
		diceTotal = 10,
		flatBonus = 5,
		pctBonus = 0.5,
		handMultiplier = M.twoPair, -- 2.0
	})
	expectApprox(r.adjusted, 22.5, "player adjusted before hand")
	expectApprox(r.beforeCrit, 45, "player beforeCrit (after hand)")
	expectApprox(r.damage, 45, "player non-crit final")
end

-- Crit applied LAST, after the hand multiplier: same as above but *2.
do
	local r = DamageFormula.computePlayerDamage({
		diceTotal = 10,
		flatBonus = 5,
		pctBonus = 0.5,
		handMultiplier = M.twoPair,
		crit = true,
		critMultiplier = 2.0,
	})
	expectApprox(r.beforeCrit, 45, "player crit beforeCrit unchanged")
	expectApprox(r.damage, 90, "player crit final (x2 after hand)")
	expectEqual(r.crit, true, "player crit flag true")
end

-- Crit multiplier is configurable and multiplies the post-hand value.
do
	local r = DamageFormula.computePlayerDamage({
		diceTotal = 4,
		handMultiplier = M.fiveOfAKind, -- 8.0
		crit = true,
		critMultiplier = 3.0,
	})
	expectApprox(r.beforeCrit, 32, "player big-hand beforeCrit (4*8)")
	expectApprox(r.damage, 96, "player big-hand crit (x3)")
end

-- Lifesteal is a fraction of the FINAL (post-crit) damage.
do
	local r = DamageFormula.computePlayerDamage({
		diceTotal = 20,
		handMultiplier = M.highCard,
		crit = true,
		critMultiplier = 2.0,
		lifesteal = 0.25,
	})
	expectApprox(r.damage, 40, "player lifesteal final damage")
	expectApprox(r.lifestealHeal, 10, "player lifesteal heal (25% of final)")
end

-- Default crit multiplier falls back to Balance.
do
	local r = DamageFormula.computePlayerDamage({
		diceTotal = 10,
		handMultiplier = M.highCard,
		crit = true,
	})
	expectApprox(r.damage, 10 * Balance.Player.baseCritMultiplier, "player default crit multiplier")
end

-- ==================== DamageFormula: deterministic crit/dodge rolls ====================

-- roll < chance => hit; injected rng makes this deterministic.
record(DamageFormula.rollCrit(0.5, function() return 0.0 end) == true, "rollCrit 0.0 < 0.5 => true")
record(DamageFormula.rollCrit(0.5, function() return 0.9 end) == false, "rollCrit 0.9 !< 0.5 => false")
record(DamageFormula.rollCrit(0.05, function() return 0.05 end) == false, "rollCrit boundary (roll==chance) => false")
-- crit chance clamps to Balance max (1.0): even a 0.999 roll crits at 100%.
record(DamageFormula.rollCrit(5.0, function() return 0.999 end) == true, "rollCrit clamps chance to max")

record(DamageFormula.rollDodge(0.3, function() return 0.1 end) == true, "rollDodge 0.1 < 0.3 => true")
record(DamageFormula.rollDodge(0.3, function() return 0.5 end) == false, "rollDodge 0.5 !< 0.3 => false")
-- dodge chance clamps to Balance max (0.9): a 0.95 roll can never be dodged.
record(DamageFormula.rollDodge(5.0, function() return 0.95 end) == false, "rollDodge clamps chance to max (0.9)")

-- ==================== DamageFormula: incoming damage resolution ====================

-- Dodge => zero damage, HP and shield untouched.
do
	local r = DamageFormula.resolveIncomingDamage({ incoming = 100, shield = 30, hp = 80, dodged = true })
	expectEqual(r.dodged, true, "incoming dodged flag")
	expectApprox(r.hpLost, 0, "incoming dodged hpLost 0")
	expectApprox(r.shield, 30, "incoming dodged shield unchanged")
	expectApprox(r.hp, 80, "incoming dodged hp unchanged")
end

-- % reduction only (no shield): 100 * (1 - 0.2) = 80 straight to HP.
do
	local r = DamageFormula.resolveIncomingDamage({ incoming = 100, damageReduction = 0.2, shield = 0, hp = 200 })
	expectApprox(r.afterReduction, 80, "incoming DR afterReduction")
	expectApprox(r.hpLost, 80, "incoming DR hpLost")
	expectApprox(r.hp, 120, "incoming DR remaining hp")
end

-- Shield absorbs fully: 50 <= 100 shield, HP untouched.
do
	local r = DamageFormula.resolveIncomingDamage({ incoming = 50, shield = 100, hp = 80 })
	expectApprox(r.shieldAbsorbed, 50, "incoming shield full absorb")
	expectApprox(r.shield, 50, "incoming shield remaining")
	expectApprox(r.hpLost, 0, "incoming shield hp untouched")
end

-- Overflow past shield carries to HP in the same hit.
do
	local r = DamageFormula.resolveIncomingDamage({ incoming = 100, shield = 30, hp = 80 })
	expectApprox(r.shieldAbsorbed, 30, "incoming overflow shield absorbed")
	expectApprox(r.shield, 0, "incoming overflow shield emptied")
	expectApprox(r.hpLost, 70, "incoming overflow hpLost")
	expectApprox(r.hp, 10, "incoming overflow remaining hp")
end

-- Full ordering: reduction THEN shield THEN HP. 100 * (1-0.5)=50; shield 20 => 30 to HP.
do
	local r = DamageFormula.resolveIncomingDamage({ incoming = 100, damageReduction = 0.5, shield = 20, hp = 80 })
	expectApprox(r.afterReduction, 50, "incoming order afterReduction")
	expectApprox(r.shieldAbsorbed, 20, "incoming order shield absorbed")
	expectApprox(r.hpLost, 30, "incoming order hpLost")
	expectApprox(r.hp, 50, "incoming order remaining hp")
end

-- DR clamps to Balance max (0.9): 0.99 requested => 0.9 applied, 100 => 10 to HP.
do
	local r = DamageFormula.resolveIncomingDamage({ incoming = 100, damageReduction = 0.99, shield = 0, hp = 80 })
	expectApprox(r.afterReduction, 10, "incoming DR clamp afterReduction")
	expectApprox(r.hpLost, 10, "incoming DR clamp hpLost")
end

-- HP floors at 0 (lethal overkill): 500 damage vs 80 HP => hp 0, hpLost 80.
do
	local r = DamageFormula.resolveIncomingDamage({ incoming = 500, shield = 0, hp = 80 })
	expectApprox(r.hp, 0, "incoming lethal hp floored at 0")
	expectApprox(r.hpLost, 80, "incoming lethal hpLost == hp")
end

-- ==================== InventoryLayout: empty-tile / row growth ====================

-- columnCount + cellSize: 7 columns of ~96px targets (10 pad) fit an 800px row,
-- and the cell then fills the row exactly.
expectEqual(InventoryLayout.columnCount(800, 96, 10), 7, "columnCount 800/96/10 => 7")
expectEqual(InventoryLayout.cellSize(800, 7, 10), 105, "cellSize 800/7/10 => 105")
expectEqual(InventoryLayout.columnCount(0, 96, 10), 1, "columnCount zero width => 1")

-- Row growth uses columns = 8, minRows = 5 (a 40-tile visible area).
local COLS = 8
local MINROWS = 5

-- Empty inventory: still fills the whole visible area.
do
	local p = InventoryLayout.plan(0, COLS, MINROWS)
	expectEqual(p.totalRows, 5, "plan(0) totalRows == minRows")
	expectEqual(p.emptyCount, 40, "plan(0) empties fill visible area")
end

-- A few items: no extra rows, empties fill the rest of the visible area.
do
	local p = InventoryLayout.plan(9, COLS, MINROWS)
	expectEqual(p.totalRows, 5, "plan(9) stays at minRows")
	expectEqual(p.emptyCount, 31, "plan(9) empties = 40 - 9")
end

-- Exactly filling the visible area: no empties, no extra row yet.
do
	local p = InventoryLayout.plan(40, COLS, MINROWS)
	expectEqual(p.totalRows, 5, "plan(40) exactly fills visible area")
	expectEqual(p.emptyCount, 0, "plan(40) no empties")
end

-- Overflow by ONE: grows by exactly one row, and only that row is padded.
do
	local p = InventoryLayout.plan(41, COLS, MINROWS)
	expectEqual(p.totalRows, 6, "plan(41) adds exactly one row")
	expectEqual(p.emptyCount, 7, "plan(41) only the new row is padded (8-1)")
end

-- Filling that new row: empties return to zero (no premature extra row).
do
	local p = InventoryLayout.plan(48, COLS, MINROWS)
	expectEqual(p.totalRows, 6, "plan(48) still six rows")
	expectEqual(p.emptyCount, 0, "plan(48) new row filled, no empties")
end

-- One more after that: exactly one further row appears, again padded alone.
do
	local p = InventoryLayout.plan(49, COLS, MINROWS)
	expectEqual(p.totalRows, 7, "plan(49) adds one more row")
	expectEqual(p.emptyCount, 7, "plan(49) only the newest row padded")
end

-- Guards: bad inputs are coerced, never error.
do
	local p = InventoryLayout.plan(5, 0, 0)
	expectEqual(p.columns, 1, "plan guards columns >= 1")
	expectEqual(p.totalRows, 5, "plan guards minRows >= 1 (rows == items at 1 col)")
end

-- ==================== MergeLogic: gear merging ====================

-- Three identical Common weapons upgrade to an Uncommon of the same base id.
do
	local r = MergeLogic.evaluate({
		{ id = "WoodSword", rarity = "Common" },
		{ id = "WoodSword", rarity = "Common" },
		{ id = "WoodSword", rarity = "Common" },
	})
	expectEqual(r.ok, true, "merge 3x WoodSword Common ok")
	expectEqual(r.result and r.result.id, "WoodSword", "merge result keeps base id")
	expectEqual(r.result and r.result.rarity, "Uncommon", "merge result rarity +1")
	expectEqual(r.slot, "Weapon", "merge result slot Weapon")
end

-- Same slot but different base ids (same rarity) still merge; result uses first id.
do
	local r = MergeLogic.evaluate({
		{ id = "IronSword", rarity = "Common" },
		{ id = "FlameBlade", rarity = "Common" },
		{ id = "WoodSword", rarity = "Common" },
	})
	expectEqual(r.ok, true, "merge mixed-id same-slot same-rarity ok")
	expectEqual(r.result and r.result.id, "IronSword", "merge mixed result uses first id")
	expectEqual(r.result and r.result.rarity, "Uncommon", "merge mixed result rarity +1")
end

-- Different slots are rejected.
do
	local r = MergeLogic.evaluate({
		{ id = "WoodSword", rarity = "Common" },
		{ id = "IronHelm", rarity = "Common" },
		{ id = "LeatherBelt", rarity = "Common" },
	})
	expectEqual(r.ok, false, "merge different slots rejected")
end

-- Different rarities are rejected.
do
	local r = MergeLogic.evaluate({
		{ id = "WoodSword", rarity = "Common" },
		{ id = "WoodSword", rarity = "Common" },
		{ id = "WoodSword", rarity = "Uncommon" },
	})
	expectEqual(r.ok, false, "merge different rarities rejected")
end

-- Legendary cannot be upgraded further.
do
	local r = MergeLogic.evaluate({
		{ id = "Aegis", rarity = "Legendary" },
		{ id = "Aegis", rarity = "Legendary" },
		{ id = "Aegis", rarity = "Legendary" },
	})
	expectEqual(r.ok, false, "merge Legendary rejected (no higher rarity)")
end

-- Wrong count is rejected.
do
	local r = MergeLogic.evaluate({
		{ id = "WoodSword", rarity = "Common" },
		{ id = "WoodSword", rarity = "Common" },
	})
	expectEqual(r.ok, false, "merge with 2 inputs rejected")
end

-- resultFor previews the upgrade for a single selection.
do
	local preview = MergeLogic.resultFor({ id = "WoodSword", rarity = "Common" })
	expectEqual(preview and preview.rarity, "Uncommon", "resultFor Common -> Uncommon")
	expectEqual(MergeLogic.resultFor({ id = "Aegis", rarity = "Legendary" }), nil, "resultFor Legendary -> nil")
	expectEqual(MergeLogic.resultFor(nil), nil, "resultFor nil -> nil")
	expectEqual(MergeLogic.slotOf({ id = "WoodSword", rarity = "Common" }), "Weapon", "slotOf WoodSword -> Weapon")
end

-- ==================== MergeLogic: auto merge (cascading) ====================

-- Helper: total gear of a given rarity produced by an auto plan.
local function countRarity(plan: any, rarity: string): number
	local n = 0
	for _, inst in plan.addInstances do
		if inst.rarity == rarity then
			n += 1
		end
	end
	return n
end

-- 3 Common weapons -> 1 Uncommon.
do
	local plan = MergeLogic.planAuto({
		{ uid = "a", id = "WoodSword", rarity = "Common" },
		{ uid = "b", id = "WoodSword", rarity = "Common" },
		{ uid = "c", id = "WoodSword", rarity = "Common" },
	})
	expectEqual(#plan.removeUids, 3, "auto 3 Common: removes 3")
	expectEqual(#plan.addInstances, 1, "auto 3 Common: adds 1")
	expectEqual(countRarity(plan, "Uncommon"), 1, "auto 3 Common: result Uncommon")
end

-- 3 Common + 2 Uncommon cascade: 3 Common -> 1 Uncommon, then 3 Uncommon -> 1 Rare.
do
	local plan = MergeLogic.planAuto({
		{ uid = "a", id = "WoodSword", rarity = "Common" },
		{ uid = "b", id = "WoodSword", rarity = "Common" },
		{ uid = "c", id = "WoodSword", rarity = "Common" },
		{ uid = "d", id = "IronSword", rarity = "Uncommon" },
		{ uid = "e", id = "IronSword", rarity = "Uncommon" },
	})
	expectEqual(#plan.removeUids, 5, "auto cascade: removes all 5 originals")
	expectEqual(#plan.addInstances, 1, "auto cascade: adds 1")
	expectEqual(countRarity(plan, "Rare"), 1, "auto cascade: result Rare")
end

-- Nothing to merge (only 2 of a kind).
do
	local plan = MergeLogic.planAuto({
		{ uid = "a", id = "WoodSword", rarity = "Common" },
		{ uid = "b", id = "WoodSword", rarity = "Common" },
	})
	expectEqual(#plan.removeUids, 0, "auto nothing: removes 0")
	expectEqual(#plan.addInstances, 0, "auto nothing: adds 0")
end

-- 9 Common weapons -> 3 Uncommon -> 1 Rare (full cascade, synthetics consumed).
do
	local nine: { any } = {}
	for i = 1, 9 do
		table.insert(nine, { uid = "w" .. i, id = "WoodSword", rarity = "Common" })
	end
	local plan = MergeLogic.planAuto(nine)
	expectEqual(#plan.removeUids, 9, "auto 9 Common: removes 9")
	expectEqual(#plan.addInstances, 1, "auto 9 Common: adds 1")
	expectEqual(countRarity(plan, "Rare"), 1, "auto 9 Common: result Rare")
end

-- Different slots merge independently in one run.
do
	local plan = MergeLogic.planAuto({
		{ uid = "a", id = "WoodSword", rarity = "Common" },
		{ uid = "b", id = "WoodSword", rarity = "Common" },
		{ uid = "c", id = "WoodSword", rarity = "Common" },
		{ uid = "d", id = "IronHelm", rarity = "Common" },
		{ uid = "e", id = "IronHelm", rarity = "Common" },
		{ uid = "f", id = "IronHelm", rarity = "Common" },
	})
	expectEqual(#plan.removeUids, 6, "auto multi-slot: removes 6")
	expectEqual(#plan.addInstances, 2, "auto multi-slot: adds 2")
	expectEqual(countRarity(plan, "Uncommon"), 2, "auto multi-slot: 2 Uncommon")
end

-- Legendary cannot merge and is left untouched.
do
	local plan = MergeLogic.planAuto({
		{ uid = "a", id = "Aegis", rarity = "Legendary" },
		{ uid = "b", id = "Aegis", rarity = "Legendary" },
		{ uid = "c", id = "Aegis", rarity = "Legendary" },
	})
	expectEqual(#plan.removeUids, 0, "auto Legendary: removes 0")
	expectEqual(#plan.addInstances, 0, "auto Legendary: adds 0")
end

-- ==================== TalentLogic: weighted roll ====================

-- A fresh talents map: every talent present at level 0 (matches R-T1 defaults).
local function freshTalents(): { [string]: number }
	return {
		Sharpen = 0,
		Vitality = 0,
		Berserk = 0,
		Precision = 0,
		LuckyHand = 0,
		Vampiric = 0,
		Multiroll = 0,
	}
end

-- Pool total weight = sum of every talent's rarity weight (2 Common@100,
-- 1 Uncommon@50, 2 Rare@20, 2 Legendary@2 = 294).
do
	local _, total = TalentLogic.buildPool(freshTalents())
	expectApprox(total, 294, "talent pool total weight (all level 0)")
end

-- Deterministic pick: rng at 0 hits the first pool entry (Sharpen), a mid roll
-- lands on Vitality, and a near-1 roll reaches the last entry (Multiroll).
expectEqual(TalentLogic.pick(freshTalents(), function() return 0.0 end), "Sharpen", "pick rng=0 -> first (Sharpen)")
expectEqual(TalentLogic.pick(freshTalents(), function() return 0.5 end), "Vitality", "pick rng=0.5 -> Vitality (147/294)")
expectEqual(TalentLogic.pick(freshTalents(), function() return 0.999 end), "Multiroll", "pick rng~1 -> last (Multiroll)")

-- Maxed talents are excluded: Multiroll (extraDice) caps at level 4.
expectEqual(TalentLogic.isMaxed("Multiroll", 4), true, "isMaxed Multiroll at cap")
expectEqual(TalentLogic.isMaxed("Multiroll", 3), false, "isMaxed Multiroll below cap")
expectEqual(TalentLogic.isMaxed("Sharpen", 999), false, "isMaxed uncapped talent never maxes")
do
	local maxed = freshTalents()
	maxed.Multiroll = 4
	local _, total = TalentLogic.buildPool(maxed)
	expectApprox(total, 292, "pool excludes maxed Multiroll (294 - 2)")
	-- A near-1 roll now lands on Vampiric (the new last eligible entry).
	expectEqual(TalentLogic.pick(maxed, function() return 0.999 end), "Vampiric", "pick skips maxed talent")
end

-- An empty talents map means every talent defaults to level 0 (profiles omit
-- unrolled talents), so buildPool still uses the full definition list and a pick
-- returns a valid, non-maxed talent rather than nil.
do
	local _, total = TalentLogic.buildPool({})
	expectApprox(total, 294, "empty talents map still builds the full pool")
	local picked = TalentLogic.pick({}, function() return 0.5 end)
	record(picked ~= nil, "pick with empty talents map returns a valid talent",
		("picked=%s"):format(tostring(picked)))
end

-- Distribution sanity (R-T2): rarer talents roll LESS often than common ones.
do
	local rng = Random.new(1337)
	local counts: { [string]: number } = {}
	local samples = 4000
	for _ = 1, samples do
		local id = TalentLogic.pick(freshTalents(), function()
			return rng:NextNumber()
		end)
		if id then
			counts[id] = (counts[id] or 0) + 1
		end
	end
	local common = (counts.Sharpen or 0) + (counts.Vitality or 0)
	local legendary = (counts.Multiroll or 0) + (counts.LuckyHand or 0)
	record(common > legendary, "distribution: Common rolled more than Legendary",
		("common=%d legendary=%d"):format(common, legendary))
	record((counts.Vitality or 0) > (counts.LuckyHand or 0), "distribution: a Common rolled more than a Legendary",
		("vitality=%d luckyHand=%d"):format(counts.Vitality or 0, counts.LuckyHand or 0))
end

-- ==================== TalentLogic: effect values & formatting ====================

expectApprox(TalentLogic.effectValue("Sharpen", 3), 6, "effectValue Sharpen L3 (2*3)")
expectApprox(TalentLogic.effectValue("Berserk", 2), 0.10, "effectValue Berserk L2 (0.05*2)")
expectApprox(TalentLogic.effectValue("Sharpen", 0), 0, "effectValue at level 0 is 0")

expectEqual(TalentLogic.formatBonus("Sharpen", 3), "+6 Attack", "formatBonus flat attack")
expectEqual(TalentLogic.formatBonus("Berserk", 2), "+10% Attack", "formatBonus percent attack")
expectEqual(TalentLogic.formatBonus("Precision", 1), "+2% Crit Chance", "formatBonus percent crit")
expectEqual(TalentLogic.formatBonus("Vampiric", 1), "+3% Lifesteal", "formatBonus percent lifesteal")
expectEqual(TalentLogic.formatBonus("Multiroll", 4), "+4 Dice", "formatBonus flat dice")
expectEqual(TalentLogic.effectLabel("pctAttack"), "Attack", "effectLabel pctAttack -> Attack")

-- ==================== summary ====================

local total = passed + failed
if failed == 0 then
	print(("[LogicTests] ALL PASSED (%d/%d)"):format(passed, total))
else
	print(("[LogicTests] %d/%d passed, %d FAILED:"):format(passed, total, failed))
	for _, msg in failures do
		print("  - " .. msg)
	end
	error(("[LogicTests] %d assertion(s) failed"):format(failed), 0)
end
]]></ProtectedString>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<token name="RunContext">0</token>
				<string name="ScriptGuid">{EDF20685-96D8-4EE9-B557-BBBAC474C1B5}</string>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
				<string name="Name">LogicTests</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
				<UniqueId name="UniqueId">5f065c052d3bb4eb0a62a75400000d51</UniqueId>
			</Properties>
		</Item>
	</Item>
	<Item class="ServerStorage" referent="RBXBED0F027ED2E4F4984EEBEA6EFC22B46">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">ServerStorage</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be0000038f</UniqueId>
		</Properties>
	</Item>
	<Item class="HttpService" referent="RBX9504659208FE45E6A81B9A9EC2621FE9">
		<Properties>
			<bool name="HttpEnabled">false</bool>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">HttpService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003ac</UniqueId>
		</Properties>
	</Item>
	<Item class="Lighting" referent="0">
		<Properties>
			<Color3 name="Ambient">
				<R>0</R>
				<G>0</G>
				<B>0</B>
			</Color3>
			<float name="Brightness">2</float>
			<Color3 name="ColorShift_Bottom">
				<R>0</R>
				<G>0</G>
				<B>0</B>
			</Color3>
			<Color3 name="ColorShift_Top">
				<R>0</R>
				<G>0</G>
				<B>0</B>
			</Color3>
			<float name="EnvironmentDiffuseScale">0</float>
			<float name="EnvironmentSpecularScale">0</float>
			<float name="ExposureCompensation">0</float>
			<token name="ExtendLightRangeTo120">0</token>
			<Color3 name="FogColor">
				<R>0.75</R>
				<G>0.75</G>
				<B>0.75</B>
			</Color3>
			<float name="FogEnd">100000</float>
			<float name="FogStart">0</float>
			<float name="GeographicLatitude">41.7332993</float>
			<bool name="GlobalShadows">true</bool>
			<token name="LightingStyle">1</token>
			<Color3 name="OutdoorAmbient">
				<R>0.5</R>
				<G>0.5</G>
				<B>0.5</B>
			</Color3>
			<bool name="Outlines">false</bool>
			<bool name="PrioritizeLightingQuality">false</bool>
			<float name="ShadowSoftness">0.5</float>
			<token name="Technology">1</token>
			<string name="TimeOfDay">14:00:00</string>
			<BinaryString name="AttributesSerialize"><![CDATA[AgAAACYAAABSQlhfTGlnaHRpbmdUZWNobm9sb2d5VW5pZmllZE1pZ3JhdGlvbgMBIAAAAFJC
WF9PcmlnaW5hbFRlY2hub2xvZ3lPbkZpbGVMb2FkBAEAAAA=]]></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">Lighting</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003ae</UniqueId>
		</Properties>
	</Item>
	<Item class="VideoService" referent="RBXDE4F3AF3E6B84B73A9EEA4BC092CA40B">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">VideoService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003cc</UniqueId>
		</Properties>
	</Item>
	<Item class="LodDataService" referent="RBX2ACD72D7030A48468E3A32EC653A0207">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">LodDataService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be000003cd</UniqueId>
		</Properties>
	</Item>
	<Item class="TestService" referent="RBXFE5336509E1B4A29B15DF2DF5492C8F8">
		<Properties>
			<bool name="AutoRuns">true</bool>
			<string name="Description"></string>
			<bool name="ExecuteWithStudioRun">false</bool>
			<bool name="IsPhysicsEnvironmentalThrottled">true</bool>
			<bool name="IsSleepAllowed">true</bool>
			<int name="NumberOfPlayers">0</int>
			<double name="SimulateSecondsLag">0</double>
			<bool name="ThrottlePhysicsToRealtime">true</bool>
			<double name="Timeout">10</double>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">TestService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000417</UniqueId>
		</Properties>
	</Item>
	<Item class="SerializationService" referent="RBX827BC5230B1C49FC9C87A02FAD2AA999">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<UniqueId name="HistoryId">00000000000000000000000000000000</UniqueId>
			<string name="Name">SerializationService</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			<UniqueId name="UniqueId">11e3e833394f3b5a0a5ca3be00000603</UniqueId>
		</Properties>
	</Item>
	<SharedStrings>
		<SharedString md5="yuZpQdnvvUBOTYh1jqZ2cA=="></SharedString>
	</SharedStrings>
</roblox>